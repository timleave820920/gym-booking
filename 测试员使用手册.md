# 综合训练馆订课系统 · 测试员使用手册

> 面向**第一次接触本小程序**的测试员：用 10 分钟看懂系统是什么、有哪些角色、每个功能怎么操作、测试时要注意哪些规则。
> 配套文档：`README.md`（快速开始）、`测试员常见问题.md`（部署排障）→ 本文只讲"怎么用"。

---

## 一、系统是什么

一个健身馆的**微信小程序**：学员在手机上**预约课程→在线支付→到店签到**；教练在手机上**查看课表→扫码核销签到**；管理员在**网页后台**管课程排期、看运营数据。

```
┌──────────────────────── 微信小程序（手机）────────────────────────┐
│                                                                   │
│  学员端（预约/上课/活动/我的）        教练端（课程/学员/结算）      │
│        │                                    │                    │
└────────┼────────────────────────────────────┼────────────────────┘
         ▼                                    ▼
┌───────────────────────────────────────────────────────────────────┐
│          后端 API（Node.js，云托管服务器 或 本地 127.0.0.1:3000）  │
│          订单/支付/候补/签到/次卡/能量币/消息/画像/埋点…          │
└───────────────────────────────────────────────────────────────────┘
         │
┌────────┴─────────────────────────┐
│  数据库：生产 = MySQL（云托管）   │
│  本地/测试 = SQLite（gym.db）     │
└───────────────────────────────────┘
         │
┌────────┴─────────────────────────┐
│  管理后台（电脑浏览器打开 web/     │
│  courses.html，访问码保护）        │
└───────────────────────────────────┘
```

**一句话**：手机小程序给学员和教练用，网页后台给管理员用，后端 API 是大脑，数据库是记忆。

---

## 二、三种身份

| 身份 | 入口 | 能做什么 |
|---|---|---|
| **学员** | 小程序登录页 | 浏览/预约课程、支付（微信/余额）、退订、候补排位、签到、次卡、能量币、储值、消息、完善画像 |
| **教练** | 小程序登录页（账号喻馥雅，后台绑定教练身份） | 我的课程（签到核销）、我的学员、结算收入 |
| **管理员** | 电脑浏览器打开后台网页 | 运营数据看板、课程设定、排课系统、教练分配 |

**登录分流**（登录页只有两种登录方式，角色由后端账号决定）：

```
登录页 → 勾选协议 → 微信一键登录 / 手机号快捷登录 → 学员端（预约 tab）
        （若该微信账号已被后台设为教练 → 教练工作台）
```

> 手机号登录已合规改造：微信未认证时手机号登录会报错并引导微信一键登录（测试时优先用微信一键登录）。
> 2026-08-18 起登录页已移除昵称快捷登录/演示身份/清空数据库等测试入口，测试账号一律走微信一键登录。

---

## 三、测试环境

| 环境 | 后端地址 | 怎么用 |
|---|---|---|
| **本地开发** | `127.0.0.1:3000` | `cd server && node index.js`（Node ≥22.5），开发者工具编译小程序 |
| **云端生产** | `https://gym-server-297498-11-1469244356.sh.run.tcloudbase.com` | 微信开发者工具真机预览 / 正式版，走云托管 |

> 后端地址唯一配置点：`miniprogram/utils/api.js` 的 `FALLBACK_BASE_URL`。切换环境改这一处后**重新编译**。
> ⚠️ 云端每次 push 后自动重建，**重建期间（数分钟）接口可能报错，且重建完成前访问的是旧版本**——测出"奇怪现象"先等 2-3 分钟重试（详见第八节）。

---

## 四、测试账号

### 4.1 测试账号（数据库账号，微信一键登录）

| 账号 | 身份 | 用途 |
|---|---|---|
| **田立** | 学员 | 主测试账号（有订课/消费记录） |
| **蚂蚁** / **艳子** | 学员 | 多账号场景（候补、并发、互不干扰测试） |
| **喻馥雅** | 教练 | 教练端全部功能（课程/学员/扫码/结算） |

> **怎么用**：登录页勾选协议 → 微信一键登录 → 登录后完善资料弹窗里**把昵称改成对应测试名**（如"喻馥雅"）。
> 账号身份由数据库决定（seed 已建）：学员直接进预约页；教练身份需管理员在后台「教练分配」里绑定后才分流到教练工作台。

### 4.2 假用户（`node server/seed-fake-users.js` 生成）

`fake_1` ~ `fake_5` 随机昵称学员，已预约占满部分场次——**测满员/候补场景的好帮手**（会随机增删，非固定）。

### 4.3 管理员

网页后台需要**访问码**（生产环境 `ADMIN_TOKEN` 已配置，配置方式见《云托管持久化与身份配置.md》）。本地开发未配置时不校验。

---

## 五、学员端功能导览（底部 4 个 tab）

```
┌─────────────────────────┐
│ ① 预约（首页）           │
│  问候语+搜索+课程列表     │
│ ② 上课（我的课程）       │
│  待上课/已完成/候补队列    │
│ ③ 活动（会员中心）       │
│  会员等级/储值/次卡/能量币 │
│ ④ 我的（个人中心）       │
│  头像昵称/画像/成就/消息  │
└─────────────────────────┘
```

### ① 预约 tab
- 顶部**搜索框**：按课程名/教练名/描述搜索（有防抖，输入即过滤）
- **课程卡片**：课程名 + 时间 + 教练 + 价格 + 剩余席位；满员课显示「N人排队」
- 点卡片 → **课程详情页**：大图 + 详情 + 立即预订

### ② 上课 tab（我的课程）
- **待上课**：订的课都在这里，可**退订**（课前 2 小时内不可退）
- **已完成**：历史课程（签到过才算"完成"）
- **候补**：排位中的课（前面还有 N 人）

### ③ 活动 tab（会员中心）
| 功能 | 说明 |
|---|---|
| 会员等级 | 青铜(98折)→白银(95折)→黄金(92折)→钻石(88折)，订课/签到升级，等级越高折扣越大（会员价=原价×折扣向下取整） |
| 储值充值 | 余额支付课程（充值走微信支付） |
| 次卡包 | 买 N 次课包，按次扣（比单次划算） |
| 能量币 | 签到+10、完成课+20、充值每¥100→50币、成功邀请+100、升级+200；能量商店换奖品 |
| 邀请好友 | 好友注册并订课后双方得奖励 |

### ④ 我的 tab（个人中心）
| 功能 | 说明 |
|---|---|
| 头像/昵称 | 点头像换微信头像，点昵称一键填入微信昵称 |
| **资料与生日**（画像） | **一次性表单**：首次填写送 20 能量币 + 生日月首订 8 折；保存后卡片隐藏，点「🎯 资料与生日 ›」可再次展开修改 |
| 我的课程/订单 | 订课记录 / 充值记录 |
| 成就与记录 | 勋章墙 + 锻炼统计（只统计**签到成功**的课） |
| 消息通知 | 站内信（订课成功/候补转正/退款到账/开课提醒） |
| 联系我们 | 场馆微信小助理 + 微信群二维码 |

### 核心流程：订课 → 支付 → 签到

```
课程详情 → 立即预订 → 选择支付方式 → 支付成功 → 我的课程出现该课
                                              ↓
        开课前 30 分钟～课后 30 分钟（签到窗口）→ 学员端扫码页展示签到码
                                              ↓
        教练扫码核销 → 到课确认 → 锻炼统计 +1 / 能量币 + 成长奖励
```

**支付方式**：微信支付（真实链路，商户号未配置时禁用并提示）/ 储值余额 / 次卡。

### 满员怎么办？—— 候补排位

```
课程满员 → 按钮变「N人排队」→ 付费排位（金额=课程价）→ 进入候补队列
                                        ↓ 有人退订
                      最早排位者自动转正（站内信通知）→ 获得席位
                                        ↓ 课程开始仍未排到
                      自动全额退款
```

**注意**：候补是付费排位——转正或退款，钱闭环严格。

---

## 六、教练端功能导览

用账号「喻馥雅」（后台已绑定教练身份）微信一键登录 → 进入**教练工作台**（底部 3 个 tab）：

```
┌─────────────────────────────┐
│ ① 我的课程（默认）           │
│  今天起 7 天日期条 + 课程卡   │
│  窗口内 → 扫码签到按钮        │
│ ② 我的学员                   │
│  已签到学员列表 + 跟课记录     │
│ ③ 结算                       │
│  月份切换 + 课时费/奖励/合计   │
└─────────────────────────────┘
```

| 功能 | 操作 |
|---|---|
| **签到核销** | 课程卡点「签到」→ 相机扫学员二维码 / 手动输 5 位纯数字签到码 → 成功提示 |
| 签到窗口 | 开课前 30 分钟～课后 30 分钟，窗口外无入口/拒绝（**服务器时钟裁决，手机改时间无效**） |
| 学员管理 | 学员 Tab 看已签到学员 + 每人的全部跟课记录，可写备注 |
| 结算 | 按**签到数**计课时费 + 签到奖励（月视图，历史可查） |

---

## 七、管理后台（网页）

电脑浏览器打开 `web/courses.html`（或部署后的后台地址），顶部 **4 个 tab**：

```
┌────────┬────────┬────────┬────────┐
│运营数据 │ 课程设定 │ 排课系统 │ 教练分配 │   ← 默认打开运营数据
└────────┴────────┴────────┴────────┘
```

### ① 运营数据（默认）
- **7 个 KPI 卡**：新增用户/订课率/签到率/留存/储值/确认收入/退款（含环比）
- **趋势图**：近 7/30 天 用户/订课率/签到率/收入（原生 canvas）
- **📋 运营日报**：日期选择 + 重新生成 + 一句话总结 + 关键数据 + 行动建议（红黄绿严重度）
- **4 组折叠卡**：收入线索 / 用户增长（RMF 分层+沉睡双档+画像筛选）/ 课程热度 / 系统健康
- **用户分析**：RMF 分层清单（近度/频次/金额打分）+ 画像筛选（性别/年龄段/生日月）+ 点用户看行为时间线 + 群组勾选发站内信 + CSV 导出
- **⑥ 浏览分析**：曝光→详情→订课→签到漏斗 + 意图人群（看了没订）+ 搜索词 TOP + 热度对比

### ② 课程设定
新增/编辑课程（名称/分类/价格/教练/场馆/描述/轮播图等）。

### ③ 排课系统
按模板或手动给课程排场次（日期/时段/容量/教练/场馆），发布后学员端可见。

### ④ 教练分配
设置/解除教练身份（学员列表「设为教练」+ 档案选择），教练登录分流依据。

---

## 八、测试必知的关键业务规则

| 规则 | 内容 | 测试验证点 |
|---|---|---|
| **签到窗口** | 开课前 30 分钟～课后 30 分钟（服务器时钟裁决） | 窗口外：学员端无入口、教练端拒绝；窗口内：两端正常 |
| **退订截止** | 课前 2 小时（2026-08-18 用户拍板） | 截止前可退（退款=实付）；截止后不可退 |
| **签到=唯一到课证明** | 成长统计只算签到成功的课 | 订了不来 → 不计锻炼次数 |
| **候补** | 付费排位，转正/退款，钱闭环 | 转正后扣款一致；未排到自动全额退 |
| **会员折扣** | 会员价=原价×折扣，向下取整到元 | 支付金额/退款金额一致 |
| **生日月 8 折** | 填了生日且当月 → 生日月内**首笔**订课自动 8 折（与会员价取更优） | 第二单回会员价；非生日月不折 |
| **画像卡一次性** | 填写保存后整卡隐藏，仅留「资料与生日」入口 | 保存后卡片消失、入口可再次展开编辑 |
| **金额单位** | 界面显示"元"，内部"分" | 支付/退款/余额数字一致 |
| **埋点** | 首页曝光/详情停留/搜索词自动采集（登录后） | 运营数据→浏览分析可见漏斗 |

---

## 九、测试注意事项（踩过的坑）

1. **云端重建窗口**：push 后 2-3 分钟接口可能 404/异常，且重建完成前是旧版本——"奇怪现象"先重试。
2. **开发者工具缓存**：新增页面报 `WXML file not found` → 工具 → 清缓存 → 全部清除 → 重新编译。
3. **本地后端与云端数据互相独立**：四套环境（本地/CI/预发布/生产）数据库各自独立，本地测的数据不会出现在云端。
4. **时间敏感测试**：签到/候补过期依赖服务器时钟，真机改时间无效——需要"穿越"测试请找开发用测试代码模拟。
5. **多账号测试**：候补转正、并发抢课用「田立/蚂蚁/艳子」+ 假用户配合。
6. **金额核对**：测试支付后，余额变化 = 支付金额（分位一致）；退款后恢复。
7. **勿删测试数据**：云端是生产 MySQL，管理员后台「清空用户」等危险操作只在本地测试库做。

---

*文档版本：2026-08-18 ｜ 对应 commit 27a673f*
