
## 2026-08-13：DEFINITION-OF-DONE 消费级验收清单（阶段0）

用户问答引导制定：范围=核心交易链路+4大tab页；可靠性=钱正确/数据不丢/状态一致/不崩不白屏/可追溯（用户补充：数据抱怨可回溯定位并给客户满意答复）；体验=异常态/边界态/多端/性能全选；证明=自动化+真机双保险。

产出: DEFINITION-OF-DONE.md（勾选式活文档）。下一步阶段1：测试框架化。

## 阶段1：质量安全网（提交 29928ee）

**1. 测试框架化**: minitest 全套入库（此前被 gitignore 从未提交！）；run-tests.js 从 70 项扩到 77 项，补关键链路：BAL-01 余额不足拒绝、MEM-12 会员价取整实扣¥78（¥80×0.98=78.4→78）、RCG-01/02 充值分页 10/10/5+hasMore。

**2. 覆盖率机制**: 新增 minitest/coverage.test.js（node:test + assert，同进程加载 server——index.js 加 require.main 守卫+module.exports 支持可测试化）。跑法: node --test --experimental-test-coverage。基线: db.js 37.5
## 阶段1：质量安全网（提交 29928ee）

**1. 测试框架化**: minitest 全套入库（此前被 gitignore 从未提交！）；run-tests.js 从 70 项扩到 77 项，补关键链路：BAL-01 余额不足拒绝、MEM-12 会员价取整实扣¥78（¥80×0.98=78.4→78）、RCG-01/02 充值分页 10/10/5+hasMore。

**2. 覆盖率机制**: 新增 minitest/coverage.test.js（node:test + assert，同进程加载 server——index.js 加 require.main 守卫+module.exports 支持可测试化）。跑法: node --test --experimental-test-coverage。基线: db.js 37.5% / index.js 15.5% / 配置类 100%（探针走核心链路）。

**3. 可追溯性（呼应 DoD）**: 新增 server/logger.js logOp()，index.js 埋点 5 处（支付/充值/退款/兑换/签到），写 server/logs/ops.log（时间|openid|操作|详情|结果|单号），gitignore。测试验证 41 条日志含单号金额。

**下一步**: 阶段2（db.js 按域拆分/路由表化）或先扩展覆盖率探针到全部接口。

## 安全网自动化（提交 1187b67）：从「习惯」到「机制」

用户追问「改代码先测红不提交」是否自动化/是否成文——坦白：此前只是 memory 习惯，无机制无文档。本轮落地三层：

1. **机制**: .githooks/pre-commit（git config core.hooksPath .githooks，hook 入库随仓库走）——commit 时自动跑 77 项测试，红则 exit 1 拒交，自动拉起未运行的后端。双向验证：正向(全绿放行) + 负向(临时破坏断言→拦截「禁止提交」→恢复)。
2. **契约**: CONVENTIONS.md 成立——4 条强制规矩（测试闸门/关键操作日志/钱规则前后端一致/新接口必带测试+探针）+ 命名/金额单位(分)/提交格式/错误处理约定。作为「与 AI 协作开发契约」。
3. **联动**: DEFINITION-OF-DONE.md 证明栏引用强制机制（70→77 项更新）。

**经验**: 规矩要「机制化」——口头/文档约定都会被遗忘，pre-commit hook 是 Git 原生、无需第三方依赖、无法绕过的闸门（除非显式 --no-verify）。hook 入库 + core.hooksPath 方案：新克隆需跑一次 git config core.hooksPath .githooks。

## 保证机制 + 架构规模化批1（提交 575b3c9）

**用户要求**: L2/L3 保证不忘（完成时通知）；断言破坏必须告知。

**保证机制（四重冗余）**:
1. TODO.md 加 P1 条目（L2 推送验证/L3 发布闸门/阶段2 架构）
2. CONVENTIONS.md 加「对用户的承诺」节 + 强制规矩#5（断言破坏必须告知：恢复+说明）
3. 项目 MEMORY.md 记录承诺
4. automation 周检查任务（automation-1786585405879，每周日 10:00 检查 L2/L3 进度汇报用户）

**架构规模化批1（db.js 拆分）**:
- server/db-core.js（306行）：连接+建表+courseCols
- server/db/users.js（88行）：9 用户函数
- server/db/coin.js（148行）：9 能量币函数（跨域依赖 findUserByOpenid 从 users 引入）
- db.js 聚合入口 1817→1304 行；index.js 的 db.xxx 调用零改动
- 踩坑：db/ 子目录 require 路径需 ../db-core；coin.js 需显式 require users（跨域函数在拆分后不自动可见）
- 验证：77/77 全绿（行为等价）+ hook 自动放行；提交 575b3c9

**批2 待办**: members（getMemberLevel/addBalance/充值/退款）、orders/bookings/waitlist（最大域）、courses/messages/invite。

## 架构规模化批2（提交 a7deff1）：members/invite/courses 三域拆分

**拆分**: db.js 1304→865 行；新增 db/members.js(150行,9函数+常量)、db/invite.js(107行,5函数)、db/courses.js(210行,16函数)。courses 零跨域依赖最易拆；members 依赖 users/coin/energy-config；invite 依赖 members(addBalance)。

**踩坑（重要经验）**:
1. 行号提取夹带：ENERGY_CONFIG/MEMBER_CONFIG 声明在提取范围内被搬走 → db.js 剩余区报 not defined → 需补回 require。教训：拆分前先 grep 每个全局常量在各区域的引用，声明归属要单独决策
2. 模板头重复声明：模块模板头部 require 与搬入区自带声明重复 → 拆后必查重复
3. 批1残留旧头：脚本 skip 行号未覆盖旧头（1-9行）→ db.js 新旧头并存。教训：skip 起点从 1 开始，或用锚点文本替换而非行号

**验证**: 77/77 全绿（行为等价）+ hook 放行。

**批2b 待办**: bookings/orders/waitlist/messages 域（互有依赖，需惰性访问 dbMod.xxx 模式避免循环依赖）；批4 路由表化。

## 架构规模化批2b 完成（提交 100a165）：db.js 只剩纯聚合入口

**拆分**: messages(77行,8函数)/orders(459行,11函数)/bookings(267行,9函数) 三域。db.js 865→106行（纯聚合头+导出，业务函数全部迁出）。依赖方向：bookings→orders→invite→members→coin/users，无循环（依赖分析证明 orders 不调 bookings）。

**修复链（运行时逐个暴露，已沉淀进 safe-refactor skill）**:
1. ORDER_SELECT SQL 常量被夹带搬进 messages.js → 从 git 原文件取回补入 orders.js
2. module.exports = { 开头被行号范围误搬进 bookings.js → 补回 db.js
3. orders.js 缺 calcRechargeBonus/rewardInviter/sendMessage → 逐一补 require
4. bookings.js 缺 sendMessage → 补
5. db.js 导出残留 ENERGY_CONFIG → 补回 require 保持契约

**关键改进**: 拆完必做"跨域引用静态审计"（grep 引用清单 vs require 头逐名核对），一次性补完，不再靠测试逐个炸。

**状态**: 数据库层模块化 100% 完成（core + 7 域模块 + 聚合入口）。剩余: 批4 路由表化(index.js if-else → 路由表)。

## 架构规模化批4 完成（提交 6009909）：路由表化，阶段2 收官！

**改动**: index.js if-else 40+ 分支 → 声明式 API_ROUTES 路由表（54 条：字符串精确路径 + 正则参数路径；readBody 前置逻辑包进闭包；内联小接口直接入表）。分发循环 + 未匹配回落静态资源/404。行为等价：77/77 全绿 + 404 兜底验证。

**阶段2 架构规模化全部完成**: db.js 1817 行巨石 → 106 行聚合入口 + 8 域模块 + core；index.js 40+ 分支 → 54 条路由表。TODO.md 勾选完成。

**架构收益**: 新接口 = 路由表加一行 + 测试加一例；新域逻辑 = db/ 下建模块。规模化问题（单文件膨胀/线性路由）已结构性解决。

**剩余路线**: 阶段3 生产化（正式小程序 P0 串行 / L2 CI / L3 发布闸门 / 云部署）；风格方向待定。

## L2 CI 上线（提交 b63a4b0，run 31665718847 全绿）

**流程**: gh auth refresh 加 workflow scope（坑：head 截断杀死轮询进程，需后台跑）→ .github/workflows/ci.yml（checkout→setup-node 22→seed→起后端→77测试→覆盖率探针→上传报告）→ 3 轮红灯修复 → 绿。

**L2 首日战绩（抓到 3 个本地测不出的真 bug）**:
1. **orders.session_id**：建表 NOT NULL vs 充值写 NULL——本地旧表宽松掩盖，CI 干净库报错 → 改可空
2. **场次满员 status 从不更新**：全库无任何代码把场次置 full；booked_count 5 处变更点（createBooking/cancelBooking/payOrder×2/joinWaitlist）全部漏联动 → 新增 syncSessionStatus(courses.js) 统一调用
3. **coverage 探针自身 6 处 bug**：缺 http require、clean 列错（invitations 无 user_openid）、下单接口用错(/api/bookings 无 order)、booking 时机(支付后才有)、余额支付变 wxpay、invite 字段(inviter 非 openid)、coin 路径——**此探针创建起从未真正通过**，一直被 grep 过滤掩盖！

**重大教训**: 验证必须看「完整输出 + 退出码」——grep 过滤会掩盖测试失败（探针失败 1 个月级未被发现）。已补充：本地完整跑 + $? 检查。

**当前状态**: 三层闸门 L1(hook)/L2(CI) 全绿；覆盖率基线 59.27%（db.js 100% 行）；剩余 L3 发布闸门 + P0 正式小程序。

## 真bug教训机制化（提交 3d183b8）：回归断言 + 缺陷台账 + 强制规矩

用户追问「第一个真bug是否文档化、是否有自动化保证不再犯」→ 诊断：记录有（memory+commit），但无结构化台账、本地 hook 有 2 个盲区（schema 类问题本地旧表掩盖测不出；满员 status 回归断言只活在 coverage 探针、不在 hook 的 77 项内）。已补齐三件套：

1. **回归断言 +2**（run-tests.js 77→81 项）：MEM-03b 充值订单 session_id 必须为 NULL（bug①）；SEC-04b 订满后场次 status=full（bug②，走下单→支付完整链路）
2. **BUG-LEDGER.md 缺陷台账**：3 个真 bug 五要素（现象/根因/修复/回归测试/防护层）结构化登记
3. **CONVENTIONS.md 强制规矩 #6/#7**：验证必须看完整输出+退出码（bug③教训）；新 bug 必修双保险（回归测试+台账+commit 引用编号）

**踩坑**：SEC-04b 首版直接断言 SEC-04 的 fullSid2 → 红。根因：mkSession(date,start,end,cap,booked) 第5参 booked=1 造的是「初始即满员」场次（插入硬编码 status='published'），并发下单全被拒、无人成功，status 当然不动。且下单只建订单不动余位，booked_count+1 在支付时才发生。改为新造 booked=0 场次走真实链路（下单→wxpay 支付→断言 full）。

**待办（根因级改进）**：本地测试跑在共享开发库上，schema 类 bug（bug①类）本地永远测不出、只能靠 L2 CI 干净库。可选改进：run-tests.js 支持 DB_PATH 指向临时干净库，让 L1 也能抓 schema 回归（已记 TODO）。

## 本地干净库模式上线（提交 3ad7b7e）：L1 也能抓 schema 类 bug

用户拍板「现在做」→ 完成 TODO「本地干净库测试支持」，三层闸门补上最后一块：

1. **db-core.js**：`DB_FILE = process.env.DB_PATH || server/data/gym.db`，目录自动 mkdir
2. **run-tests.js 干净库模式**（设 DB_PATH 时自管生命周期）：删旧临时库 → seed → 独立端口(3100+)起后端 → 健康检查轮询 → 跑测试 → finally kill 后端 + 删库。关键点：调用方把 DB_PATH 传给 run-tests.js **进程本身**，脚本内 require('../server/db.js')（RCG 分页插入/清理用）也指向临时库，与子进程后端一致；尾部 process.exit → process.exitCode（否则 finally 不执行）
3. **pre-commit hook 强制干净库**：不再依赖「开发后端是否在跑」，每次提交都是全新 schema 验证
4. **负向验证（关键）**：临时把 orders.session_id 改回 NOT NULL → 干净库模式本地立即红（`NOT NULL constraint failed: orders.session_id`，MEM-03）→ 改回 → 81/81 全绿。**证明 L1 现在就能抓 schema 类 bug**（此前只能等 CI push）
5. **开发库零污染验证**：干净库模式前后 uid_test 用户 3→3
6. **.gitattributes**：脚本固定 eol=lf（Windows CRLF 会破坏 sh 执行，提交 395e707/9a90f68）

**遗留**：CI workflow 未动（CI 本身干净环境，无需改）；「验证三连」已含干净库跑法（CONVENTIONS 更新）。

## 覆盖率探针扩展（提交 dbc0941）：62.09% → 82.77%，抓到 2 个真 bug！

用户问「开始 L3 前覆盖率怎么解决」→ 扩展 coverage.test.js 从核心链路到全接口（补 6 类缺口：消息中心/邀请完整链路/能量币兑换/管理后台/教练端/候补复杂路径），探针 14 段全走**真实链路**（不再用测试构造绕道）。

**战绩（探针走真实链路抓出 2 个 82 项全绿掩盖的真 bug）**:
- **#4** PUT /api/courses/:id 部分字段更新 500：updateCourse 全字段 UPDATE 传 undefined → node:sqlite 抛错。修复：先查当前行，未传字段 `??` 沿用原值
- **#5** 满员场次（status=full）无法排候补：bug② 修复引入 syncSessionStatus 置 full 后，createOrder/joinWaitlist 只接受 published → 候补整体不可用。**旧测试用「直接插 booked_count=满员+status='published'」场次绕过状态流转，全绿掩盖**。修复：两处状态检查接受 published/full（book 由 remaining 兜底，waitlist 同理）。补回归 SEC-04c（82 项）

**覆盖率结果**：invite 100% / courses 96% / members 94% / orders 86% / coin 82% / index 73% / messages 58%（定时任务分支难触发）/ users 68%。整体 **82.77%**（目标 80%+ 达成）。薄弱区域已记录进 DoD 证明栏。

**踩坑**：① 探针失败残留脏数据（uid_dbg 调试数据）导致 clean 外键失败——调试脚本必须清理 ② Windows taskkill //PID 杀不掉 node 进程，旧后端占 3000 导致「重启了但连的还是旧代码」——用 PowerShell Stop-Process ③ s5 过期场次要 booked_count=1 才满员可排位

**下一步**：覆盖率达标，可开始 L3 发布闸门（真机手测清单）。

## 三问答复 + 机制增强（提交 3caad26）

用户追问：①为何不追 100% 覆盖率 ②环境坑怎么应对 ③台账登记是否自动化、能否放心不再追问。

**答复要点**：①覆盖率是度量不是目标，最后 10-20% 是静态/启动/定时分支，追 100% 边际收益低、测试成本高；关键业务链路已全覆盖 + 薄弱区域记录在 DoD。②环境坑（Windows taskkill 杀不掉 node）已结构规避——干净库模式自管端口不碰 3000；调试路径的应对写进 GLOSSARY 环境坑速查。③**诚实**：检测自动化（hook/CI/探针），登记是 AI 职责非代码自动化——不能拍胸脯「放心」，但已加机制兜底：周检 automation 新增第 5 项台账一致性检查（git log fix 提交 vs BUG-LEDGER 编号）+ 第 6 项覆盖率对比（基线 82.77%，降 >3pp 提醒）。

## L3 发布闸门第一步：环境地图（提交 b986e63）

用户要求开始 L3，但先要「正式运营后有哪几个环境」的清晰定义。读 CLOUD_DEPLOY/README 确认既定路线（本地后端 SQLite → CI → 微信云开发生产，USE_CLOUD 切换，云函数 login/users 已就绪、业务表未迁移）。

产出 ENVIRONMENTS.md（小白语言）：
- **4 环境**：①本地开发（实验室，127.0.0.1:3000+SQLite）②CI 质检（GitHub 自动，L1/L2 闸门）③预发布（真机彩排，L3 手测）④生产（正式小程序+云开发）
- 动作×环境对照表（改代码=①、commit=①触发L1、push=触发L2、真机验收=③、发布/运营=④、查投诉数据=④）
- 发布流水线（①改→L1→L2→③手测→审核→④上线）+ 3 条铁律（改动必须走闸门、四环境数据独立走迁移脚本、密钥按环境隔离）
- **诚实标注**：④生产是「半成」——云开发仅登录/用户就绪，订单/课程/预约等业务未迁移上云（TODO P1 数据迁移脚本+云开发部署）；完整生产=业务全量上云+迁移脚本+正式 AppID/商户号+L3 全绿

**下一步**：L3 真机手测清单（按 DoD 四柱起草）。

## L3 发布闸门落地（提交 e6527e7）

产出 RELEASE-GATE.md（上线执行手册）：DoD 四柱映射总检查表（自动化已证明项预填 ✅ 免重复手测、真机必测项标 📱）+ **RT-1~RT-10 真机手测清单 40+ 项**（登录/浏览/订课支付/充值/候补/签到/退订/我的消息邀请能量币/管理后台/异常多端；每项步骤+预期，必测：支付 RT-3、充值 RT-4、候补 RT-5、签到 RT-6）+ 发布记录归档模板（追加式，长期可追溯）+ 发布前最后三步（红绿灯/配置核对/备份恢复演练）。挂入 CONVENTIONS 关联文档。

**执行状态**：清单就绪，**等待田立真机执行**（需 2 个微信号 + 测试场次准备；AI 陪跑记录）。三层闸门 L1✅/L2✅/L3📋(清单已建、未执行)。

## L3 真机首测（RT-1.1 通过）——抓到 BUG-LEDGER #6

田立真机开测（手机热点网络）。约 8 轮排查后定位根因并修复：

**排查方法论**：手机浏览器通但小程序不通 → 加后端 REQUEST_LOG 确认请求没到 → 真机调试看 Console → 失败日志显示打到 `127.0.0.1`（非局域网 IP）→ 定位 net-config.json 未被打包。

**BUG-LEDGER #6（真机连不上后端）**：net-config.json 在 .gitignore → 微信开发者工具默认过滤 gitignore 文件不打进预览包 → 真机 require 失败回退 127.0.0.1（手机自己）→ ERR_CONNECTION_REFUSED。**模拟器正常是因为模拟器 127.0.0.1=电脑本机，回退值恰好可用——环境差异掩盖 bug 的典型案例**。修复（3df626a/1151d81）：移出 gitignore + .git/info/exclude 本地忽略 + api.js 加 FALLBACK_BASE_URL 写死兜底 + 请求失败打印真实 errMsg + 彻底重启工具清编译缓存。

**附带工具经验**：①扫新码第一次不启动=微信缓存旧包，需删小程序缓存 ②工具编译缓存顽固，改代码不生效时彻底重启工具 ③REQUEST_LOG=1 环境变量可开关请求日志（已留 index.js，正式环境可关）。

**RT-1.1 ✅**：真机微信一键登录成功（demo_user 复用，login_count 30）。文档入档：RELEASE-GATE 手测进度节、GLOSSARY 环境坑、BUG-LEDGER #6 闭环（ccdfe44）。

**信心评估（当前质量防线全景）**：L1 本地干净库 81 项（schema 类可抓）+ L2 CI 干净库 81 项+覆盖率探针 + BUG-LEDGER 台账 + 7 条强制规矩 + 四重保证机制。剩余主要风险：L3 发布闸门未启用、正式小程序未注册（微信支付/云开发前置）。

## 术语库 GLOSSARY.md（提交 08c3de7）

应要求建项目术语库：7 大领域（测试质量/数据库/架构/Git与CI/后端接口/上线部署/协作流程）60+ 术语，每条「一句话人话定义 + 对本项目意义」两段式，含干净库模式、三层闸门、schema 类 bug、seed、事务、状态机等关键知识点。已挂入 CONVENTIONS 关联文档（新成员先读）。约定：新增术语保持两段式。**用户协作偏好更新：重视文档沉淀，术语库是知识资产，值得继续维护。**

## 业务规则基线 B1-B3（提交 6fa2c15）

田立确认「锻炼次数按实际签到算」并点出运营闭环：签到是用户成长（total_classes/等级）与教练收入（课时提成）的**共同锚点**——用户累积成长→要求教练签到；教练要提成→必须如实服务并签到，双向倒逼。已固化进 CONVENTIONS 业务规则基线：B1 签到=唯一到课证明（已落地）、B2 教练提成按签到数结算（**模块未实现**，已记 TODO P1 硬约束：结算数据源=场次名单已签到学员数，需新建教练结算表+规则配置+收入报表）、B3 签到窗口纪律（已落地 #10）。核实结论：后端前端均无提成代码。

## L3 手测进度 RT-1~RT-8 全过（截至 2026-08-13 16:10）

真机手测已过 RT-1（登录/资料展示）/RT-2（浏览课程+满员展示）/RT-3（订课支付-核心）/RT-4（充值+复充10%）/RT-5（候补全链路：排位/转正/退款）/RT-6（签到+窗口保护）/RT-7（退订退款钱闭环）/RT-8（我的/消息/能量币/会员）。累计抓真 bug：#6（net-config 打包）、#7（满员场次列表消失）、#8（套餐展示首充30%）、#9（候补刷钱洞 P0）、#10（签到无时间窗口）、#11（我的页三连）、#12（订课/充值无站内信，埋点被绕过）。**产品决策**：消息中心纯通知列表（点击仅已读不跳转）。**待测**：RT-9 管理后台、RT-10 边界回归；RT-1.4 双账号隔离待双设备。

## L3 收尾：RT-10.2 抓出 P0 狂扣费 + 阶段结论（2026-08-13 16:24）

**#13 狂点狂扣费（P0，RT-10.2 连点测试发现）**：前端 pay() 无防连点锁 + createOrder 查 bookings 表在支付前是空窗（7 次连点=7 个 pending 订单）+ payOrder 幂等只查订单自身状态 → 7 笔扣款 546 元仅 1 个名额。**三层修复**：前端 `_paying` 锁 + createOrder pending 查重 + payOrder 事务内 booked 查重；补 ORD-04b/04c 回归（98→100 项全绿）；现场 6 笔重复扣款已退款 468 元（余额 10644→11112→11024 复测扣 88 元）。**铁律教训：资金链路幂等必须「下单查 pending + 支付查 booked」双层**。**L3 阶段结论**：必测核心链路全过，累计 8 真 bug（#6~#13，含 2 个 P0），RT-9 管理后台田立主动延后、双设备项暂缓（逻辑已被自动化覆盖）。下次发布前需补：RT-9 + 双设备项。三层闸门状态：L1✅ L2✅ L3✅（核心链路）。

**收尾插曲：探针适配 2 处新逻辑（a4ad09e）**——收尾时发现 CI 红了（run 31681763462）：① #10 签到窗口修复后探针 06 段固定 19:00 场次被窗口拒绝 ② be6b062 排课冲突检测后探针 12 段发布规则 10:00 与 seed 场次冲突被跳过。修法：签到场次改「当前+30分」动态窗口时间、发布规则改 14:00-15:00 避开 seed。**教训：改业务规则（窗口/冲突检测）后必须同步检查测试与探针是否适配，否则 CI 红在测试而不是代码**。

## 设计系统 v1 启动（UiDesigner 专家协作 · 977dba1）

**用户目标**：高度统一性 + 明确收敛的风格标志 + 运动健康不高燃 + 耐看不腻。**遗忘旧实验文件**（web/ 下 3 个 design 文件作废），当前基础上逐步迭代。
**现状盘点结论**：32 页约 6000 行 wxss；**20 种字号、15 种圆角、40+ 色值**散乱——统一性差的根源是缺 Token 语法，非单个页面问题。
**提案核心决策**（已获田立拍板）：①品牌三色重新定位——奶油底=画布(不变)、墨黑=骨架(文字/结构/**主按钮**)、荧光绿=能量点缀(选中/装饰/**次按钮**)，荧光绿从 20+ 页大面积铺满降级为限面积点缀，解决"爆/腻"；②按钮 4 平级→5 级语义(L1 墨黑主行动/L2 荧光绿次行动/L3 描边/L4 文字/L5 危险+disabled)；③字号 20→6 级、圆角 15→4 档、间距 8pt 网格；④三签名：S1 奶油画布/S2 墨黑骨架/S3 荧光绿能量。
**批1 已落地（977dba1）**：app.wxss 全套 CSS 变量(--cream/--ink/--lime/--ink-900/500/300/0/语义色/--fs-*/--r-*/--sp-*)，全局类全改 var()；btn-primary 改墨黑+btn-lime/ghost/danger/disabled 新层级；支付页/课程详情页主 CTA 迁移墨黑（登录页本就墨黑）。**发现**：全局 btn-primary 仅 1 页在用，多数页面按钮写死色→批3 页面级套用为主战场。
**批2 已落地（cd81d26）**：按钮 5 级全状态（pressed/active 各色变体 + disabled 合并 + 尺寸阶梯 btn-lg/md/sm）；标准空态组件（empty-state 圆图标/标题/副文案/CTA）；标准加载态（loading-dot 三连点动画 + skeleton 骨架屏）。我的课程页示范迁移（删页面自写空态、继承全局）。**待批3**：32 页逐步套用（高流量页优先：预约/支付/我的/课程详情/登录）。

**批3 已落地（9f7a7a2，23 文件）**：全项目色值收敛完成——①39 处无语义米灰/弱灰 → Token（ink-300/divider/divider-strong）②跨页复用色注册为业务主题 Token 并批量替换：--coin-gold 能量币金 #F8D044（跨 9 页）、--coin-gold-deep #BA7517、--seat-full 满员橙 #EF9F27、--line-warm 暖描边 #C9C7BC ③收尾 11 处残留灰 ④**复用色值 >1 次的散乱色已归零**，34 个 WXSS 结构全 OK。JS 内成就徽章色保留（JS 无法用 wxss 变量，与 Token 同源）。**批3 剩余**：页面组件级统一（空态/加载态/按钮类的逐页套用，消息页等已部分用全局类名）。

**批4 已落地（b76de0a，8 文件 -150 行）**：页面组件统一——删除 8 页自写空态/加载态样式（类名与全局一致改继承全局）；修复批量删除遗留畸形块（教训：正则删块会留 `}.class{` 半截，需循环清理+结构验证）；状态按钮语义确认符合系统（booked=墨黑+绿 / waitlist=紫 / full/ended=灰 / ongoing=红，预约页 cb-btn 荧光绿=列表内 L2 次行动符合设计）。**设计系统 v1 四批全部落地**：批1 Token 基建(977dba1) + 批2 组件积木(cd81d26) + 批3 色值收敛(9f7a7a2) + 批4 页面组件统一(b76de0a)。待：真机视觉确认 + 管理端/教练端低流量页后续可继续收敛。

## 设计系统 v1.1 卡片语法统一（74df2a8，田立四点反馈驱动）

**用户四点反馈**：①预约按钮与席位信息无对齐关系 ②头像+名字+描述太散无重心 ③UI 全扁平缺质感 ④预约页(黑描边卡)与上课页(无描边卡)像两个应用。**根因**：白底卡片描边使用不一致 + 预约页元素过多互相抢戏（黑描边/紫圆点/粗边头像/斜体时间/多态按钮）。
**落地**：预约页卡片重构示范——去黑描边/紫圆点/粗边头像（96→72rpx 浅底）、时间去斜体（34rpx 无衬线）、席位改浅红底徽章、按钮去描边统一 var() 语义色、头部重心让位给课程名。**全项目推广**：白底卡片去黑描边 18 处（10 页统一「无描边白卡 32rpx 圆角 + 轻阴影」语法），黑描边仅保留荧光绿强调块/装饰元素（coin-rules 等品牌签名）。
**技术教训**：Python 批量处理 wxss 时 bash 传参 `\s` 会被 shell 转义导致正则失效，必须用 heredoc（<<'PYEOF'）；`re.sub(r'([^{}]+)\{...')` 贪婪匹配会跨块，改用「逐行收集块 + 块内删行」方式可靠。**白底+描边残留已清零，34 个 WXSS 结构全 OK**。

## 设计系统定稿（34591af，田立认可「还不错」）

DESIGN-SYSTEM.md v1.1 落地：风格定位（奶油画布+墨黑骨架+荧光绿能量，三铁律：白底=无描边卡/一页一墨黑CTA/荧光绿限面积）+ 全 Token 表（品牌/中性/语义/业务主题色 26 个 + 字阶 8 级 + 圆角 4 档 + 间距 8pt 网格 + 阴影 2 档）+ 组件规范（卡片/按钮 5 级/状态按钮/标签/空态/加载/TabBar）+ 页面迁移检查清单 8 项 + 演进记录。已挂入 CONVENTIONS 核心文档（新成员/未来 AI 改样式前必读）。**至此设计系统 v1.1 完整闭环：提案→四批落地→真机确认→文档定稿。**

## 微信小程序专家体检 + 包体积瘦身（ece466e）

**体检结论**：功能/质量/视觉均消费级，但「能开发不能上线」——主包 2.2MB 超 2MB 红线、无分包、无 lazyCodeLoading、图片全本地、微信支付未接真实、无隐私协议。上线三件套（正式注册/真实支付/HTTPS 域名）未启动。
**瘦身落地**：删 6 张 0 引用 placeholder 图（2_269/2_472/2_1409/hyrox_adv_rope/kettlebell/dumbbell 共 543KB），主包 2.2→1.68MB；开启 lazyCodeLoading。**重要教训**：删图前二次核查发现 4 张大编号图（2_1350/1358/1366/1374，462KB）被 utils/mock.js 引用——mock.js 是 6 页共用的「后端不可用降级演示数据」，非废弃文件，故保留这 4 张。**批量删资源必须查清 mock/降级路径引用，不能只看业务页面。** Windows 中文路径下 bash rm + 回收站工具兼容问题，改用 PowerShell Remove-Item 处理。

## 微信隐私合规落地（bb0abf4）

三件套交付：①PRIVACY.md 隐私协议（信息收集清单=openid/昵称/头像/手机号可选/订单/储值/签到，使用/存储/共享/用户权利八项/未成年人/更新/联系，基于代码实际盘点非模板）②miniprogram/privacy.json 官方隐私接口声明（wx.getUserProfile + getPhoneNumber 两个事件，与后台「用户隐私保护指引」需一致）③登录页首次启动隐私弹窗（同意/拒绝/查看全文，requirePrivacy 拦截 login/phoneLogin 入口，storage 记 privacy_agreed）。**合规要点**：项目用了 2 个需声明隐私接口；隐私协议必须与代码事实一致（梳理了 users 表字段 + 登录收集点）。**待办**：正式注册后需在微信后台配置「用户隐私保护指引」与 privacy.json 保持一致。
