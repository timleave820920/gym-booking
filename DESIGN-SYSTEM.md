# DESIGN-SYSTEM.md · 综合训练馆设计系统 v1.1

> **本文件是项目的「视觉宪法」。所有页面改动（人 / AI）必须遵守这里的 Token 与组件规范，禁止在页面里散写颜色、字号、圆角、间距。**
> 适用：小程序 `miniprogram/` 全部页面样式；管理端 `web/` 页面遵循同一套品牌色。
> 技术载体：`miniprogram/app.wxss`（CSS 变量定义处）+ 全局组件类。

---

## 0. 风格定位（先读这段）

**一句话**：奶油画布 + 墨黑骨架 + 荧光绿能量 —— 运动、健康、沉稳、耐看，不爆不腻。

| 关键词 | 含义 |
|---|---|
| 高度统一 | 所有页面的卡片/按钮/标签/空态/加载态用同一套语法 |
| 明确标志 | 奶油底 #F9F4DF 一眼可认；荧光绿只做「能量点缀」 |
| 运动健康 | 主色系 = 墨黑 + 荧光绿，克制有力 |
| 不高燃 | 荧光绿**限面积使用**（点缀/选中/次按钮），大面积主操作用墨黑 |
| 耐看不腻 | 暖底 + 低刺激配色，长时间使用不疲劳 |

**三条铁律**：
1. **白底 = 无描边卡片**（32rpx 圆角 + 轻阴影），黑描边只留给强调块/装饰元素
2. **一个页面只有一个墨黑主 CTA**（唯一决定性动作），其余按钮降级
3. **荧光绿是信号色**——只出现在：选中态、能量币主题、列表内次行动按钮、装饰点缀

---

## 1. 色彩 Token（定义在 app.wxss `page{}`）

### 1.1 品牌色（三色签名）

| Token | 值 | 用途 | 使用禁区 |
|---|---|---|---|
| `--cream` | `#F9F4DF` | 全 App 页面背景 | 不作为文字色/按钮底 |
| `--ink` | `#1A1A23` | 结构色：文字、分割、**主按钮底** | 大面积铺底（会太闷） |
| `--lime` | `#B9FF66` | 能量点缀：选中态、装饰、**次按钮底** | 大面积背景/正文（会刺眼） |

### 1.2 中性色（4 层灰阶）

| Token | 值 | 用途 |
|---|---|---|
| `--ink-900` | `#1A1A23` | 标题/主文字 |
| `--ink-500` | `#6B6B76` | 次级文字/描述 |
| `--ink-300` | `#B0AFA8` | 占位/禁用/弱化 |
| `--ink-0` | `#FFFFFF` | 表面（卡片/输入框底） |

### 1.3 语义色

| Token | 值 | 用途 |
|---|---|---|
| `--success` | `#3D9970` | 成功（已订/已完成标签） |
| `--warning` | `#D97706` | 警告 |
| `--danger` | `#E5484D` | 危险/错误（进行中/删除） |
| `--accent` | `#5B57EB` | 强调（管理端/教练端主题、候补） |
| `--divider` | `#F0EDDF` | 分隔线/浅底/禁用按钮底 |
| `--divider-strong` | `#D9D4C0` | 描边（输入框/弱分隔） |

### 1.4 业务主题色（跨页复用，2013-08-13 收敛注册）

| Token | 值 | 用途 |
|---|---|---|
| `--coin-gold` | `#F8D044` | 能量币主题金（跨 9 页复用） |
| `--coin-gold-deep` | `#BA7517` | 能量币深金（文字/装饰） |
| `--seat-full` | `#EF9F27` | 满员警示橙（席位 0/满员态） |
| `--line-warm` | `#C9C7BC` | 暖描边（输入框/分隔） |

### 1.5 浅色底（标签底/柔化区块，app.wxss 已定义 `.bg-soft-*` / `.tag-*` 使用）

`#E5FFC9`(软绿) `#DCD8FF`(软紫) `#FFE3E3`(软粉) `#FFF0D6`(软黄) `#F0EDDF`(软灰) `#FCEBEB`(浅红底)

> ⚠️ **新增颜色规则**：任何颜色必须满足下列之一，否则禁止引入：
> ① 是上述 Token；② 是浅色底变体（标签/徽章底）；③ 是消息类型分类色（语义图标色）；④ 是活动页 banner 渐变（全 App 唯一的高能量装饰位）。

---

## 2. 字号 Token（基线 28rpx ≈ 14pt）

| Token | 值 | 用途 |
|---|---|---|
| `--fs-display` | 48rpx | 数字大标题（余额/成绩） |
| `--fs-title` | 44rpx | 页面大标题 |
| `--fs-section` | 36rpx | 区块标题 |
| `--fs-card` | 32rpx | 卡片标题/强调数字 |
| `--fs-body` | 28rpx | **正文基线** |
| `--fs-meta` | 24rpx | 元信息/描述 |
| `--fs-caption` | 22rpx | 辅助说明/标签 |
| `--fs-badge` | 20rpx | 徽标/极小字 |

字重仅 3 档：**700 标题 / 600 强调 / 400 正文**。禁止 800/900 或散值（26/30/34/38/40…）。

---

## 3. 圆角 / 间距 / 阴影

### 3.1 圆角 4 档

| Token | 值 | 用途 |
|---|---|---|
| `--r-card` | 32rpx | 大卡（课程卡/我的卡） |
| `--r-box` | 24rpx | 小卡/输入框/二维码容器 |
| `--r-chip` | 16rpx | 标签内嵌/小型元素 |
| `--r-pill` | 999rpx | 按钮/胶囊/头像 |

### 3.2 间距 8pt 网格

`--sp-1` 8rpx / `--sp-2` 16rpx / `--sp-3` 24rpx / `--sp-4` 32rpx / `--sp-6` 48rpx
所有 margin/padding 必须从这 5 档取，禁止散值（12/18/28/36…）。

### 3.3 阴影 2 档

| Token | 值 | 用途 |
|---|---|---|
| `--shadow-card` | `0 6rpx 24rpx rgba(0,0,0,0.05)` | 卡片（无描边白卡） |
| `--shadow-pill` | `0 12rpx 28rpx rgba(26,26,35,0.12)` | 墨黑主按钮 |

> 小程序内投影要克制——**质感来自明度差与密度，不靠堆阴影**。禁止自定义更强阴影。

---

## 4. 组件规范

### 4.1 卡片（白卡语法，全 App 唯一）

```
background: var(--ink-0);        /* 白底 */
border-radius: var(--r-card);    /* 32rpx */
box-shadow: var(--shadow-card);  /* 轻阴影 */
/* 无 border！白底卡片禁止描边 */
```

- 黑描边（3rpx solid #1A1A23）**只允许**出现在：荧光绿/紫底强调块（品牌签名）、装饰元素、头像上的少量点缀
- 卡片内部结构：**一个视觉主角**（标题/大数字），其余元素弱化让位

### 4.2 按钮（5 级语义）

| 层级 | 类 | 底色 | 文字 | 用途 |
|---|---|---|---|---|
| L1 主行动 | `btn-primary` | `--ink` 墨黑 | 白 | **唯一 CTA**（立即订课/确认支付/去选课） |
| L2 次行动 | `btn-lime` | `--lime` 荧光绿 | 墨黑 | 列表内快捷按钮（预约） |
| L3 描边 | `btn-outline` | 白 | 灰 | 弱行动（取消） |
| L4 文字 | `btn-ghost` | 透明 | 灰 | 链接/次要操作 |
| L5 危险 | `btn-danger` | `--danger` 红 | 白 | 删除/退订 |

- 尺寸阶梯：`btn-lg` 88rpx / `btn-md` 72rpx / `btn-sm` 56rpx
- 全状态：`:active` 按压变深、`.btn-disabled` 灰底禁点（全局已定义）
- **每个页面只有一个 `btn-primary`**，其余操作一律降级

### 4.3 状态按钮（课程/场次语义色）

| 状态 | 底色 | 文字 |
|---|---|---|
| 已预订 booked | `--ink` 墨黑 | 白 |
| 候补/排位 waitlist | `--accent` 紫 | 白 |
| 满员 full | `--divider` 灰 | `--ink-300` |
| 进行中 ongoing | `--danger` 红 | 白 |
| 已结束 ended | `--divider` 灰 | `--ink-300` |

### 4.4 标签 tag（5 色）

`tag-green`(成功) `tag-orange`(警告) `tag-purple`(强调) `tag-gray`(中性) `tag-dark`(墨黑+荧光绿字)——22rpx 字、胶囊形、浅底深字。

### 4.5 空态 / 加载态（全局组件，页面禁止自写）

```
空态：.empty-state（圆图标 128rpx + .empty-txt 主文案 + .empty-sub 副文案 + .empty-btn 墨黑CTA）
加载：.loading-tip + .loading-dot 三连点动画；列表骨架 .skeleton
```

### 4.6 TabBar

自定义胶囊：白底 72rpx 圆角容器 + `--lime` active + `--accent` admin-active。学员端/教练端/管理端共用此语法。

---

## 5. 页面迁移检查清单（改任何页面先过一遍）

- [ ] 颜色全部来自 Token（无散写 hex）
- [ ] 白底卡片无描边，32rpx 圆角 + 轻阴影
- [ ] 页面只有一个墨黑主按钮（btn-primary）
- [ ] 荧光绿只用在中性位（选中/能量/次行动）
- [ ] 字号从 8 档取，字重 700/600/400
- [ ] 间距 8pt 网格
- [ ] 空态/加载态用全局组件
- [ ] 状态按钮用语义色（booked/waitlist/full/ongoing/ended）

---

## 6. 演进记录

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-08-13 | v1 | 四批落地：Token 基建(977dba1) + 组件积木(cd81d26) + 色值收敛(9f7a7a2) + 页面组件统一(b76de0a)；主按钮荧光绿→墨黑 |
| 2026-08-13 | v1.1 | 卡片语法统一(74df2a8)：白底卡片全去描边 18 处，预约页卡片重构示范（田立四点反馈驱动） |

*创建：2026-08-13（UiDesigner 专家协作）。本文件与 `GLOSSARY.md`、`CONVENTIONS.md` 并列为核心文档，新成员必读。*
