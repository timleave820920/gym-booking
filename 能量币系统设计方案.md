# 能量币系统 · 设计方案（待确认）

> 版本：v1 ｜ 日期：2026-08-11 ｜ 状态：⏳ 待用户确认关键决策
> 配套：活动页功能创意库.md（游戏化 ④所有权——能量币系统）
> 原则：**所有成长数值独立配置文件**（与 member-config.js 同模式），奖品独立配置

---

## 一、系统定位

**能量币 = 平台通用激励货币**：通过签到/上课/邀请/充值/升级等行为获得，在能量商店兑换奖品。形成「行为 → 赚币 → 兑换」的留存闭环。

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  获取能量币   │ →  │  累计/展示    │ →  │  商店兑换    │
│ 签到/上课/   │    │ 个人中心余额  │    │ 兑换券/实物  │
│ 邀请/充值/升级│    │ 流水明细      │    │ 库存/兑换记录 │
└─────────────┘    └─────────────┘    └─────────────┘
```

---

## 二、数据模型（3 处改动）

### 1. `users` 表加字段
```sql
coin_balance INTEGER DEFAULT 0    -- 能量币余额
```

### 2. 新表 `coin_logs`（能量币流水）
```sql
CREATE TABLE coin_logs (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid TEXT NOT NULL,
  change      INTEGER NOT NULL,          -- 变动（+签到 -兑换）
  balance_after INTEGER DEFAULT 0,       -- 变动后余额
  reason      TEXT DEFAULT '',           -- 签到/上课/邀请/充值/升级/兑换
  ref_id      TEXT DEFAULT '',           -- 关联单号
  created_at  TEXT DEFAULT (datetime('now','localtime'))
);
```

### 3. 新表 `coin_exchanges`（兑换记录）
```sql
CREATE TABLE coin_exchanges (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid TEXT NOT NULL,
  item_id     TEXT NOT NULL,             -- 奖品 ID
  item_name   TEXT NOT NULL,             -- 奖品名快照
  cost        INTEGER NOT NULL,          -- 消耗币数
  code        TEXT UNIQUE,               -- 兑换码（虚拟奖品）
  status      TEXT DEFAULT 'pending',    -- pending 待领取 / claimed 已领取
  created_at  TEXT DEFAULT (datetime('now','localtime'))
);
```

---

## 三、配置结构（2 个独立文件，用户可改）

### 1. `server/energy-config.js` —— 成长数值（获取规则）

```js
module.exports = {
  // 行为 → 能量币（一次性规则）
  earnRules: {
    checkin:    10,     // 每次签到（到店核销）
    attendClass: 20,    // 完成一节课（签到后额外）
    invite:     100,    // 每成功邀请 1 人（叠加储值奖励）
    recharge:   50,     // 每充值 ¥100 → 50 币（按金额折算）
    levelUp:    200     // 每次升级 → 200 币
  },

  // 每日获取上限（防刷币）
  dailyLimit: 500,

  // 能量币有效期（0 = 永久有效）
  expireMonths: 0
};
```

### 2. `server/shop-items.js` —— 奖品配置（独立文件）

```js
module.exports = [
  { id: 'water-bottle',  name: '定制运动水杯',   cost: 300,  type: 'physical', stock: 10,  desc: '到店前台领取' },
  { id: 'week-pass',     name: '周卡（7天不限次）', cost: 800,  type: 'virtual',  stock: 99, desc: '兑换后自动激活' },
  { id: 'towel',         name: '品牌训练毛巾',    cost: 500,  type: 'physical', stock: 20,  desc: '到店前台领取' },
  { id: 'coach-1v1',     name: '私教体验课 1 节',  cost: 1200, type: 'virtual',  stock: 5,  desc: '兑换后联系客服预约' }
  // ... 用户可自行增删改
];
```

> 说明：奖品 `type`：`physical` 实物（到店领取）/ `virtual` 虚拟权益（自动激活/兑换码）

---

## 四、接口设计

| 接口 | 说明 |
|---|---|
| `GET /api/coin/balance?openid=` | 能量币余额 + 今日已获取 |
| `GET /api/coin/logs?openid=` | 流水明细（分页） |
| `GET /api/coin/shop` | 奖品列表（含库存/已兑状态） |
| `POST /api/coin/exchange` | 兑换奖品（扣币 + 生成兑换记录/码） |
| `GET /api/coin/exchanges?openid=` | 我的兑换记录 |

**获取触发点**（复用现有逻辑，自动发币）：
- 签到：`checkinBooking` 成功 → +签到币 +上课币（一条流水）
- 邀请：`rewardInviter` 发储值的同时 → +邀请币
- 充值：`applyRecharge` 发储值的同时 → +充值币（按金额折算）
- 升级：等级变化时检测（登录/等级查询时对比）→ +升级币

---

## 五、前端页面

1. **活动中心**：6 宫格 → 7 宫格（+能量商店），会员卡预览下加「能量币余额」行
2. **能量商店页** `pages/coin-shop`：顶部余额卡 + 奖品网格（名称/币价/库存/兑换按钮）+ 兑换成功弹窗 + 我的兑换记录
3. **个人中心**：能量币余额展示（与储值余额并列）

---

## 六、❓ 待确认问题清单（请逐条回答，推荐值已标注）

### A. 获取规则
| # | 问题 | 推荐值 |
|---|---|---|
| A1 | 签到（到店核销）给多少币？ | 10 币/次 |
| A2 | 完成一节课（签到+上课）额外给多少？ | 20 币/次（签到给10+上课给20=30） |
| A3 | 成功邀请 1 人给多少币？ | 100 币/人（**叠加**现有储值奖励？还是替代？）|
| A4 | 充值给币比例？ | 每充 ¥100 → 50 币（即 5%）|
| A5 | 升级给多少币？ | 每次升级 → 200 币 |

### B. 上限与周期
| # | 问题 | 推荐值 |
|---|---|---|
| B1 | 每日获取上限？（防刷币） | 500 币/天 |
| B2 | 能量币会过期吗？ | 永久有效（暂不过期）|
| B3 | 邀请是否叠加储值奖励一起给？ | 叠加（币+储值双奖励）|

### C. 商店与兑换
| # | 问题 | 推荐值 |
|---|---|---|
| C1 | 奖品清单用我上面的 4 个示例（水杯/周卡/毛巾/私教课），还是你有实际奖品？ | 先用示例，后续可改配置 |
| C2 | 实物奖品怎么领取？ | 兑换后显示「到店前台报手机号领取」|
| C3 | 虚拟奖品（周卡/私教课）怎么发放？ | 自动激活（周卡） / 兑换码（私教课）|
| C4 | 兑换后能退换吗？（退回能量币） | 暂不支持退换 |
| C5 | 活动中心入口：7 宫格（加能量商店）OK？ | OK |

---

## 七、开发量预估

| 项 | 工作量 |
|---|---|
| 后端（表+配置+接口+触发点） | 0.5 天 |
| 前端（商店页+活动中心+个人中心） | 0.5 天 |
| 测试（新增 COIN 用例） | 0.2 天 |
| **合计** | **约 1 天** |

---

*请回答第六节的问题清单（可直接说"全部按推荐值"），确认后立即开工。*
