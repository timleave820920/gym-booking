# ENVIRONMENTS.md · 环境与发布规范

> **一句话看懂**：这个项目正式运营后有 **4 个「房间」**——实验室（开发）、质检站（自动检查）、彩排厅（真机验证）、正式营业的店（生产）。每一件事（改代码、发版本、运营操作）都有它该做的房间，做错房间就会出事。
> **给谁看**：所有参与者（开发/合伙人/运营）。读完这篇，你就知道「这件事该在哪做」。

---

## 一、四个环境的地图（先记住这 4 个房间）

| 环境 | 打个比方 | 它是什么 | 它的职责 | 数据从哪来 |
|---|---|---|---|---|
| **① 本地开发环境** | 你家客厅的「实验室」 | 你电脑上的整套系统：后端跑在 `127.0.0.1:3000`（SQLite 数据库），小程序在微信开发者工具里调试 | **写代码、做实验**——怎么折腾都行，改坏了大不了重启 | 本地数据库文件 `server/data/gym.db`（测试数据，随便造） |
| **② 质检环境（CI）** | 工厂里的「机器质检员」 | GitHub 云端的自动化流水线：每次提交/推送自动跑 82 项测试 + 覆盖率 | **自动把关**——代码不合格就亮红，不许进入下一步 | 临时干净库（每次全新的，不存任何真实数据） |
| **③ 预发布环境** | 演出前的「彩排厅」 | 用**真机**（真实手机）+ 预览二维码完整走一遍核心流程；配置尽量接近生产 | **人肉验收**——机器测不出的体验/交互/多端问题在这里查 | 测试数据，但配置（AppID/云环境）与生产一致 |
| **④ 生产环境** | 「正式营业的店」 | 微信**正式小程序**（真实 AppID）+ **微信云开发**（云数据库 + 云函数） | **服务真实用户**——真实下单、真实收款、真实数据，改错的代价最大 | 腾讯云数据库（用户/订单等全部真实数据） |

> 记忆口诀：**①写代码 ②机器查 ③真人验 ④上线跑**。任何改动都从 ① 出发，经 ② 绿灯、③ 验收，最后才进 ④。

---

## 二、什么动作，在什么环境做（对照表）

| 你要做的事 | 在哪个环境 | 为什么 |
|---|---|---|
| 写代码、改页面、改数据库结构 | ① 本地 | 开发的本职工作，不影响任何人 |
| 跑 82 项自动化测试 | ① 本地（或提交时自动） | 快速反馈，改完马上知道好坏 |
| `git commit`（提交存档） | ① 本地 | **L1 闸门**：hook 自动跑测试，红了拒绝提交 |
| `git push`（推送到云端仓库） | ① 本地 → 触发 ② | **L2 闸门**：CI 在干净库再跑一遍 + 覆盖率 |
| 新功能/大改动后真机体验 | ③ 预发布 | 模拟器 ≠ 真机，支付/扫码/多端问题只有真机才暴露 |
| 上线前的完整手测（L3 清单） | ③ 预发布 | 按 DEFINITION-OF-DONE 逐项打勾 |
| 提交微信审核、正式发布 | ④ 生产 | 只有这里能触达真实用户 |
| 排课、改价格、发优惠、看营收 | ④ 生产（管理后台） | 运营操作直接影响真实营业 |
| 用户投诉了，查这笔订单/退款 | ④ 生产（云数据库） | 数据都在云上，可追溯（DoD 要求） |
| 本地 ↔ 云端数据搬运 | ①②④ 之间（迁移脚本） | 两边数据**不互通**，必须走脚本，不能手动复制 |

---

## 三、一次改动的完整旅程（发布流水线）

```
你在 ① 本地改代码
   ↓
git commit → L1 闸门（hook 自动跑测试）→ 红：打回改
   ↓ 绿
git push → L2 闸门（CI 干净库跑测试+覆盖率）→ 红：打回改
   ↓ 绿
真机预览二维码 → ③ 预发布 真人手测 → 有问题：回 ① 改
   ↓ 全过
提交微信审核 → ④ 生产 发布上线 → 真实用户使用
```

**每个环节都拦得住错误**：① 改错自己知道，② 机器查错，③ 真人查体验，④ 才是最后一道。越早拦住，代价越小。

---

## 四、铁律（三条，违反必出事）

1. **改动必须从 ① 出发，走完闸门才准进 ④**——永远不要跳过测试直接在生产改
2. **四环境的数据库互相独立**——本地 SQLite、CI 临时库、云数据库是三个世界；数据迁移只能走脚本（TODO P1 待完成）
3. **配置按环境隔离**——AppID/AppSecret/云环境 ID/商户号是生产机密，绝不能写死在代码里（密钥走环境变量，已在做）

---

## 五、诚实标注：当前状态与待办（2026-08-13）

| 事项 | 状态 |
|---|---|
| ① 本地开发环境 | ✅ 完备（82 项测试 + 干净库模式） |
| ② CI 质检环境 | ✅ 上线（GitHub Actions 全绿，覆盖率 82.77%） |
| ③ 预发布环境 | 📋 待建（本文件是第一步；L3 真机手测清单待起草） |
| ④ 生产环境 | ⏳ **半成**：云开发只有登录/用户两个云函数已就绪；订单/课程/预约等业务**尚未迁移上云**（TODO P1「数据迁移脚本」「云开发部署」）；正式 AppID 注册、商户号、备案状态以实际为准 |
| **「完整生产环境」的达成标准** | 业务全量上云 + 数据迁移脚本完成 + 正式 AppID/商户号就位 + L3 清单全绿 |

> 也就是说：**现在系统跑在 ①（本地）很完整，但 ④ 还差业务上云这块大工程**。L3 发布闸门做的是 ③ 和 ④ 之间的事——先把「怎么算能上线」定清楚。

---

## 六、公网穿透测试模式（2026-08-14 新增，拉朋友真机测试用）

**背景**：拉朋友真机测试需要后端公网可达。方案演进：Cloudflare Quick Tunnel（境外边缘，慢且断）→ **cpolar 免费隧道（国内节点，稳定 0.26~0.30s）**。

| 项 | 当前值 |
|---|---|
| 公网地址 | `https://35d0ecae.r22.cpolar.top`（免费版域名**重启会变**） |
| 前端地址源 | **唯一人工配置**：`miniprogram/utils/api.js` 的 `FALLBACK_BASE_URL`（局域网/公网切换只改这一处；net-config.json 已废弃，见 BUG-LEDGER #14） |
| 启动方式 | 双击 `server/start-cpolar-tunnel.bat`（启动后窗口显示新域名 → 同步到 api.js → 重新编译） |
| AppID | **测试号** `wxd63f6b720b3021ea`（project.config.json；任何微信可扫预览码，无成员限制） |
| 真机要求 | ①工具勾选「不校验合法域名」②真机小程序右上角「···」→「打开调试」 |

**注意事项**：
- 后端进程不能关；电脑重启后需重新：起隧道（bat）→ 同步 api.js 域名 → 重新编译
- 测试号**不能上传发布**（体验版/正式版需真实 AppID）；正式发布前把 project.config.json 的 appid 换回
- 测试号 secret 仅填 `server/.env`（已 gitignore），不入库
- 假用户测试数据：`node server/seed-fake-users.js`（幂等重置，见开发总结 2026-08-14）

---

*创建：2026-08-13（L3 发布闸门第一步）。环境有变化时更新此文件并同步 CONVENTIONS。*
