# 限时次卡包 · 需求与规则设计（待审批）

> 状态：**设计稿，待田立审批后开发**
> 关联 TODO：P1｜限时次卡包
> 本文档遵循项目硬性约定：钱闭环（扣↔退对称）、关键操作留痕（logOp）、新接口=测试+探针、钱计算前后端同规则。

---

## 一、一句话需求

销售**有时间限制的次卡包**（如 30 天 10 次）；学员订课时支付顺序变为：**次卡次数 → 储值余额 → 微信支付**。

## 二、业务规则（核心，请逐条确认）

### R1 次卡包档位（价格待定，先用占位）
| 档位 | 次数 | 有效期 | 建议价（占位） |
|---|---|---|---|
| 体验卡 | 5 次 | 15 天 | ¥288 |
| 月卡 | 10 次 | 30 天 | ¥580 |
| 双月卡 | 20 次 | 60 天 | ¥1080 |

- 档位在 `class_packages` 表配置（可后台增改），前端动态读取。

### R2 购买与生效
- 购买走现有订单体系：`order_type = 'pass'` → 支付（余额 / 微信）→ **支付成功即时发卡**。
- 有效期 = 购买当天起 N 个自然日（如 30 天），**到期日 23:59:59 失效**。
- 购买后 7 天内可申请退卡（未使用过次数前提下）——**可选规则，默认不做**，请确认。

### R3 使用顺序（订课时）
1. **有可用次卡**（存在 `remaining > 0` 且未过期的卡）→ 扣 1 次，实付 ¥0；
2. 无次卡 / 次数用完 / 全部过期 → 储值余额支付（享会员折扣价）；
3. 余额不足 / 未开通余额 → 微信支付（原价）。
- **用户不可跳过次卡去选余额**（次卡优先是硬规则，防"囤卡不用"）。

### R4 多张卡的扣次顺序
- 用户可持有多张次卡；扣次按 **「最早过期优先」**（FIFO by `expires_at`），同到期日先购先扣。
- 不提供"指定用某张卡"（界面简单优先）。

### R5 订课用次卡不享会员折扣
- 次卡本身已是预付优惠，扣次时不涉及金额、无折扣概念；折扣仅作用于余额支付。

### R6 退订退次（钱闭环）
- 订课支付方式记录在 `bookings.pay_source`（`pass` / `balance` / `wxpay`）。
- 退订时**严格对称**：`pay_source = pass` → **退回 1 次**（该卡 `remaining + 1`，有效期不变）；`balance` → 退余额；`wxpay` → 原路退回（模拟）。
- 退次幂等：同一 booking 只能退一次。

### R7 候补排位与次卡（需确认，推荐"不参与"）
- 候补排位仍用余额/微信占位，**转正后保持原支付方式**，次卡不参与候补。
- 理由：候补转正时机不确定，若转正时次卡刚过期会引发退款纠纷；规则简单优先。
- ⚠️ 备选方案（如果你想做）：转正时若用户有有效次卡 → 退还排位实付金额 + 改扣次卡 1 次。实现复杂度明显上升，**不推荐首期做**。

### R8 过期处理
- 到期未用完的次数**作废**（不退款、不顺延、不冻结）。购买时页面显著提示"过期清零"。
- 到期前 3 天发站内信提醒（复用消息系统）——P2 增强，首期可只发"已过期"通知。

### R9 消息通知（复用现成消息系统）
- 购买成功 → 站内信「次卡已到账：10 次 / 30 天」
- 扣次订课成功 → 站内信「次卡扣次，剩余 X 次」
- 退订退次 → 站内信「已退回 1 次，剩余 X 次」
- 过期 → 站内信「次卡已过期，剩余 N 次已作废」

## 三、数据模型

```sql
-- 次卡包定义（后台配置）
CREATE TABLE class_packages (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,          -- 月卡 / 双月卡
  total_count INTEGER NOT NULL,       -- 总次数
  valid_days  INTEGER NOT NULL,       -- 有效天数
  price_fen   INTEGER NOT NULL,
  active      INTEGER DEFAULT 1,
  created_at  TEXT DEFAULT (datetime('now','localtime'))
);

-- 用户持有的次卡
CREATE TABLE user_passes (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid    TEXT NOT NULL,
  package_id     INTEGER NOT NULL,
  order_id       INTEGER NOT NULL,     -- 购买订单（留痕溯源）
  total_count    INTEGER NOT NULL,
  remaining      INTEGER NOT NULL,
  expires_at     TEXT NOT NULL,        -- 到期 23:59:59
  status         TEXT DEFAULT 'active', -- active / expired
  created_at     TEXT DEFAULT (datetime('now','localtime'))
);

-- 订课记录支付来源（退订对称退款的依据）
ALTER TABLE bookings ADD COLUMN pay_source TEXT DEFAULT 'wxpay';  -- pass / balance / wxpay
ALTER TABLE bookings ADD COLUMN pass_id   INTEGER DEFAULT 0;       -- 扣次来源卡

-- 订单类型扩展
ALTER TABLE orders  -- order_type 已有 book/waitlist/recharge，新增 pass
```

## 四、接口设计（新增 4 个 + 改造 2 个）

| 接口 | 说明 |
|---|---|
| `GET /api/passes/packages` | 可售次卡档位列表（新） |
| `GET /api/passes/my?openid=` | 我的次卡（进行中/已过期，含剩余与到期日）（新） |
| `POST /api/orders`（orderType=`pass`） | 购买次卡下单（改造：新增类型） |
| `GET /api/passes/available?openid=&sessionId=` | 订课前查可用次卡（剩余张数/最优卡）（新） |
| `POST /api/bookings` | 订课：自动优先扣次（改造：支付来源判定） |
| `DELETE /api/bookings/:id` | 退订：按 pay_source 对称退（改造：退次） |

## 五、前端页面

1. **活动中心宫格**新增「次卡包」入口（或与充值并列）；
2. **次卡购买页**：档位卡片（次数/天数/价格）+ 支付方式（余额/微信）+ 「过期清零」显著提示；
3. **我的次卡页**：每张卡显示剩余次数/到期日/进度条，区分「使用中/已过期」；
4. **订课支付页**：有可用次卡时显示「次卡支付 ¥0 · 剩余 X 次」并默认选中，其余支付方式置灰说明「请先用次卡」。

## 六、防刷与一致性

- 扣次**事务内校验** `remaining > 0` 且未过期，并发扣不成负数；
- 扣次/退次**幂等**（订单/订课唯一标识防重）；
- 过期判定用**服务器时钟**；
- 关键操作（购买发卡 / 扣次 / 退次）全部走 `logger.logOp()` 留痕；
- 每新增接口至少 1 条断言进 `minitest/run-tests.js` + 1 行探针 `coverage.test.js`；
- 测试覆盖：购买→发卡、扣次→订课、退订→退次、过期→作废、多卡 FIFO、余额兜底、并发防负。

## 七、需要你拍板的决策点

| # | 决策 | 我的推荐 | 备选 |
|---|---|---|---|
| D1 | 档位与价格 | 5次/15天 ¥288、10次/30天 ¥580、20次/60天 ¥1080（可改） | 你来定 |
| D2 | 候补是否用次卡 | **不用**（转正保持原支付方式） | 转正时改扣次+退排位费（复杂，二期） |
| D3 | 过期次数处理 | **作废不退**（购买页显著提示） | 部分退回储值（金额 60% 转余额） |
| D4 | 多卡扣次顺序 | 最早过期优先（自动） | 用户指定用某张 |
| D5 | 次卡订课享折扣吗 | **不享**（预付已优惠） | 享折扣（不合理，不推荐） |
| D6 | 购买 7 天内退卡 | **首期不做** | 做（未使用可退） |
| D7 | 到期前提醒 | 到期前 3 天站内信 | 只发过期通知 |

---

*设计稿 v1，2026-08-14。待审批后开发；开发顺序：数据表 → 购买链路 → 扣次链路 → 退次链路 → 前端页面 → 测试。*
