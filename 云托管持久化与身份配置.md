# 云托管持久化与登录身份配置（必做，5 分钟）

> 2026-08-16 新增（BUG-LEDGER #25）。解决两个生产环境致命问题：
> **① 用户数据不持久化（登录变"新的号"）② 微信身份未验证（共用 demo_user）**
>
> **2026-08-16 更新（DESIGN #D2）**：生产持久化方向定为**云托管 MySQL**（代码侧已全部就绪：
> `server/mysql-schema.js` 自动建表 20 张 + `driver.ready` 门闩；本地/CI 仍走 SQLite 零依赖）。
> 下面 CFS 挂 `/data` 的 SQLite 方案为**过渡/备选**。两条路都必须配置 WX_APPID/WX_SECRET（第 3 步）。

## 一、为什么必须做

| 问题 | 根因 | 现象 |
|---|---|---|
| 登录变"新的号" | 云托管容器文件系统**不持久化**：闲置缩容/推送重建/扩容都会创建全新容器，SQLite 数据（`server/data/gym.db`）全部丢失，seed 只重建基础数据 | 每次登录提示「注册成功」而非「欢迎回来」，历史订课/会员数据反复消失 |
| 微信身份共用 | 容器内没有 `server/.env`（被 .gitignore 排除），`WX_SECRET` 为空 → code2session 无法换取真实 openid → 所有微信用户回退公共账号 `demo_user` | 朋友登录互相顶号、数据混在一起 |

代码侧已支持（无需改代码）：`db-core.js` 按 `DB_PATH` 环境变量自动建目录并打开数据库（server/db-core.js:12-14）；Dockerfile 已建 `/data` 目录。

## 二、操作步骤（微信云托管控制台）

> **MySQL 路径（DESIGN #D2，推荐）**：
> 1. 云托管控制台 → 左侧「MySQL」→ 开通（设密码，数分钟）
> 2. 记下**内网地址**（`IP:3306`）与账号密码
> 3. 服务 `gym-server` → 服务设置 → 环境变量新增：
>    - `DB_DRIVER=mysql`
>    - `MYSQL_ADDRESS=<内网IP>:3306`
>    - `MYSQL_USERNAME=<账号>`、`MYSQL_PASSWORD=<密码>`（`MYSQL_DB=gym` 可选，默认 gym）
>    - `WX_APPID=wx0aee5332d4ef20fd`、`WX_SECRET=<server/.env 中的 AppSecret>`
>    - `ADMIN_TOKEN=<任意长随机串>`（可选但**强烈建议**：管理网页访问码，BUGS-INBOX #8。配置后 web 管理接口（课程写/场次写/运营读）必须带 `Admin-Token` header 才能访问，网页会弹框输入；未配置则不校验，任何人都能调管理接口）
> 4. **切换前必须先迁移历史数据**：`scripts/migrate-sqlite-to-mysql.js`（dry-run 预览 → 正式执行；可在容器 WebShell 或本地连公网地址执行）
> 5. 保存 → 重新部署 → 冒烟（`bash deploy-smoke.sh` 含登录落库证据）→ 重建验证（#25 验收：重建后数据仍在）
>
> CFS/SQLite 过渡路径见下方第 1-2 步（对接 VPC + 挂载存储）。

> **前置（必读，2026-08-16 实操踩坑）**：所有腾讯云侧操作（建 VPC/建 CFS）必须用**「小程序公众号」登录方式**登录 console.cloud.tencent.com——微信云托管的腾讯云资源在小程序主体账号下，用「微信」方式登录（个人账号）建的资源在云托管下拉里永远看不到；微信云托管仅支持**上海地域** VPC。

1. **对接 VPC（前置）**
   - 云托管控制台 → 服务 `gym-server` → 服务设置 → 基础信息 → **对接 VPC** 开
   - VPC/子网下拉为空时：腾讯云控制台（小程序公众号登录，上海地域）→ VPC 控制台 → 新建 VPC+子网（CIDR 默认）→ 回云托管刷新下拉选择
2. **挂载持久化存储**
   - 腾讯云控制台（小程序公众号登录，上海地域）→ [CFS 文件存储控制台](https://console.cloud.tencent.com/cfs) → 新建文件系统（NFS 协议、选 VPC/子网 = 第 1 步对接的）
   - 云托管控制台 → 服务 `gym-server` → 版本/服务配置 → **存储挂载** → 选择文件系统 → **实例挂载路径填 `/data`**（与 DB_PATH 一致，勿改）
3. **配置环境变量**
   - 服务配置 → **环境变量**，添加：
     - `DB_PATH=/data/gym.db` ← 数据库落持久化盘（关键）
     - `WX_APPID=wx0aee5332d4ef20fd`
     - `WX_SECRET=<server/.env 中的 AppSecret>` ← 从 `server/.env` 复制
3. **保存并重新部署**（控制台点重新部署）
4. **验证**
   - 登录 → 首次「注册成功」；**退出再登录 → 「欢迎回来」**（不再变新号）
   - 两个不同微信登录 → 后端库中 openid 各自独立（GET /api/users 可见）
   - 推送代码触发重建后 → 用户数据仍在（持久化生效）

## 三、注意事项

- **WAL 模式与 CFS**：SQLite WAL 在 CFS（NFS 类）上**单实例**可用；若遇到 `readonly`/`locked`（多实例并发时），将 `server/db-core.js:20` 的 `journal_mode = WAL` 改为 `DELETE` 并重新部署（单实例场景不推荐改）。
- **历史数据迁移**：本地 `server/data/gym.db` 的真实用户数据如需上云，先完成本手册持久化，再按 `上云迁移指南.md` 数据迁移章节操作（迁移前建议先清理 seed-fake-users 假数据）。
- **云开发旧文档作废**：`CLOUD_GUIDE.md`（云函数方案）与 `CLOUD_DEPLOY.md` 已过时，当前生产形态 = 云托管 + callContainer，勿再按旧文档操作。
