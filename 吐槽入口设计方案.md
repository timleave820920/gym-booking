# 吐槽入口设计方案（DESIGN #D9）

> 2026-08-19 提出｜状态：设计中（待确认界面与架构后写入 DEV-BACKLOG）
> 一句话：**学员实名留言吐槽场馆，场馆在 web 后台收件箱逐条回复（承诺每条必回复），学员站内信 + 页内看到回复，闭环。**

## 一、用户拍板口径（2026-08-19）

| 决策点 | 拍板 |
|---|---|
| 入口位置 | 个人中心（与「联系客服」同菜单组） |
| 实名/匿名 | **实名**（后台见昵称头像，可跟进处理） |
| 场馆端回复 | **web 后台收件箱** |
| 回复通知 | **站内信 + 页内展示**（双保险，复用现有消息中心） |

## 二、功能设计

### 2.1 学员端（小程序）

- **入口**：个人中心「联系客服」卡片组新增「💬 吐槽」入口（同白色圆角卡片风格）
- **吐槽页**（`pages/feedback/index`）：
  - 顶部标语：「你的每一条吐槽，场馆都会认真回复」
  - 输入区：textarea（最多 500 字）+ 提交按钮（防连点）
  - 历史列表：我的吐槽 + 状态徽标（`待回复` 灰 / `已回复` 绿）+ 回复内容展示（场馆回复区）
- 提交成功 → toast「已收到，场馆承诺每条必回复」；不强制匿名选项（实名）

### 2.2 场馆端（web 后台）

- **入口**：运营数据 tab 新增「💬 吐槽收件箱」折叠卡（与 📋 运营日报 同级）
- 收件箱列表：未回复优先（倒序）——昵称/头像/时间/内容 + 状态徽标
- 每条展开回复框：填写回复 → 提交（Admin-Token 校验）
- 已回复条目显示回复内容 + 回复人 + 时间
- 顶部统计：待回复 N 条 / 今日新增 N 条（呼应「每条必回复」承诺）

### 2.3 数据模型

```sql
CREATE TABLE IF NOT EXISTS feedbacks (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid TEXT NOT NULL,          -- 学员（服务端取，防伪造）
  nickname    TEXT DEFAULT '',        -- 留言时昵称快照（防改名追溯）
  content     TEXT NOT NULL,          -- 吐槽内容（≤500 字）
  status      TEXT DEFAULT 'open',    -- open 待回复 / replied 已回复
  reply       TEXT DEFAULT '',        -- 场馆回复
  replied_at  TEXT DEFAULT '',        -- 回复时间
  reply_by    TEXT DEFAULT '',        -- 回复人（后台会话名）
  created_at  TEXT DEFAULT (datetime('now','localtime'))
);
CREATE INDEX IF NOT EXISTS idx_feedbacks_status ON feedbacks(status, created_at DESC);
```

- SQLite：db-core.js 建表；MySQL：mysql-schema.js 同步（双方言）
- 不扩 messages 表（该表是单向通知，无回复语义），独立 feedbacks 表；回复后向 messages 写一条站内信（type=feedback，可跳转吐槽页）

### 2.4 接口

| 接口 | 说明 |
|---|---|
| `POST /api/feedback` | 学员发吐槽：openid 服务端取，nickname 快照，≤500 字校验；防连点幂等（同用户 5 秒内重复内容去重） |
| `GET /api/my-feedbacks` | 学员自己的吐槽列表（含回复内容、状态、时间倒序，分页） |
| `GET /api/admin/feedbacks?status=open&offset=0` | 后台收件箱（Admin-Token 校验；未回复优先倒序） |
| `POST /api/admin/feedbacks/:id/reply` | 后台回复（Admin-Token 校验）：回复落库 + status→replied + 写站内信（title「你的吐槽有回复了」+ 跳吐槽页）；已回复不可重复回（幂等） |

## 三、界面草图

### 学员端吐槽页
```
💬 吐槽 · 你的每条吐槽场馆都会认真回复
┌──────────────────────────────────┐
│ 有什么想说的？                     │
│ [textarea 多行输入……]             │
│ 0/500                            │
│          [ 提交吐槽 ]              │
├──────────────────────────────────┤
│ 我的吐槽                          │
│ ┌────────────────────────────┐   │
│ │ 8/19 20:12  [待回复]         │   │
│ │ 更衣室吹风机坏了，修一下呗    │   │
│ └────────────────────────────┘   │
│ ┌────────────────────────────┐   │
│ │ 8/18 09:30  [已回复]         │   │
│ │ 晚课结束后加开一班？          │   │
│ │ ┌──────────────────────┐   │   │
│ │ │ 场馆回复：已排入下周课表   │   │
│ │ └──────────────────────┘   │   │
│ └────────────────────────────┘   │
└──────────────────────────────────┘
```

### web 后台收件箱（运营数据 tab 折叠卡）
```
💬 吐槽收件箱（待回复 3 · 今日新增 5）   [只看未回复 ▾]
┌─────────────────────────────────────────────────┐
│ 阿凯  8/19 20:12  [待回复]  更衣室吹风机坏了……    │
│   [回复框……]  [提交回复]                         │
│ 小萌  8/19 18:03  [待回复]  团课太晚，建议提早     │
│   [回复框……]  [提交回复]                         │
│ 老王  8/18 09:30  [已回复]  晚课加一班？           │
│   ✅ 已回复：已排入下周课表（运营·李 8/18 15:00）  │
└─────────────────────────────────────────────────┘
```

## 四、测试计划

- **FBK-01**：发吐槽落库（实名 openid + 昵称快照 + 状态 open）
- **FBK-02**：超 500 字 → 400 拒绝
- **FBK-03**：我的吐槽列表返回内容/状态/回复
- **FBK-04**：后台列表未回复优先
- **FBK-05**：回复 → status=replied + 回复内容 + 站内信生成（messages 表查 type=feedback）
- **FBK-06**：重复回复已回复条目 → 幂等拒绝（不重复写站内信）
- **FBK-07**：未登录发吐槽 → 400
- **FRONT-xx**：个人中心入口 + 吐槽页 + web 收件箱折叠卡静态断言
- coverage 探针：feedback 接口

## 五、实施步骤（确认后写入 DEV-BACKLOG）

1. **P0｜数据层**：feedbacks 建表（双方言）+ messages 站内信写入
2. **P0｜学员端**：个人中心入口 + 吐槽页（发/历史/回复展示）
3. **P1｜后台**：web 收件箱折叠卡 + 回复接口（Admin-Token）
4. **P1｜测试**：FBK-01~07 + FRONT 断言 + coverage 探针

## 六、待确认点

1. 运营数据 tab 内以折叠卡形式放收件箱（不动现有 4 tab 顺序），认可？
2. 吐槽页历史用「我的吐槽」页面内列表（不占消息中心），认可？
3. 站内信点击跳吐槽页；学员端消息中心会多一类消息，认可？
