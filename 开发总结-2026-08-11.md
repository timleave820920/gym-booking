# 综合训练馆订课系统 · 开发总结（2026-08-11）

> 今日主题：**产品化迭代** —— 管理端网页重构、消息系统一期、邀请裂变、钱链路修复（P0）、大量体验打磨。
> 当天提交 20+ 个（含并发会话），全部推送 GitHub（`timleave820920/gym-booking`，master）。
> 详细逐轮记录见 `.workbuddy/memory/2026-08-11.md`。

---

## 一、今日成果总览

| 领域 | 成果 |
|---|---|
| 管理网页 | 3000 网页重构为双 Tab（课程设定 / 排表管理）+ 邀请看板 Tab；图片本地上传；场次取消/改容量/范围筛选 |
| 消息系统 | 一期（站内信）上线：消息表+接口+5 处业务埋点+开课提醒定时任务（课前 2h）+ 消息中心页+未读角标 |
| 邀请裂变 | 分享卡片带 `?inviter` 参数 + 登录页邀请码兜底 + 后端绑定防刷 + 首订自动发奖（阶梯储值+能量币）+ 邀请看板 |
| 钱（P0） | 修复支付成功页金额错误；订单金额落实付；**修复"退款只改状态不退钱"重大缺陷**；E2E 全链路验证 |
| 内容 | 课程描述/卖点标签接入课程条与详情页；AI 生成课程封面 4 版 |
| 体验 | 个人中心精简与拆单、联系客服页（真实二维码长按识别）、能量币规则页、活动中心改版、消息中心交互改版 |
| 文档 | UX 决策清单（22 项）、消息系统设计方案、TODO 更新、本开发总结 |
| 数据维护 | demo_user 昵称改「田立」；清理 6 个历史重复账号（残留数据已迁移） |

---

## 二、功能实现清单

### 1. 3000 管理网页（web/courses.html）
- 双 Tab：**课程设定**（左列表+右表单：基本信息 / 封面上传+预览+规格提示 / 描述多行输入 / 卖点标签）+ **排表管理**（每周规则表格增删改、起止日期发布场次（幂等）、场次列表筛选、行内改容量、取消场次仅限无人预约）
- **邀请看板**：5 张指标卡（总邀请/已首订/转化率/参与邀请人/已发放奖励）+ 近 14 天趋势柱状图 + 邀请人 Top10 + 用户选择器→邀请明细
- 新接口：图片上传、场次范围/取消/改容量、规则单独保存、邀请看板、邀请明细

### 2. 消息系统一期（站内信先行）
- `messages` 表（`dedup_key` 去重）+ 列表/未读/已读/全部已读接口
- 5 处业务埋点：订课成功、退订退款、签到成功、候补转正、候补过期退款
- 开课提醒定时任务（每 60s 扫描课前 2h 场次，每场次只发一次）
- 消息中心页（类型图标/未读点/全部已读按钮/分页/空态）+ 个人中心入口未读角标（99+ 截断）

### 3. 邀请裂变 + 看板
- A 方案：`onShareAppMessage` path 带 `?inviter=openid` → `app.onLaunch` 捕获 → 登录页预填 → 登录后自动 `bindInvite`
- C 方案：登录页邀请码选填输入框兜底
- 后端校验：邀请码非空 / 不能邀自己 / 邀请人必须真实存在 / 一人一绑定
- 奖励闭环（原有）：好友首订 → 阶梯储值（1人¥100/3人¥500/5人¥1000）+ 能量币 100

### 4. 钱链路修复（P0，重点）
- 支付成功页改为显示**实付金额**（余额支付=会员折扣价）
- `payOrder` 订课后订单金额落实付（余额=折扣价、微信=原价），与 booking 一致
- **退款真正退钱**：退订/退出候补/过期退款三处，余额支付退回余额（新增 `refundOrderMoney`）
- E2E 验证：原价 ¥90 → 98 折实付 ¥88 → 扣款 88 → 退订 → 余额全额回 3822

### 5. 体验打磨（若干）
- 个人中心：去掉演示角色入口、会员卡/等级/储值菜单、右上角铃铛/齿轮按钮；「我的课程」=购买课程订单、「我的订单」=充值记录
- 联系客服页（小助理/用户群真实二维码，长按识别）；能量币规则页（图标+奖励+「我会好好练」）；活动中心页（自定义导航大标题左对齐、宫格去订单）
- 消息中心：消息不可点击、全部已读按钮移到标题右侧（带对勾图标）、修复按钮被胶囊遮挡

---

## 三、踩过的坑（今日沉淀）

### 🔴 钱相关（最严重）
1. **支付成功页显示原价而非实付**：页面直接渲染 `course.price`，而余额支付实际扣的是会员折扣价 → 页面金额 ≠ 实付。
2. **订单金额从未落实付**：`orders.amount_fen` 一直是原价，只有 booking 和余额扣款用了折扣价 → 三处数据不一致。
3. **退款只改状态不退钱（重大缺陷）**：退订/退出候补/过期退款三处只 `UPDATE status='refunded'`，钱从来没退回用户 → 已修复并补退测试遗留金额。
4. **JS 作用域 bug 导致退款报 500**：`const refundOrder` 声明在 try 块内、try 外使用 → `ReferenceError: refundOrder is not defined`，钱没退成还返回 500 → 变量必须声明在外层。

### 🟠 进程/环境坑
5. **微信开发者工具打不开**：7 个僵尸 `wechatdevtools.exe` 进程 + 残留 lockfile 互相抢锁 → 修法：结束全部相关进程（锁自动释放）→ 重启。
6. **PowerShell 中文输出被吞**：`Get-Process` 过滤结果空白导致误判"没有进程"，实际有 7 个僵尸进程 → 修法：输出写文件再读（`Out-File` + `cat`）。
7. **安全删除拦截器路径混淆**：Bash `rm` 删中文路径文件时被 safe-delete 拼错路径失败 → 改重命名（rename）或 PowerShell 精确删除。
8. **后台服务实例反复被顶掉**：与并发会话抢 3000 端口，我的后台实例多次退出（用户两次报"server 没启动"）→ 用 PowerShell 检查监听进程 + 健康检查确认版本。

### 🟡 前端逻辑坑
9. **消息中心首屏卡"加载中"**：`data.loading` 初始为 true，`loadMore()` 守卫 `if (this.data.loading) return` 把首屏请求自己拦死 → 守卫改为"仅在有数据时防重"。
10. **消息中心按钮被胶囊遮挡**：页面缺 `.page-pad` 左右边距，按钮贴最右缘 → 补边距 + 按钮移到标题右侧。
11. **活动中心标题改不动**：根因是「活动」Tab 指向的 member-center 用了**原生导航栏**（标题居中、字号固定），页面样式改不了 → 改 `navigationStyle: custom` + 手写大标题。
12. **Banner 左对齐没对齐**：banner 内部 40rpx 内缩导致标题相对页面正文右移 → 去内缩与正文对齐。

### 🟡 流程/数据坑
13. **ImageGen 并行调用同名覆盖**：同秒生成的图片文件名相同，4 张只剩 1 张 → 必须逐张生成。
14. **邀请链路是断的**：`bindInvite` 接口一直存在但前端从未调用（邀请码复制了没人能填）→ A+C 方案补全传播与绑定入口。
15. **demo_user 昵称变"学员"**：固定 openid 时新建账号未带微信昵称，历史迁移只搬订课没搬昵称 → 直改昵称 + 清理 6 个重复账号。
16. **并发会话冲突**：多次出现"upstream is gone"（跟踪分支被删）、我的提交与并发会话互相覆盖工作区 → 每次操作前确认 `git status` 与最新文件状态。

---

## 四、遗留技术债 / 待办（详见 TODO.md）

- **P0**：注册正式小程序（个体工商户）+ 微信商户号 → 解锁微信支付、云开发、订阅消息、真实手机号
- **P1**：真机回归测试（模拟器已全通）；课程搜索；订单独立详情页；退订规则弹窗；数据迁移/云部署
- **P2**：订阅消息版开课提醒、邀请海报小程序码（方案 B）、优惠券、课程评价、到课率统计、操作日志、包体积优化

---

## 五、明日建议

1. 若合伙人确认 UX 清单，先处理被调整项；
2. 启动**正式小程序注册**（个体工商户）—— 这是支付/订阅消息/云开发的前置，周期长，宜早不宜迟；
3. 真机双账号验证邀请裂变闭环（A 邀 B → B 首订 → A 到账）。

---

*整理：2026-08-11。详细逐轮记录见 `.workbuddy/memory/2026-08-11.md`。*
