# 消息系统设计方案

> 2026-08-11。待合伙人确认后实施。
> 现状：个人中心已有「消息通知」入口（未接线）；无消息表、无推送。

---

## 一、值得通知用户的消息类型（重大消息清单）

### 1.1 自动消息（系统触发，无需人工）

| # | 类型 | 触发时机 | 示例文案 | 推送机制 | 优先级 |
|---|---|---|---|---|---|
| M1 | **候补转正** | 有人取消，候补自动转正 | 「你候补的 Hyrox冲冲冲 20:00 已自动转正，记得按时到场」 | 站内信 + 订阅消息 | ★★★ |
| M2 | **开课提醒** | 开课前 30 分钟 | 「20:00 Hyrox冲冲冲 即将开课，提前 15 分钟到场」 | 订阅消息 + 站内信 | ★★★ |
| M3 | **退款到账** | 退订/退出候补/过期退款成功 | 「退订成功，¥80 已原路退回」 | 站内信 + 订阅消息 | ★★★ |
| M4 | **订课成功** | 支付完成 | 「已成功预约 Hyrox冲冲冲 明天 10:00」 | 站内信（即时页已有反馈，信留档） | ★★ |
| M5 | **签到成功** | 教练核销 | 「签到成功，训练愉快！累计已锻炼 N 次」 | 站内信 | ★★ |
| M6 | **候补过期退款** | 开课仍未排到自动退款 | 「本期候补未排到，¥80 已自动退回」 | 站内信 + 订阅消息 | ★★★ |
| M7 | **储值到账/余额变动** | 充值成功、消费扣款 | 「储值 ¥500 已到账，余额 ¥1,280」 | 站内信 + 订阅消息 | ★★ |
| M8 | **会员等级变动** | 升级/降级 | 「恭喜升级为黄金会员，享 8.5 折」 | 站内信 | ★★ |
| M9 | **成就达成** | 徽章解锁、连续打卡里程碑 | 「达成成就：连续训练 7 天」 | 站内信 | ★★ |
| M10 | **邀请奖励** | 好友注册/消费后奖励发放 | 「好友已加入，能量币 +50 已到账」 | 站内信 | ★★ |

### 1.2 人工/运营消息（管理员或系统广播触发）

| # | 类型 | 触发时机 | 示例文案 | 推送机制 | 优先级 |
|---|---|---|---|---|---|
| M11 | **课程取消/调课** | 管理员取消/调整场次（未来功能） | 「你预约的 20:00 场次已取消，费用将自动退回」 | 站内信（必达） | ★★★ |
| M12 | **系统公告** | 版本更新、维护、规则变更 | 「系统已升级，新增能量商店」 | 站内信广播 | ★★ |
| M13 | **优惠活动** | 新课程、活动、优惠券 | 「夏日训练营上线，首节 5 折」 | 站内信（低打扰，可退订） | ★ |

---

## 二、推送机制设计（分级）

| 机制 | 说明 | 适用类型 | 当前阶段可行性 |
|---|---|---|---|
| **① 站内信**（主渠道） | 写入 messages 表，消息中心列表 + 红点角标，**无需授权、100% 触达** | 全部 M1-M13 | ✅ 现在就能做（本地后端） |
| **② 微信订阅消息**（提醒渠道） | 一次订阅推一条：用户在关键节点授权（如转正时点订阅），后端调 `subscribeMessage.send` 推送到微信服务通知 | M1/M2/M3/M6/M7 高价值提醒 | ⚠️ 需要正式小程序 + 订阅消息模板审核；**测试号阶段先用站内信** |
| **③ 即时反馈**（非消息） | 页面内 toast / 支付成功页 / 签到页，操作当场反馈，不入消息中心 | 支付成功、签到等即时场景 | ✅ 已有 |
| **④ 长期订阅**（未来） | 一次性订阅数量受限，长期订阅需特定类目（如体育健身类可申请），到期续订管理 | 开课提醒 | 视类目资质 |

**关键决策**：**站内信是基础（现在实现），订阅消息是增强（正式小程序后再接）**。所有消息先落站内信，订阅消息作为可选叠加通道，同一类型同时支持两种机制（站内信必达 + 订阅消息推送）。

---

## 三、数据模型

```sql
-- 站内信
CREATE TABLE messages (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid TEXT NOT NULL,          -- 收件人（broadcast 时逐人落库）
  type        TEXT NOT NULL,          -- booking/waitlist/order/member/system/promo
  title       TEXT NOT NULL,          -- 标题（列表展示）
  content     TEXT NOT NULL,          -- 正文
  biz_type    TEXT DEFAULT '',        -- 业务关联：course/order/recharge/...
  biz_id      INTEGER DEFAULT 0,      -- 关联 id（跳转用：订单id/场次id）
  jump_url    TEXT DEFAULT '',        -- 跳转页面路径（/pages/student-orders/index 等）
  is_read     INTEGER DEFAULT 0,
  created_at  TEXT DEFAULT (datetime('now','localtime'))
);
CREATE INDEX idx_messages_user ON messages(user_openid, created_at DESC);

-- 订阅消息模板配置（正式小程序后启用）
CREATE TABLE subscribe_templates (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  msg_type    TEXT NOT NULL,          -- 对应 M1/M2/M3...
  template_id TEXT NOT NULL,          -- 微信模板ID
  scene       TEXT DEFAULT 'once',    -- once=一次性 / long=长期
  enabled     INTEGER DEFAULT 1
);

-- 用户订阅记录
CREATE TABLE user_subscriptions (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid TEXT NOT NULL,
  template_id TEXT NOT NULL,
  remain      INTEGER DEFAULT 1,      -- 剩余可发次数（一次性订阅=1）
  updated_at  TEXT DEFAULT (datetime('now','localtime'))
);
```

**消息产生方式**：在业务代码的关键节点（订课成功/退订/转正/退款/签到/储值）调用 `notify(user_openid, type, ...)`，内部同时做：落站内信 + （若配置且用户有订阅额度）发订阅消息。

---

## 四、消息中心页面设计

### 4.1 页面结构
- **入口**：个人中心「消息通知」；入口处显示**未读角标**（红点+数字）
- **顶部**：大标题「消息中心」+ 右上「全部已读」
- **分类 Tab**：全部 / 课程 / 订单 / 系统（按 type 分组）
- **消息列表项**：
  - 左侧：类型图标（课程=铃/日历、订单=¥、候补=转、系统=公、活动=旗）
  - 中部：标题（14px 加粗）+ 摘要（12px 灰，1-2 行省略）+ 时间（今天 HH:MM / 昨天 / 日期）
  - 右侧：未读蓝点（已读后消失）
  - 未读项背景轻微高亮，已读变灰
- **点击行为**：跳转关联页面（候补转正→上课页；退款→订单页；公告→不跳转，仅读全文）；点击即标记已读
- **空态**：无消息时显示「暂无消息」插画提示
- **分页**：每页 20 条，上拉加载

### 4.2 未读角标
- 个人中心入口 + 各 Tab 页顶部显示未读数（`GET /api/messages/unread-count`）
- 小程序启动/onShow 时拉取；已读后刷新归零

### 4.3 后端接口
```
GET    /api/messages?openid=&type=&page=   消息列表（分页）
GET    /api/messages/unread-count?openid=  未读数
POST   /api/messages/:id/read              标记已读
POST   /api/messages/read-all              全部已读
POST   /api/messages/send                  内部/管理员发消息（含 broadcast 全员）
```

---

## 五、待确认点

1. **分类 Tab**：需要「全部/课程/订单/系统」四个 Tab，还是只要单一列表？（建议：先单一列表+顶部筛选，简单）
2. **订阅消息范围**：正式小程序后，先接哪几个高价值类型？（建议：候补转正 + 开课提醒 + 退款到账 3 个）
3. **开课提醒触发**：定在课前 30 分钟还是 1 小时？（建议 30 分钟）
4. **运营消息（M13）**：是否需要订阅/退订机制（免打扰开关）？（建议二期做）
5. **消息留存**：是否保留全部历史（建议保留，已读不删，可选"清空已读"）

---

*方案状态：待合伙人确认。确认后按「站内信先行」实施（消息表 + 接口 + 消息中心页 + 各业务节点埋点），订阅消息留接口待正式小程序。*
