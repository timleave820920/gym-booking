# 季卡/年卡设计方案（DESIGN #D14）

> 2026-08-19 提出｜状态：✅ 已确认（2026-08-19 用户拍板 3 决策 + 3 待确认点全认可，任务已写入 DEV-BACKLOG.md #D14）
> 一句话：**季卡/年卡拥有者在有效期内无限次订课（0 元），同一时间只能订一堂课（全部订课时间查重）。**

## 一、用户拍板口径（2026-08-19）

| 决策点 | 拍板 |
|---|---|
| 时长 | 季卡 3 个月 / 年卡 12 个月（购买日起算） |
| 价格 | **运营配置**（后台可改，先按次卡价格体系定默认值） |
| 同一时间只能订一堂 | **全部订课查重**——该学员所有已订未退订课（含普通单次/次卡订的课）时间区间重叠即拒绝 |
| 购买入口 | 活动中心「次卡包」页新增季卡/年卡分区 |
| 订课计费 | **有卡自动用**——有有效季卡/年卡 → 订课默认 0 元走卡，无需选择 |

## 二、数据模型

```sql
-- 1) 卡档位（价格运营配置，幂等种子）
CREATE TABLE IF NOT EXISTS unlimited_plans (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  type       TEXT NOT NULL,          -- season / year
  name       TEXT NOT NULL,          -- 季卡 / 年卡
  months     INTEGER NOT NULL,       -- 3 / 12
  price_fen  INTEGER NOT NULL,
  active     INTEGER DEFAULT 1,
  created_at TEXT DEFAULT (datetime('now','localtime'))
);

-- 2) 用户持有的卡（单用户单卡，到期重购续期：旧卡到期后购买新卡覆盖/续期）
CREATE TABLE IF NOT EXISTS unlimited_passes (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid TEXT NOT NULL,
  type       TEXT NOT NULL,
  order_id   INTEGER DEFAULT 0,
  start_at   TEXT NOT NULL,          -- 生效（购买）日
  expires_at TEXT NOT NULL,          -- 到期日 23:59:59（北京时间，time.js）
  status     TEXT DEFAULT 'active',  -- active / expired
  created_at TEXT DEFAULT (datetime('now','localtime'))
);
```

- 有效判定：`status='active' AND expires_at > 当前时刻`（time.js 北京时间，BUG-LEDGER #28）
- 已过期自动转 expired（复用 expireOverduePasses 同款任务或查询时惰性判定）
- 双方言建表：SQLite db-core.js + MySQL mysql-schema.js

## 三、接口设计

| 接口 | 说明 |
|---|---|
| `GET /api/unlimited/plans` | 卡档位列表（价格动态读取，改配置即生效） |
| `POST /api/orders`（order_type='unlimited'） | 购买季卡/年卡下单（复用订单系统） |
| payOrder 扩展 | order_type='unlimited' 支付成功 → 发卡（旧卡未过期则续期顺延 / 已过期则重开） |
| `GET /api/unlimited/my` | 我的卡：有无/类型/有效期/剩余天数 |
| 订课接口扩展 | 有有效卡 → 订课/候补金额 0 元（pay_source='unlimited'），无需支付选择 |

## 四、订课规则（核心）

### 4.1 时间冲突查重（「同一时间只能订一堂课」）

订课/候补提交时（服务端校验，事务内）：

```sql
-- 学员所有已订未退（status='booked'）订课中，与本场次时间区间重叠的计数
-- 重叠条件：date 相同 AND b_start < s_end AND b_end > s_start
SELECT COUNT(*) FROM bookings b
  JOIN course_sessions s2 ON b.session_id = s2.id
  WHERE b.user_openid = ? AND b.status = 'booked'
    AND s2.date = ?
    AND s2.start_time < ?   -- 新场次 end
    AND s2.end_time   > ?   -- 新场次 start
```

- 命中 ≥1 → 400「该时段已订其他课程，同一时间只能订一堂课」（返回冲突课名，前端提示）
- 不重叠的课同一天可订多堂
- 查重覆盖**全部订课**（普通/次卡/候补转正后的都算），不区分来源
- 候补也查重（候补占位同样防重叠）

### 4.2 计费

- 有有效卡 → 订课/候补 **0 元**（pay_source='unlimited'，实付=扣款=退款=0 三一致 BUG-LEDGER #9）
- 无卡 → 现有流程（次卡→余额→微信）不变
- 有卡但选次卡？——「有卡自动用」：订课提交时后端优先判卡（有卡直接 0 元），前端不展示支付方式选择

### 4.3 退订

- 退订截止（开课前 2 小时）沿用
- 卡用户退订：无次数可退，直接释放名额（不退款、不扣卡）
- 候补退出：同现有（金额 0 无退款）

## 五、界面设计

### 次卡包页（passes-buy）新增「无限次卡」分区

```
┌──────────────────────────────────┐
│ 无限次卡  · 有效期不限次数订课      │
│ ┌────────────────────────────┐  │
│ │ 季卡  ¥2,980   90 天不限次    │  │
│ │ 年卡  ¥9,880   365 天不限次    │  │
│ └────────────────────────────┘  │
│ 我的卡：季卡 有效期至 2026-11-19  │
│ （剩余 92 天）                    │
└──────────────────────────────────┘
```

- 卡片选择 + 底部 CTA 购买（复用现有支付流程）
- 已有卡：显示我的卡 + 剩余天数；购买新卡 → 旧卡续期顺延（或提示）
- 订课流程：有卡学员订课按钮直接 0 元确认（不出现支付方式选择）

## 六、测试计划

- **UNL-01**：购买季卡 → 发卡（start/expires 正确，expires=起算+3 个月 23:59:59）
- **UNL-02**：卡有效期外订课 → 走正常支付（不能白嫖）
- **UNL-03**：卡有效期内订课 → 0 元成功（pay_source='unlimited'，booking amount_fen=0）
- **UNL-04**：时间重叠订课 → 400 拒绝（含与普通付费课重叠）
- **UNL-05**：不重叠同日多堂 → 允许
- **UNL-06**：候补 0 元 + 时间查重
- **UNL-07**：退订 → 释放名额无退款无扣卡
- **UNL-08**：旧卡未过期买新卡 → 续期顺延
- **UNL-09**：过期卡 → 判定 expired，订课走正常支付
- **UNL-10**：余额/次卡支付路径回归不受影响
- **FRONT-xx**：次卡包页季卡/年卡分区断言
- coverage 探针：unlimited 接口

## 七、实施步骤（确认后写入 DEV-BACKLOG）

1. **P0｜数据层**：unlimited_plans / unlimited_passes 双方言建表 + 幂等种子（价格运营配置）
2. **P0｜订单**：order_type='unlimited' 购买链路（下单→支付→发卡/续期）
3. **P0｜订课规则**：payOrder 加 unlimited 分支（0 元）+ 时间冲突查重（订课/候补）
4. **P1｜前端**：次卡包页季卡/年卡分区（购买 + 我的卡展示）+ 订课 0 元流程
5. **P1｜测试**：UNL-01~10 + FRONT 断言 + coverage 探针

## 八、待确认点（2026-08-19 全部拍板 ✅）

1. 默认价格（运营可改）：季卡 ¥2,980 / 年卡 ¥9,880 → ✅ 认可
2. 已有卡再买新卡 → 续期顺延（旧卡剩 N 天 → 新卡从旧卡到期次日算起）→ ✅ 认可
3. 候补转正后也 0 元且占用查重名额（与订课一致）→ ✅ 认可
