# 2026-08-14 工作日志

**活动中心按钮图标设计（Ardot 画布 714659336495310）**:
- 需求：活动中心每个按钮配图标（不要文字），风格对齐能量商店（墨黑+荧光绿+圆角胶囊克制风）
- 交付：画布设计稿 7 个状态按钮图标——统一「圆形徽章 + 线性符号」语言（3px 描边·圆头端点）：预约=日历+加号(荧光绿底墨黑)、已预订=圆环+对勾(墨黑底荧光绿)、已排位=圆环+时钟(紫底白)、已满=圆环+禁止斜杠(灰)、进行中=圆环+播放三角(红底白)、已结束=圆环+方块(灰)、进入=圆环+右箭头(墨黑底白)；下方附设计规范
- 待落地：小程序 student-activity 页 cb-btn/banner-btn 加图标（SVG 或 base64），7 态按钮去文字改图标

## 活动中心图标落地（61c6e25）

设计稿（画布 714659336495310）确认后落地：**app.wxss 新增 `.ico-*` 系列 7 类**（data URI 纯线性 SVG，24 网格、2~2.5px 描边、圆头端点，与个人中心 icon-* 完全同语言）；**activity + courses 两页（同构）cb-btn 7 态去文字改图标**：已预订=对勾（同个人中心 icon-check 概念唯一）/ 预约=日历+加号 / 排位=时钟 / 满=禁止斜杠 / 进行中=播放三角 / 结束=方块 / 进入=右箭头；banner「进入」也图标化。按钮从文字胶囊改 72rpx 方形圆角 flex 居中。详情页 book-btn 是主 CTA 保留文字（设计系统 L1 规则）。**坑：画布 SVG 图标 frame 默认 x:-24,y:-24 在胶囊外，需显式定位居中；小程序 WXSS data URI 中 # 须转 %23**。

## 活动中心功能入口图标设计（画布 3:85 区块）

7 个入口图标（纯图形无文字，与能量商店/个人中心 icon-* 同语言）：会员等级=圆环+V（复用 icon-lv）、储值会员=加号+虚线框（复用 icon-recharge）、邀请好友=人形+加号（新）、次卡包=卡片+荧光绿次数点（基于 icon-card）、能量商店=闪电（同能量币概念）、场馆理念=灯泡（新）、参赛荣誉=奖杯（复用 icon-trophy）。**概念唯一原则**：能复用的图标直接复用个人中心同款。入口形态：浅米底图标盒 120×120 圆角 24 + 下方名称。待落地：活动中心页新增 7 入口区（对应 member-level/member-recharge/invite/passes-buy/coin-shop/about-us/honors 页面）。

## 活动中心功能入口区落地（daedcfd）

设计稿确认后落地：Banner 下方新增 7 入口网格（会员等级/储值会员/邀请好友/次卡包/能量商店/场馆理念/参赛荣誉），entry-item 200rpx×4 一行 flex-wrap；`ent-*` 7 类图标 24 网格纯线性 SVG 墨黑 data URI（概念唯一：lv/recharge/trophy 与个人中心同款图形、coin=闪电、pass=卡片+荧光绿次数点、idea=灯泡、invite=人形+加号）；浅米底图标盒 72rpx 圆角 20 + 名称 24rpx；goEntry(data-url) 统一跳转。7 类 wxml/wxss 1:1 验证齐。

## 按钮文案压缩 + 图标未更新排查（c38534b）

①按钮文案压缩 2 字：已预订→已约、已排位→已排、进行中→进行、已结束→结束（i18n waitlisted/ongoing/ended + 两页写死的已预订）；②「活动中心 7 图标没更新」排查：代码已就位（daedcfd 含 entry-grid 8 处 + ent- 7 类），utf8 data URI 写法与个人中心 icon-* 完全一致（10 处验证可用）——结论是开发者工具未重新编译/缓存问题，非代码 bug。教训：**图标不上屏先查是否 Ctrl+B 重新编译，再查 data URI 写法，别急着改代码**。

## ⚠️ 小程序 SVG 图标 data URI 坑（8dc231e，重要）

**症状**：活动中心 7 入口图标（ent-*）编译后不显示。**根因**：`data:image/svg+xml;utf8,<svg...>` 形式在**页面级 WXSS + background-size 缩放**时渲染失败（工具/真机渲染内核差异）——个人中心 icon-* 用 utf8 但**无 background-size**（background-size:contain 在 .menu-icon 上）所以正常。**修复**：全部图标改 `data:image/svg+xml;base64,...`（微信官方推荐形式，零转义零解析风险）；activity 页 7 个 ent-* + 全局 app.wxss 7 个 ico-* 共 14 个图标 base64 化。**铁律：小程序 WXSS background-image 用 SVG data URI 一律 base64，别用 utf8 形式**（utf8 的 # 转 %23 只是其一，空格/引号/中文都会踩雷）。

## 支付页次卡优先体验（68a7f25）

用户需求：有次数包余额时默认用次卡、支付方式区灰掉、**不默认选中任何支付方式**（避免误导）。落地：JS 加 usePass/passRemaining（loadPassHint 检测 available>0 → usePass=true，payMethods 全部 selected:false）；computeTotal 次卡时金额 0；selectMethod usePass 拦截；pay 余额校验跳过；confirmPay 传 payMethod='pass'（后端 effMethod=pass 强制次卡金额 0，传啥都覆盖）。wxml：金额卡「本次抵扣（次数包）¥0 + 剩余 N 次」、支付方式区 method-row-off 置灰（pointer-events:none + JS 双保险）+ pass-lock-tip 说明、CTA 改「使用次数包」。无次卡逻辑不变。**产品细节**：默认选中态会误导用户以为选了余额支付，实际扣次卡——次卡支付时任何选中态都该去掉。

**限时次卡包（P1，已推送 27d55b0+458d201）**:
- 需求：设计方案 v1→v2 经田立批复（D1 档位 12次/60天¥900+24次/120天¥1800 可配置；D2 候补用次卡，退回时卡过期→作废清理；D3 过期作废不退；D8 单卡累加：次数累加+作废日期顺延（剩20天买60天→80天）；D9 活动中心入口+每档使用说明；D10 会员卡展示剩余次数与过期天数）
- 实现：class_packages(种子两档+desc)/user_passes(单卡累加)表；bookings/waitlist/orders 加 pay_source+pass_id；db/passes.js 域模块（list/getUserPass/getUserPassInfo/applyPassPurchase[事务外，payOrder内调]/consumePass/refundPass[过期→expired]/expireOverduePasses）；createOrder 支持 orderType=pass；payOrder 次卡优先（canUsePass→effMethod=pass，强制不可跳过），book/waitlist 扣次金额0；退订/退出候补/过期退款对称退次（卡过期作废）；转正沿用 pay_source；3 接口(可售/我的/可用)+过期定时任务+4类站内信；前端：活动中心入口+购买页(使用说明/逾期提示/余额支付)+会员卡次卡区块+支付页提示条
- 测试：run-tests.js 新增 12 节 PASS-01~15（13条断言），coverage 探针 3 条；**116/116 全绿（普通+干净库双模式）**
- 坑：①applyPassPurchase 自带 BEGIN 与 payOrder 外层事务嵌套 → "cannot start a transaction within a transaction"，改为事务外单条 SQL；②测试用例跨轮累加=测试库未清 user_passes（预清理+section11 双处补 DELETE）；③getUserPassInfo 只查 active 导致过期卡 hasPass:false，改为查最新一条用 expired 标志；④createOrder 对非 recharge 强制 sessionId → 放行 pass

---

## 本会话工作（合规 + 连接 + 勾选，837376d）

**隐私合规链**（92bd337→fdad6c6→0c3de3c→837376d）：隐私三件套→官方授权流程(agreePrivacyAuthorization+getPrivacySetting)→服务协议 SERVICE.md→协议全文页(agreement/index)→默认不勾选+演示入口拦截→勾选持久化(agreed_terms)。

**连不上后端根治**（#6 复发）：net-config.json 运行时生成、打包可能丢失→require 失败回退 api.js 写死 FALLBACK_BASE_URL（过时 IP）。根治=兜底 IP 同步当前 192.168.194.11（双层保障）。**多实例隐患**：多个 node 进程并存时谁最后写 net-config 谁覆盖（出现 3340/3282 端口记录），已人工修正指回 3000。

**并行协作纪律**：只提交自己的文件（显式 git add 指定路径）；并行会话半成品会破坏 hook（passes.js 路径 bug 已修）；commit 撞号用 reset 重提。

## 登录卡死修复（aab8196）

**症状**：点登录无反应。**根因**：requirePrivacy 是同步 return false 拦截 + 异步 wx.getPrivacySetting 检测——开发者工具未配置隐私指引时 needAuthorization=false，弹窗不出、登录被 false 卡死。**修复**：requirePrivacy 改 Promise 异步：privacyOk/本地标记已同意→放行；低版本库/检测失败/无需授权→放行（不卡死）；需要授权→弹窗+toast 提示，_pendingLogin 标记，同意后自动续登录；login/phoneLogin/quickLogin 全部 async await 化。**教训**：微信隐私接口的检测是异步的，登录拦截逻辑若用同步返回值判断异步结果必卡死；「未配置指引」的环境应放行（隐私由微信兜底拦截）。

## 我的页次数包展示（5ddaea9）

用户反馈：个人中心「有N笔储值奖励待领取」是什么功能？去掉，区域留给次数包信息。改造：删除 reward-ribbon/claimReward/getMyRewards；新增 vip-pass 行（剩余次数/总次数/过期天数/已过期红色态，点按跳 passes-buy 购买页；无次卡显示「尚未购买·去了解」空态引导）。数据源 `/api/passes/my`（getUserPassInfo：hasPass/remaining/total/daysLeft/expired）。视觉：深色卡内 rgba 白底 + 荧光绿 label + 白字数字，遵循设计系统。

## 我的页会员区三行重构（071d314）

用户三点重构需求：①第一行=我的会员+等级按钮（不变），升级提示「再上N节课升级X」右对齐在按钮下方，**去掉「会员价折扣」后半句**（JS hint 拼接处改）②第二行=次数包：未购显示「尚未购买次数包 + 去购买→」，已购保持剩余次数/过期天数展示 ③第三行=储值信息：与能量币统一底框样式（vip-row-item 通用类），「去充值」从荧光绿按钮改为「去充值 →」文字表达。删除旧 vip-mid/vip-bal/vip-btn/vip-hint/vip-coins 类，新增 vip-lv-group/vip-lv-hint/vip-row/vip-row-item/vip-row-label/value/go 通用结构。**设计要点**：三行信息（次数包/储值/能量币）统一「底框 + 左标签 + 数值 + 右go链接」语言，视觉一致性提升。

## 头像/昵称改造 + 点头像换头像（36af39c→2f0e36f）

①会员区第一行「我的会员」改为头像+昵称（globalData.userInfo，avatarError 兜底）②点头像弹 ActionSheet：微信头像（getUserProfile）或本地上传（chooseMedia→readFile base64→复用后端 POST /api/upload，512KB 限制→返回 /images/xxx）；保存路径=setData 即时反馈 + 全局/storage 更新 + updateProfile 后端持久化；api.js 新增 uploadImage。**要点**：后端已有通用图片上传接口（课程封面同用），头像上传零后端改动；vip-user catchtap 防冒泡到 vip-top 等级跳转。

## 微信头像修复（5a46fd3，微信政策坑）

**症状**：选「使用微信头像」后头像没变。**根因**：`wx.getUserProfile` 自 **2022.10.25** 起被微信收回——返回统一灰色默认头像+「微信用户」昵称，拿不到真实头像（WebSearch 核实官方公告）。**修复**：改用官方「头像昵称填写能力」`button open-type="chooseAvatar"` + `bindchooseavatar`（基础库 ≥2.21.2，lib 3.17.1 支持）；点头像直接弹官方选择框（微信头像/从相册选），回调拿临时路径 → base64 → `/api/upload` 持久化。**教训**：微信 API 政策会变，`getUserProfile` 已废，头像/昵称必须用 chooseAvatar + nickname 输入框填写能力。

**课程详情页重构（已推送 420b674）**: ①顶部 swiper 轮播（最多5张；server/uploads 服务器端存储不入包，/uploads/ 静态路由；3s 自动右滑 circular 循环；未配置图显示 1-5 数字占位轮播）②课程名+简要标题(summary)+分享按钮(open-type=share 好友/群)③时间(起止+年月日星期小字)④地点图标+地址(默认成都市成华区好事健身馆)+导航按钮(wx.openLocation,lat/lng 默认30.6636/104.1049)⑤已预约 X/20+头像墙(scroll-view scroll-x,头像+昵称,listBookedUsersWithInfo)⑥教练头像/名字/说明(coaches.bio 种子:喻馥雅"Hyrox个人精英运动员,40+引体达人,二娃妈妈,素人零基础")⑦训练详情长文(descText 复用 course_desc,空则 placeholder"4人一组团课...");3000网页课程设定新增详情页配置区块(轮播图上传uploads/删除/排序/1-5数字占位+summary+地址+经纬度+教练介绍);courses 表加 images/summary/address/lat/lng,coaches 加 bio;上传接口支持 dir=uploads;SES-06 断言+探针,122/122全绿;坑:上传返回路径曾写死/images/前缀

**详情页服务器图片 URL 修复**（已推送）: /uploads/ 相对路径在小程序 image 里被当包内资源→404/500；详情页 fullUrl() 用 api.LOCAL_BASE_URL 拼完整 URL（images/cover/coachAvatar 三处）

**席位读数统一人头图标（已推送 eca0ffe）**: 全局 .icon-person（SVG 黑描边人头，base64）放 app.wxss；4 处席位读数前统一加图标——预约页/活动页席位胶囊（.cb-seats 改 flex 垂直居中+gap）、详情页已预约 X/容量（booked-count 改 flex-row）、教练端已报名 X/容量（course-meta 改 flex-row+meta-ico 24rpx）；wxss 括号自检全 OK

**服务掉线恢复（13:51）**: 上课页报 ERR_CONNECTION_REFUSED（member/config、bookings、waitlist 全拒）——3000 端口监听进程异常/被顶掉（检测时在抖动：偶发 200→000→全 000）；重启后 localhost+局域网IP health 200，三个接口全通。老问题复现：并发会话抢端口导致反复掉线

**奖牌emoji与等级名对齐修复（已推送 daba4be）**: 根因=text 内 emoji+中文混排共用基线→中文视觉偏低；修法=拆分 flex-row（.lv-emoji 30rpx + .vip-lv-txt/.card-lv-txt line-height:1 + align-items:center + emoji translateY(1rpx) 微调）；两处：个人中心 🥉青铜、会员卡顶部 🥉Lv.1

**预约页会员价标等级（已推送）**: 价格旁从「会员 ¥88」改为「青铜会员 ¥88」——applyDiscount 带 levelName 入参，data 加 memberLevelName（默认'会员'兜底），getMemberLevel.levelName 动态读取；实测 demo_user=青铜98折→¥90（青铜会员 ¥88）

**难度火焰→哑铃（已推送）**: 预约页/活动页课程条难度点阵由🔥emoji改为黑描边线性哑铃SVG(.cb-dumbbell 30/34rpx)，配色=#1A1A23与课程名/全局icon语言一致，无文件依赖

**活动中心 7 概念图标精修落地（已推送 9ccd7a9）**: 用户选定：会员等级B皇冠，其余按推荐——储值A钱包/邀请C礼物/次卡A卡+哑铃/能量B能量币+加号/场馆C盾牌/荣誉A奖杯；全 SVG base64 内联（app.wxss 全局 .ico-level/.ico-wallet/.ico-invite/.ico-pass/.ico-shop/.ico-idea/.ico-honor，复用 .ico 40rpx 基类），配色按宫格底色适配（紫/绿底白线，浅色底黑线+青柠点缀）；member-center 宫格 text 图标→SVG，清 g-icon-txt 死代码；pre-commit 全绿

**哑铃图标 WXSS 编译错误修复（已推送）**: 根因=Python 三引号字符串内用 `'''' + B64 + ''''`（4单引号）不触发插值，把字面量写进 wxss（226:14 unexpected token）；app.wxss 用 4 双引号 `""""` 反而触发字符串拼接正确；修法=replace 为真实 base64；教训：**模板字符串内拼变量用 f-string/format/显式拼接，三引号内的引号拼接不可靠**；全项目扫描坏模式+9个base64全部解码验证通过

**7 图标不可见修复（已推送）**: 根因=生成 SVG 时把颜色写成 %23 前缀（URL 编码形式），SVG 解析器不认 %231A1A23 为颜色→描边全空→图标透明不可见；哑铃图标用 # 直接写所以正常；修法=重生成标准 #hex base64 替换 app.wxss 7 段；教训：**SVG data URI 内颜色用 #hex 直接写，%23 只在非 base64 的 URL 形式才需要**；解码验证 7/7 通过

**活动中心宫格配色方案A落地（已推送）**: 用户选方案A（深色语义色，白图标）；色值：等级#534AB7/储值#BA7517/邀请#3B6D11/次卡#185FA5/能量#D85A30/理念#1A1A23/荣誉#A32D2D，明度统一；gi-* 类重命名为语义名（gi-level/gi-wallet/gi-invite/gi-pass/gi-shop/gi-idea/gi-honor，与 ico-* 对应）；5 个图标改白线+青柠点缀（level宝石/invite蝴蝶结/shop闪电/honor星），wallet/idea 本就白线；解决原同色撞车(两金)+深浅混杂问题

**成就系统 30 项时间线（已推送 dd9c3c9）**: ①连续打卡卡去右侧箭头、本周记录去"X次·达标"标签（改右上显示总次数）；②30 项成就按 5 阶段分组：第1-5天5项(密集，D1三连)+第2-4周3项+第1-3月3项+第2-4季度18项(每2周固定节奏)+一整年1项；设计约束全满足：前期密集/后期固定节奏/相邻间隔≤14天(实测1-14天)/至少达成一项(D1即可)；③判定基于9维数据(订课/签到/连续/累计次数时长/课程种类/注册天数memberDays/储值/邀请/能量币)，纯前端判定+多源并行拉取；④member/level 加 created_at+memberDays 字段；⑤demo_user 实测解锁 11/30 全对；测试 122/122 全绿

**成就时间线布局改版（已推送）**: ①阶段命名：萌芽期(第1-5天)/成长期(第2-4周)/进阶期(第1-3月)/蜕变期(第2-4季度)/巅峰期(一整年)；②每行 3 奖章（33.33% 三列 flex-wrap）；③奖章=奖牌样式：黑描边外环+彩色内芯+单字图标+底部双缎带(旋转22°)，未解锁灰；名字小字在下方；移除 medal-txt/ach-desc 旧样式

**成就阶段命名→火焰进阶（已推送 a35edb1）**: 用户选方案D：火星(第1-5天)/火苗(第2-4周)/火焰(第1-3月)/烈焰(第2-4季度)/燎原(一整年)；时间范围保留为副标题小字

**次卡包档位卡改版（已推送）**: 修复勾与价格重叠——pkg-check 原绝对定位右上角压住价格；改为仿支付页 method-row：左(名称+次数天数chip+说明) flex-1、右(价格+radio圆点纵向) 不重叠，radio-on=青柠底黑边黑点；删 pkg-check 绝对定位样式

**成就解锁发能量币（已推送 5f305ab）**: 每解锁 1 项成就 +50 能量币；新增 server/db/achievements.js（30项定义+9维ctx+syncAchievements 幂等发放，UNIQUE(user_openid,ach_key)）；GET /api/achievements/sync；成就币 bypassLimit 绕过每日上限（coin.js addCoins 加第5参 bypassLimit）；前端 ACHIEVEMENTS 加 key 对齐后端，loadAchievements 并行调 sync（服务端解锁权威+本地兜底）+新解锁 toast；坑：①SQL 误用 SQLite date()/time() 当 JS 函数+赋值号=比较（改 JS 日期）②s.duration_min 应为 c.duration_min ③能量币每日上限拦截成就奖励（bypass）④补发 demo_user 被拦截的 50；E2E: demo_user 11 项×50=550，400→950，二次 sync newly=0 幂等 ✓；测试 122/122 全绿

**取消能量币每日上限（已推送）**: dailyLimit 500→0（config 驱动，coin.js limit=0 自动跳过；前端规则页/商店文案自动显示"不限量"）

**取消能量币每日上限（已推送）**: dailyLimit 500→0（config 驱动；coin.js limit=0 自动跳过；前端文案自动"不限量"）；COIN-04b 由"日限截断500"改为"无限额全额1000"；122/122 全绿

**能量币规则加"解锁成就+50币"（已推送）**: energy-config earnRules 加 achievement:50，achievements.js REWARD_COINS 改同源读取；coin-rules 页新增 🏅解锁成就 条目

**测试账号体系（已推送 bf5b680）**: 重建 3 测试号（openid=昵称djb2哈希，与登录页一致）：喻馥雅 demo_3dmuxq（role=coach，再登录返回 coach✓）/蚂蚁 demo_2tax2/艳子 demo_2v4cm（student）；登录页改：教练账号补 coach_id=1（喻馥雅档案）+ 登录后按角色分流（coach→教练端课表 redirectTo，student→预约页 switchTab）；删旧 3 号（阿强/小美/大壮，无数据）

**登录页昵称快捷登录（已推送 c0621b8）**: 邀请码输入框→昵称输入：匹配 喻馥雅(coach)/蚂蚁/艳子 → 显示检测提示+「以此账号登录」（modal 确认后 djb2 哈希 openid 直登，喻馥雅自动进教练端）；微信一键登录/手机号登录统一 demo_user 田立（doLogin 去昵称哈希，加 testNick 参数）；邀请绑定改读 storage pending_inviter（分享卡片路径保留，手动邀请码入口移除）；坑：Python 脚本锚点行尾逗号被吃导致语法错误（matchedTest 缺逗号）

**拉朋友测试前收尾（已推送 33d47d6）**: gitignore 补 `server/data/*.db.bak*`（bak2 漏网导致 data 目录整目录显示 untracked）；远端 master 确认 = 33d47d6；工作区仅剩 miniprogram/images/1786672368808_0rnu.jpeg（4.6KB，无代码引用，疑为误放的微信保存图，未提交）；沙箱环境 refs/remotes/origin 写入被静默拦截（fetch/update-ref 成功但 remote-tracking ref 不落盘，"upstream is gone" 提示无害，push 正常）

**公网穿透（Cloudflare Quick Tunnel，临时方案）**: 用户要拉朋友公网测试 → 本机已装 cloudflared（C:\Program Files (x86)\cloudflared\），`cloudflared tunnel --url http://127.0.0.1:3000 --no-autoupdate` 起 Quick Tunnel（免费免注册），URL=`https://giants-shelter-formation-budapest.trycloudflare.com`（随机，重启即变）；公网 /api/health 200（回源约1s）。前端改两处指向公网 URL：net-config.json baseUrl（.git/info/exclude 忽略，不入库；顺带修正旧会话残留的 3160 死端口）+ api.js FALLBACK_BASE_URL（**未提交**，URL 会失效）。要点：①预览/开发版必须勾选「不校验合法域名」才能用该 https 域名；②隧道重启/电脑重启后 URL 变了要同步改两处；③测试完建议改回局域网 IP 模式；④后端进程不能关。备选方案：cpolar/natapp（国内稳，需注册）、云服务器部署（正式，需备案）、微信云开发（api.js 已预留 USE_CLOUD）

**18:05 隧道重连**: 旧隧道控制流断（日志 control stream failure + Retrying 死循环），URL 失效（curl 000）；杀进程（Git Bash taskkill 参数报错，改 PowerShell Stop-Process -Force）→ 清日志 → 重启 → 新 URL=`https://complexity-dirt-utility-mpegs.trycloudflare.com`（health 200）；api.js/net-config.json 同步换新地址。**隧道自愈性差**: Quick Tunnel 控制流断了不会自动换 URL，进程僵死，需手动杀+重启

**真机慢诊断 + 前端秒开缓存（已推送 66e0e79）**: 用户反馈真机 UI 读取慢。诊断：主因=公网隧道 RTT ~1.3s（局域网下秒开，上 Cloudflare 隧道后所有请求+1s）；次因=无缓存秒开机制（进页先清空→等网络→才渲染，白屏/加载中）。落地：①新建 `utils/session-cache.js`（按日期存原始 sessions，TTL 10min，过期视为无缓存）②预约页/活动页 loadSessions/loadTodayCourses 改「有缓存先渲染（loading=false）→ 后台刷新替换；无缓存才 loading」；刷新失败且已有数据则保持展示（不闪 mock）③两页 wxml 加骨架屏（3 张 sk-card：头像+行+按钮灰块，sk-breathe 呼吸动画）替代「加载中...」/白屏；wxss 两页各加 .sk-* 系列。缓存存原始数据、渲染时重新 decorate（席位/状态实时）；memberPrice 折扣异步返回后 applyDiscount 重算不受影响。单测 4/4 PASS（读写/TTL/无缓存）+ 122/122 全绿。**注意**：缓存仅在「同日期+10min 内」生效，跨天/过期自动走骨架屏，无需清缓存

**换 cpolar 国内穿透（18:29 上线，已推送 af69022）**: Cloudflare 隧道不稳定（1 小时断一次）→ 用户手动安装 cpolar（3.3.18，装到 C:\Program Files\cpolar\，注册 Windows 服务 cpolar_service 常驻）。坑：①官网 zip 直链 404，正确链接是 /static/downloads/releases/3.3.12/cpolar-stable-windows-amd64-setup.zip（内含 MSI）；②msiexec //a 提取在沙箱被拦 → 用户手动安装（安装时自动注册服务+预置 remoteDesktop/website 模板隧道）；③服务在 token 写入前启动 → 卡 authToken is null 循环，9200 无响应 → 杀进程重启后服务读到 token 恢复；④`cpolar http 3000` 默认 -log none 无输出，加 `-log stdout -log-level INFO` 才看得到 "Tunnel established at https://xxx.r8.cpolar.cn"。**最终**：cpolar 免费隧道 `https://68ef72a.r8.cpolar.cn`（http 0.47s / https 0.85s，优于 Cloudflare 1.3s，且国内节点稳定）；前端 api.js/net-config.json 已切 cpolar，Cloudflare 进程已停；新增 `server/start-cpolar-tunnel.bat` 一键起隧道。**注意**：cpolar 服务（Services 会话）常驻但跑模板隧道不碍事；手动隧道进程（Console）重启电脑后需双击 bat 重起，免费版域名会变需同步前端

**21:14 cpolar 隧道重启（电脑重启过）**: 手动隧道进程随重启消失（旧 URL 404）；重起 `cpolar http 3000 -log stdout` → 新 URL=`https://35d0ecae.r22.cpolar.top`（稳定延迟 0.26~0.30s，节点 r22 比 r8 快；首次请求 4s 是冷启动建连）；api.js/net-config.json 已换新地址

**测试号 AppID 切换（已提交 8f2c643，推送失败待重试）**: 用户拉新微信测试遇「预览无权限」（预览码只有项目成员可扫，体验成员不行；后台成员管理仅管理员可见，用户是开发者角色）。用户申请测试号成功（appid=wxd63f6b720b3021ea，secret 仅用户持有、未入库）：project.config.json appid wx509088154a505409→wxd63f6b720b3021ea（private config 无 appid，只改一处）。**要点**：①测试号任何微信可扫预览码、无成员限制；②测试号不能上传发布（体验版/正式版需真实 AppID）；③secret 敏感绝不入 git；④正式发布前 appid 换回。**坑**：sandbox 申请后 appid/secret 不直接弹（已是测试者的微信要去 mp.weixin.qq.com 登录选「小程序测试号」→开发设置看 appid、secret 点重置）。**推送受阻**：GitHub 连接 reset/超时（国内网络），commit 在本地，网络恢复后 git push origin master 补推

**真机"本地服务器没有启动"根因 + 地址源重构（已提交 fb451d8）**: 测试号预览包在新微信上登录报"本地服务器没有启动"，而公网隧道/后端均 200——查 net-config.json 发现被**并行后端实例覆盖**成 `http://192.168.101.7:3536`（PORT=3536 的进程启动时写入后退出，IP 也从 194.11 变成 101.7，电脑换了网络）。前端 LOCAL_BASE_URL 优先读 net-config → 真机连向不存在的服务。**根治**：废弃 net-config.json 作为前端地址源（删除文件 + api.js 删 require 逻辑），LOCAL_BASE_URL 直接=FALLBACK_BASE_URL（唯一人工配置源，局域网/公网切换只改一处）；net-config.json 是运行时产物会被并行实例互相覆盖，不可作配置源（BUG-LEDGER #6 的反面教训：之前担心它丢，现在发现它更危险的是"被写错"）。**另发现**：真机跳过域名校验需小程序右上角「···」→「打开调试」（官方 sandbox 文档要求，仅勾选工具设置不够）。**登录号与 appid 匹配规则**：工具登录微信必须是当前项目 appid 的开发者——测试号 appid 需用申请测试号的微信登录工具；真实号 appid 用田立主号。测试号预览无成员限制，任意微信可扫

**次卡包档位改名（已提交 4381ded）**: 月卡→12次包、双月卡→24次包（server/db/passes.js 种子两行；现有库实测已是新名——并行实例启动时已同步或曾改过，UPDATE 0 行；接口 /api/passes/packages 返回新名，前端动态读取无硬编码）；测试 122/122 全绿

**⚠️⚠️ git 仓库灾难性损坏（22:05，未解决，等网络恢复）**: `.git/objects/pack/*.pack` 文件丢失（只剩 .idx），objects 仅剩 2 个松散对象 → 本地历史对象几乎全丢；`.git/refs` 目录也被删（git 报 not a git repository）。疑似并行会话 git gc 被打断（objects/pack 22:05 生成但 .pack 未落盘）+ refs 清理。**损失**：66e0e79 之后 7 个提交（af69022/8f2c643/fb451d8/4381ded/dd4fa4e/3131859/28b5e05）的本地历史——**但工作区文件完整，代码零丢失**；远端 GitHub 有 af69022 及之前全部历史（ls-remote 确认 af69022=远端 master）。**恢复计划（网络恢复后）**：①git fetch origin 全量恢复 af69022 及祖先（GitHub 443 当前不通，重试中）②重建 refs/heads/master=af69022 ③工作区重新 add+commit（7 个丢失提交内容合并为一个新提交）④push。**注意**：fetch 报 invalid index-pack output 时先删 .git/objects/pack/multi-pack-index+*.idx+commit-graphs 再 fetch；refs 目录 mkdir -p .git/refs/{heads,tags,remotes/origin}。**当前 git 命令不可用，勿动 .git！**

**测试时间耦合缺陷（已修 run-tests.js，未提交）**: 22:00 后跑测试必挂——fullSessionId 是今天 22:00 场次，refundExpiredWaitlist 在 GET /api/waitlist 时把已开课场次的 waiting 自动标 refunded → WTL-06 转正/07 退出全挂 + PASS-10 连锁崩。修复：`const fullDate = (new Date().getHours() >= 21) ? tomorrowStr : todayStr`（21 点后满员场次用明天日期）。验证 121/121 全绿。**教训**：测试场次时间不能写死当天晚间，否则夜间跑测试必挂；hook 拦截恰好暴露此问题

**假用户测试数据（已造数据未提交）**: server/seed-fake-users.js（可复用：清 fake_% → 随机 3-5 名假用户 → 订 demo_user 全部 23 场次）；courses.js listBookedUsersWithInfo 对 fake_ 前缀返回确定性伪随机 coCount 0-5（同场稳定跨场不同）；已填充 4 名（陈语嫣/吴雨桐/王一诺/郑凯文）92 条 booking；后端已重启（3000 新代码），公网验证 coCount 正常分布。**待提交**（git 恢复后）

**我的课程卡片进详情 + 详情页排位中态（已改未提交）**: ①student-my-courses 卡片加 bindtap goDetail（data-session=sessionId），内部按钮 bindtap→catchtap 防冒泡误跳；②详情接口 handleSessionDetail 补 waitlisted_by_me（waitlist status='waiting'）；③详情页 data 加 isWaitlisted，按钮"排位中"（isBooked 优先"已预订"），bookNow 拦截 toast"排位中，等待转正"；④后端已重启公网验证字段生效。**待提交**（git 恢复后）

**已预订课程可进详情（已提交 dd4fa4e）**: 用户需求：已预订课程点课程条进详情，详情页底部按钮改"已预订"。改动：①预约页 decorateSession disabled `isBooked||...` → `!isBooked && status!=='upcoming'`（已预订可点，进行中/已结束未预订仍不可点），canWaitlist 加 `&& !isBooked`（已预订满员不跳候补）；②活动页 goDetail 删 isBooked toast 拦截改直进详情，decorate disabled 同理放开；③详情页本已支持（isBooked 显示"已预订"+bookNow 拦截），零改动。列表按钮态：预约页已约 btn-booked、活动页已约 ico 保持

**预约墙昵称补全（已提交 3131859）**: 详情页预约墙头像下昵称空白——根因 demo_user 等账号注册时 nickname 空且已注册登录不更新。三层修复：①handleLogin 已注册分支加「登录携带昵称且库空→updateProfile 补全」（avatar 保留已有）；②现有数据补全 田立/蚂蚁/艳子/喻馥雅（UPDATE 时发现已补好，疑似并行会话或近期登录已写）；③前端 wxml 空昵称兜底"微信用户"

**预约墙同堂次数标（已提交 28b5e05）**: 详情页预约墙头像右下角荧光绿小圆标(#C6F04B+黑描边2rpx，34rpx 圆)，显示查看者与该用户共同 booked 过的场次数（含当前场，一次 SQL 批量算）；>9 前端显示 "..."，0 也显示（字面遵循"标明同堂过几次"）。后端 listBookedUsersWithInfo(sessionId, viewerOpenid) 改造 + index.js 传 openid；SQL 单测 4 视角通过。**待生效**：后端需重启才生效（当前 3000 进程旧代码）；测试数据 booked 多为 demo_user 单人，同堂>0 需多人同场订课才能看到

## 朋友真机测试环境搭建（测试号方案）

现状确认：cpolar 公网隧道活着（35d0ecae.r22.cpolar.top，api.js FALLBACK 唯一地址源）→ 朋友无需同一 WiFi；登录已接 jscode2session 但 WX_SECRET 未配 → 所有人共用 demo_user。**测试号限制**：不能加体验成员/发体验版，3 个朋友只能走「预览」二维码（每次 5 分钟有效，需开发者工具点预览生成）。已做：①index.js 增加 .env 加载（不覆盖已有环境变量）②后端默认 WX_APPID 对齐前端测试号 wxd63f6b720b3021ea（原 wx509088154a505409 不一致会导致 code 换不到 openid）③server/.env 创建（WX_SECRET 待填）④.gitignore 排除 .env 防 secret 入库。**待办**：田立从测试号页面复制 AppSecret 填入 server/.env → 重启后端 → 验证朋友各自独立 openid。

## 文档更新（22:23，用户要求总结今日）

已更新：①新建「开发总结-2026-08-14.md」（进度/坑/设计/环境/明日建议全览）②BUG-LEDGER +3 条：#14 net-config 覆盖（已修）、#15 测试时间耦合（已修）、#16 git 仓库损坏（修复中）③ENVIRONMENTS 新增「六、公网穿透测试模式」④TODO 更新（git 恢复 P0 + WX_SECRET + 正式部署）⑤MEMORY.md 长期笔记同步。**git 损坏状态下文档只改工作区文件，不提交**

## TODO 新增：次数包按上课日期判断可用性（22:27，未实现仅记录）

用户规则：卡今天全部过期 → 不能使用次数包预订明天或以后的场次；支付界面显示「次数包已过期」，只能储值/微信支付。**现状差距**：getUserPass 只判断当前时刻未过期（订明天课+卡今天过期会被错误允许用次卡）；前端 loadPassHint 同款。实现要点已写入 TODO：后端 canUsePass 加 expires_at ≥ 上课日期判断；前端支付页过期提示条 + 禁用次卡。**未实现，等用户确认**
