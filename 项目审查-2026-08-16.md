# 项目结构评审 · 2026-08-16（首评）

> **目的**：按用户定义的 4 维标准定期评估项目结构质量，保证「流程清晰 / 失败可学 / 项目变薄 / 知识可带走」四项长期成立。
> **频率**：每周五（CLAUDE.md / CONVENTIONS.md 承诺表 + cron 闹钟双保险）。
> **方法**：每维按「强项证据 / 硬伤证据」收集后评分（满分 5），对照上次评审跟踪修复项，输出本文件追加记录。

---

## 一、评估标准（用户定义，2026-08-16 原文收录）

| # | 标准 | 我关心什么 | 评估时的检查方法 |
|---|---|---|---|
| 1 | **流程清晰**：从需求到设计到开发到部署的完整流程 | 每个环节有入口、有文档、有闸门；环节之间不脱节 | 抽查一条需求：登记→设计→确认→开发→测试→部署→验收是否全程可追溯 |
| 2 | **失败可学**：从失败中学习并防止再犯 | 每个 bug 有台账五要素 + 防护层；同类失败不二次发生；机制强制而非口头 | 数 BUG-LEDGER 条数、查同类根因是否复发、查强制规矩是否由 bug 驱动而生 |
| 3 | **项目变薄**：合并/复用元素，性能与维护面最优 | 无死代码、无重复实现、共享工具单源、不一致语义为零 | 抽查重复模式（问候/日期条/签到窗口/折扣），grep 死引用，核对关键常量是否单源 |
| 4 | **知识沉淀**：为其他项目标记一切有价值的东西 | 知识入库（版本控制）、跨项目提炼、文档保鲜（数字不腐烂） | 查 .workbuddy 是否入库、文档里的测试数/覆盖率是否与现状一致、有无跨项目可带走的提炼 |

## 二、评分与总评（2026-08-16）

| 维度 | 评分 | 一句话结论 |
|---|---|---|
| 1 流程清晰 | 4.0/5 | 需求→设计→开发闭环优秀（钩子登记+设计确认+任务化+提交引用）；**部署段无闭环**（无冒烟、L3 闸门从未执行） |
| 2 失败可学 | 4.5/5 | BUG-LEDGER 五要素+防护层是教科书级；**运维/平台类教训缺机制**（旧镜像/持久化反复以变体复发） |
| 3 项目变薄 | 2.5/5 | 后端 4425 行零依赖很薄；**前端 37 页重复严重**（问候×4、日期条×3、签到窗口×4、折扣两处不一致）+ 死代码一批 |
| 4 知识沉淀 | 3.0/5 | 会话日志原料丰富但**曾本地独有不入库**、最忙日断更；跨项目提炼为零；文档数字腐烂（81/82 项 vs 实际 150） |

**结构性问题三件套**：①部署无冒烟/无版本对账（#12 旧镜像花了整天）②知识资产本地独有（.workbuddy gitignore）③前端复制粘贴开发（37 页 4 件套）。

## 三、发现的问题清单（首评）

### 已修复（2026-08-16 当日落地）
- [x] 部署冒烟脚本 `deploy-smoke.sh`（健康检查 + 4 新路由 400 特征判定新/旧镜像，本地验证 5/5 绿）
- [x] `.workbuddy/` 会话日志入库（gitignore 移除 + 补全 8/16 断更日志）
- [x] 本地 git 对象库恢复（用 /tmp/gym-remote/.git 替换，提交回归本地）

### 待办（按优先级，下次评审对照）
- [ ] **P0 云托管 CFS 持久化（#25）**——用户控制台挂载 + 环境变量；未完成则每次重建数据重置
- [ ] **P1 签到窗口 30 分钟单源化**——4 份硬编码（bookings.js:167 / student-my-courses:114 / student-checkin:39 / coach-home:9）收进共享配置
- [ ] **P1 会员价两处不一致**——student-activity 硬编码 `*0.9` vs student-courses 真实折扣
- [ ] **P1 死代码清理**——member-card 整页、cloudfunctions 死分支（USE_CLOUD=false）、api.js 6 处「云函数暂未实现」reject、index.js detectLanIP/writeNetConfig 半死机制、db.js 17 个未用 re-export、_co-test.js、debug-i18n4.js、web/ 10 个设计稿（归档）
- [ ] **P2 重复实现提取**——getGreetingWord×4、buildWeek×3、avatarError×6、decorate×3、mock 回退模板×2
- [ ] **P2 文档保鲜机制**——CONVENTIONS 测试数（81→150）、DoD/RELEASE-GATE 覆盖率基线、3 份云文档去重归档
- [ ] **P2 收件箱过期闭环**——待确认 >2 天自动提醒（#8/#11 已拖 2 天）

## 四、跨项目可带走资产（沉淀清单）

> 这些是「下一个项目直接抄」的东西，已从本项目语境剥离。

1. **钩子驱动的流程**：UserPromptSubmit 钩子自动登记需求/bug → 编号收件箱 → 设计确认 → 任务化 → 提交引用编号
2. **BUG-LEDGER 五要素 + 防护层**：现象/根因/修复/回归测试/防护层（哪层抓住、修复后哪层兜底）
3. **干净库测试模式**：临时库 + seed + 独立端口 + 自清理 + pre-commit hook 强制（`DB_PATH` 环境变量切换）
4. **显式时区工具**：`server/time.js`（Intl 固定 Asia/Shanghai）+ `TZ=UTC` 模拟容器时区跑全量测试 + 确定性断言
5. **部署冒烟脚本**：`deploy-smoke.sh` 路由特征判定新/旧镜像（400 参数校验 = 路由在线）
6. **微信平台坑**：chooseAvatar/chooseMedia 不能写 requiredPrivateInfos（只支持地理类）；gitignore 文件不进预览包（net-config #6）；devtools 无真实头像（#18）；模拟器 127.0.0.1 语义差异掩盖网络问题（#6）
7. **资金闭环规矩**：支付/退款严格对称、幂等「下单查 pending + 支付查 booked」双层（#9/#13 教训）
8. **测试造数教训**：相对时间动态化（跨天/跨周必挂 #15/#23）；测试构造不能绕过真实状态流转（#5/#7）
9. **金额 0 是有效值**：前端判断用 `!= null` 不用 truthy（#14 次卡坑）
10. **Windows 行尾噪音**：全仓 CRLF 显示 M 用 `git add --renormalize .` 一键清理

## 五、每周五复评检查单

- [ ] 评分 4 维（对照第二节标准与检查方法）
- [ ] 对照「三、待办」逐项核对（新评时并入新发现）
- [ ] BUG-LEDGER 新增条数 / 同类复发检查（台账是否在长）
- [ ] 文档保鲜抽查：测试数、覆盖率基线、路由表、环境地址是否与现状一致
- [ ] 新增了什么值得带走的东西 → 追加第四节
- [ ] 把本轮记录追加到本文件「复评记录」区
