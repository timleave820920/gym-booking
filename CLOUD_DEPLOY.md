# 微信云开发部署手册
综合训练馆订课系统 · 后端上云（生产环境方案）

---

## 一、为什么用云开发

| 对比项 | 本地后端（当前） | 微信云开发 |
|---|---|---|
| 运行位置 | 你的电脑（127.0.0.1） | 腾讯云服务器（PaaS） |
| 登录 openid | 模拟（客户端生成） | **真实**（云端自动注入） |
| 需要服务器 | ❌ 电脑即可 | ❌ 不需要（平台托管） |
| 需要域名/备案 | 真机需局域网 IP | ❌ 不需要 |
| 数据库 | SQLite 本地文件 | 云数据库（腾讯云托管） |
| 成本 | 0 | 免费额度够开发期 |

## 二、开通云开发环境（约 2 分钟）

1. 打开微信开发者工具，加载本项目
2. 点击顶部工具栏「**云开发**」按钮
3. 弹出开通引导 → 选择「**按量付费**」（有免费额度，不用花钱）
4. 创建环境，环境名称建议 `gym-prod`（可自定义）
5. 开通后记下**环境 ID**（形如 `gym-prod-xxxxxxxx`）

## 三、创建云数据库集合

1. 云开发控制台 → 「数据库」
2. 点「+」创建集合，名称填 **`users`**（必须精确匹配）
3. 权限设置选「仅创建者可读写」（或自定义）

## 四、上传云函数

本项目已准备好两个云函数：

```
cloudfunctions/
├── login/    # 注册/登录（首次注册、再次登录，自动获取真实 openid）
└── users/    # 用户列表（后台学员管理用）
```

操作步骤：
1. 在微信开发者工具的**资源管理器**中，找到 `cloudfunctions` 目录
2. **右键 `login` 文件夹** → 选择「**上传并部署：云端安装依赖**」
3. **右键 `users` 文件夹** → 同样操作
4. 等待上传完成（首次会自动安装 `wx-server-sdk`）

> 如右键菜单没有「上传并部署」，先在 `project.config.json` 确认 `cloudfunctionRoot: "cloudfunctions/"` 存在（已配置）。

## 五、切换小程序端到云开发

编辑 `miniprogram/utils/api.js`：

```js
// 改这两行：
const USE_CLOUD = true;                 // false=本地后端，true=云开发
const CLOUD_ENV = 'gym-prod-xxxxxxxx';  // 改成你的环境 ID
```

`app.js` 会在启动时自动初始化 `wx.cloud`，无需其他改动。

## 六、验证

1. 编译小程序 → 登录页 → 登录
2. 首次登录提示「注册成功」→ 云开发控制台数据库 `users` 集合中能看到**真实 openid** 的记录
3. 退出 → 再登录 → 提示「欢迎回来」，`login_count` 递增
4. 后台 → 学员管理 → 显示云数据库中真实用户

## 七、回退到本地后端

改回 `USE_CLOUD = false` 即可，本地 `server/index.js` 不受影响，两种模式可随时切换。

---

## 常见问题

**Q: 云函数上传失败？**
A: 确认网络正常；右键时选「云端安装依赖」而不是「不上传依赖」。

**Q: 提示 collection not exists？**
A: 云数据库里还没有创建 `users` 集合，按第三节创建。

**Q: 登录拿到的 openid 和之前本地的一样吗？**
A: 不一样。云开发模式是微信真实 openid（每个用户唯一且稳定），本地模式是模拟的。切换模式后用户记录会重新注册一次，这是正常的。
