# 运营小助理（日运营报告）设计方案（DESIGN #D6）

> 2026-08-18 设计草案（待用户确认后写入 DEV-BACKLOG.md）
> 状态：✅ 已确认（2026-08-18 用户拍板 3 项决策，任务已写入 DEV-BACKLOG.md）

---

## 一、目标

基于 #D4 已交付的 Dashboard 聚合数据（7 核心指标 + 4 组补充指标 + 7/30 天趋势），**自动生成简短的日运营报告**：

1. 关键数据及趋势（环比/连续升降）
2. 对**用户**和**排课**的明确行动建议（带数据依据）
3. 报告落库，后台**随时可查历史**

**定位**：规则引擎（纯后端 JS，确定性、零成本、可测试），**不接 LLM**——报告质量由规则保证，运营者一眼看懂「今天该做什么」。

## 二、报告结构（每天一份）

```
📊 8月18日 运营日报
一句话总结：今日收入 ¥1,234（较昨日 +12%）| 订课率 78% | 新用户 23
── 关键数据 ──
新用户 23（昨日 31，▼26%·连续2天下滑）
订课率 78%（↑6pp）| 签到率 82% | 到课 156 人次
确认收入 ¥980（未确认 ¥254·占 21%）| 退款 ¥12（正常）
── 趋势 ──
订课率 7 天：72%→78% 连续上升 📈；新用户连续 2 天下滑 ⚠️
── 行动建议 ──
①【用户·优先】新用户连续 2 天下滑——建议触达沉睡用户（14 天档 45 人）+ 发邀请活动
②【排课】「搏击入门」连续 3 天空置率 >60%——建议调整时段或换教练
③【排课】「瑜伽进阶」候补 12 人已转正 8 人——建议下周二加一场
④【收入】未确认收入占 21%——多为可退订，可推「确认权益」提醒
```

## 三、建议规则引擎（确定性规则表）

> 规则按严重度排序输出 top 5；每条 = 触发条件 + 建议文案 + 依据数据。全部基于 #D4 dashboard.js 现有字段，**无新聚合**。

| # | 触发条件（基于今日 vs 昨日/7 日均值） | 行动建议 | 对象 |
|---|---|---|---|
| 1 | `new_users` 环比下降 ≥20% | 拉新：触达沉睡用户 + 邀请有礼活动 | 用户 |
| 2 | `booking_rate` < 60% | 排查冷门课程，调整排期/宣传位 | 排课 |
| 3 | `courses.top` 候补转正率高或持续满员 | 该课程加场次/扩容 | 排课 |
| 4 | `courses.cold` 课程空置率 >60% 且连续 ≥3 天（7 天窗口判断） | 该课调整时段/教练或下线 | 排课 |
| 5 | `revenue.cancel_rate` > 20% | 检查退订原因（时间/价格冲突） | 排课 |
| 6 | `checkin_rate` < 70% | 课前 3 小时提醒未签到学员 | 用户 |
| 7 | `growth.dormant.d14` 环比增加 | 定向触达沉睡 14 天档用户 | 用户 |
| 8 | `unconfirmed_revenue_fen` 占比 >30% | 收入未锁定预警，促确认 | 收入 |
| 9 | `refund_fen` 环比上升 ≥50% | 退款异常预警，核查原因 | 收入 |
| 10 | `growth.funnel.first_booked` 转化 <30% | 新客激活：首次订课激励 | 用户 |
| 11 | 7 天 `retention.d7` <15% | 留存预警，优化新手体验 | 用户 |
| 12 | 全指标正常 | 「一切正常，无行动项」 | — |

**趋势解读**：对 7 天 trend 数组做「连续 ≥3 天上升/下降」检测 → 输出 ⚠️/📈/📉 标记。

**一句话总结**：优先展示下降最严重的核心指标（收入/订课/新客/签到），全正常则报最好指标。

## 四、存储与接口

```sql
daily_reports (
  date TEXT PRIMARY KEY,          -- 报告日期（北京时间）
  summary TEXT,                   -- 一句话总结
  metrics TEXT,                   -- 关键数据 JSON（7 指标+环比+标记）
  trends TEXT,                    -- 趋势解读 JSON
  actions TEXT,                   -- 行动建议 JSON（severity 排序）
  generated_at TEXT
)
```

- **生成时机**：每日**首次**访问报告接口时惰性生成（同一天幂等，不重复生成）+ 手动「重新生成」按钮（覆盖当天）
- `GET /api/admin/reports?date=YYYY-MM-DD`（ADMIN_PATHS 保护；无 date 返回最近 7 天列表）
- 无数据的天（如上线前）返回「当日无运营数据」占位

## 五、前端（web 运营数据 tab）

- 新增「📋 运营日报」折叠卡（放最顶部，打开即报告）：日期选择器（默认今日）+「重新生成」按钮
- 报告渲染：一句话总结大字 + 关键数据网格 + 趋势区 + 行动建议列表（按严重度红/黄/绿底色）
- 全部基于 #D4 现有接口 `GET /api/admin/dashboard`——**报告引擎只读 dashboard 数据，无耦合新聚合**

## 六、测试

- REP-xx：生成成功（今日报告含 summary/metrics/actions）、幂等（同日不重复）、指定日期历史、无数据日占位、无 token 401、重新生成覆盖、规则触发断言（构造低订课率/高空置数据 → 对应建议出现）、trend 连续升降检测
- coverage 探针 1 行
- FRONT-xx：web 页面防回退断言（`运营日报` 折叠卡存在）

## 七、已确认决策（2026-08-18 用户拍板）

1. **生成时机**：**惰性生成**——每日首次访问报告接口时生成并存库（同日幂等）+ 手动「重新生成」按钮覆盖当天。无需定时任务。
2. **建议条数**：**全量列出**——所有命中规则全部输出（按严重度排序），不做条数截断。
3. **语气**：**中文运营口吻**（如第二节示例，口语化、一眼懂）。
