# 开发总结 2026-08-17 · 上云迁移战役：从 SQLite 到生产 MySQL

> High-level 复盘。缺陷细节见 BUG-LEDGER #14/#25/#28/#30~#34；方案设计见 MySQL持久化迁移设计方案.md；操作步骤见 上云迁移指南.md。

## 一句话

**把"本地好用的 SQLite"搬上微信云托管生产 MySQL，并打穿"构建→部署→探针"的自动化验证链路——从早上 CrashLoop 到晚上 `038` 在线、真机订课、版本清理、日志命令行可见，一天跑完。**

## 背景：为什么必须迁移（30 秒版）

项目用 SQLite 起步（本地零依赖、开箱即用）。但生产是**微信云托管 = Docker 容器**——容器文件系统**不持久化**：闲置缩容、推送重建后 SQLite 数据文件全丢，每个用户都变"新号"（BUG-LEDGER #25）。本地好用 ≠ 生产可行。定案：生产换**平台托管的 MySQL**（数据存在平台侧，重建不丢），本地/CI 继续 SQLite——双驱动抽象，业务代码不感知。**这就是今天所有坑的总根源：一套代码跑两种数据库方言。**

## 四大类坑（按"为什么会发生"分类，不是按时间）

### A. SQL 方言坑——本地全绿，生产炸（#31/#32/#33 + `||`）

| 坑 | 现象 | 根因 | 为什么本地测不出 |
|---|---|---|---|
| DDL 默认值（#31） | 建表 ER_PARSE_ERROR | SQLite 写法 `VARCHAR DEFAULT (CURRENT_TIMESTAMP)` 在 MySQL 非法 | 测试永远跑在 SQLite 方言上 |
| 保留字（#32） | `change`/`date`/`role` 裸用报错 | SQLite 不保留、MySQL 8.0 保留 | 同上 |
| `last_insert_rowid()`（#33） | 订课/候补/发卡全 500 | MySQL 无此函数；LAST_INSERT_ID() 是连接级的，连接池下不可靠 | SQLite 单连接进程内有效 |
| `\|\|` 拼接 | 去重逻辑错 | SQLite 是拼接符，MySQL 是 OR | 同上 |

**共性：SQLite 假设。** 每一处都是"SQLite 单连接、方言自由"的产物。**对策**：业务 SQL 双方言兼容写法（反引号、run() 返回值传参、JS 侧拼接）+ **静态断言审计**（MYSQL-06/07/08/09 正则扫描 DDL/业务 SQL，防回退）——因为运行时测试永远测不到 MySQL 路径。

### B. 启动链路坑——探针 refused 的真相（#34）

**现象**：部署日志永远停在 seed 数据汇总，index.js 从未执行，「后端服务已启动」从来没出现过。
**根因**：`CMD ["sh","-c","node seed.js && node index.js"]`——seed 完成后**靠事件循环空转自然退出**。SQLite 无异步句柄，正常退出；**MySQL 连接池是活跃 TCP 句柄，事件循环不空 → seed 进程挂起 → `&&` 等不到 → index.js 永不启动 → 3000 没人监听 → 探针 refused → 部署回滚**。
**教训**：链式 CMD（A 退出才启动 B）任何一环挂起 = 整个服务死；「进程自然退出」假设在异步句柄（MySQL 池）下失效。
**根治（架构级）**：seed 移出启动阻塞位——`index.js` **先 listen（探针窗口内即监听 3000）→ 建表就绪 → 进程内幂等跑种子**。启动顺序从此不依赖任何"前一步正常退出"。MYSQL-09 断言防回退。

### C. 部署链路坑——构建失败会回滚旧镜像，你看到的是旧行为（#12/#24）

**现象**：修了代码重新部署，启动日志"没有任何新东西"，以为没修好。
**真相**：云托管部署有 3 个独立环节——**构建**（Git clone + docker build）→ **部署**（起容器）→ **探针**（检查 3000）。031/033/034 的「创建版本失败：任务失败」= **构建环节失败**（GitHub clone 网络问题，BuildId=0）→ 平台**回滚旧镜像**继续部署 → 探针打的、日志看的全是旧镜像。**代码修得再对，只要构建不过，你看到的就是旧行为。**
**对策**：
- 判定镜像新旧用「新镜像特征」：新路由返回 400（旧镜像 404）、web 管理页 200（旧 404）、ADMIN 保护 401（旧 400）——deploy-smoke.sh 30 秒定案
- 构建失败绕开 GitHub：**上传代码包部署**（`scripts/pack-deploy-zip.ps1` 打正斜杠 zip）
- 部署状态用 API 查（`scripts/cloudrun.sh records`，BuildId=0 即构建失败判据）

### D. 安全审计坑——上线前没发现（#14）

coach-assign（设教练）不在 ADMIN_PATHS 保护清单——任何能访问接口的人可把自己设成教练提权。#8 加保护时按记忆列清单，漏了它。**教训：保护类改动必须全量审计管理路由（grep 所有 /api/admin + 管理语义路由），人工清单不可靠。** 现在 deploy-smoke 把「coach-assign 无 token → 401」作为新镜像特征，永不再漏。

## 方法论教训（High-level 五条）

1. **存储选型先回答「目标平台提供什么」**：容器平台的文件系统语义（不持久化、不跨实例共享）与单机完全不同。「本地好用」≠「生产可行」——选型时就定为平台托管型存储（云托管内置 MySQL），本地用 SQLite 只当开发方言。
2. **双方言代码的测试盲区是结构性的**：运行时测试永远只跑一种方言——另一条路径的验证只能靠**静态断言 + 人工方言审计清单**（保留字、专属函数、拼接符、DDL 默认值，逐条 grep）。本项目的 MYSQL-01~09 + PASSES-01 就是这张安全网。
3. **启动链路要"先活后好"**：服务进程先监听（探针可过）再异步就绪（建表/种子/预热）。任何"前置步骤必须成功退出"的设计都是部署炸弹。
4. **部署失败要分环节定位，别被表象骗**：构建失败 → 回滚旧镜像 → 你验证的全是旧代码。先判定镜像新旧（特征探测），再查启动日志（「日志止于某处」= 该进程未退出/未启动），最后才谈代码。
5. **可见性就是生产力**：今天后半程的转折是 `tcbr` API 打通——部署日志、任务状态、版本列表、删除、冒烟全走命令行（scripts/cloudrun.sh），不再人肉复制粘贴控制台。调试痛苦度直接减半。

## 现在的架构形态（一天跑通后的最终态）

```
启动：node index.js
  → listen 3000（探针窗口内即可过）
  → driver.ready（MySQL 异步建表门闩 / SQLite 立即）
  → 进程内 seed.run()（幂等，已有数据跳过）

部署：Git push 自动构建（偶发失败） / 上传代码包 zip（绕开 GitHub）
验证：bash deploy-smoke.sh（8 项，30 秒定案新旧镜像）
运维：scripts/cloudrun.sh {records|logs|task|versions|delete|smoke}
```

生产环境变量（云托管 EnvParams）：`DB_DRIVER=mysql`、`MYSQL_*`、`WX_APPID/WX_SECRET`、`ADMIN_TOKEN`——全部配置且已验证。

## 数据

- BUG-LEDGER：新增 #30/#31/#32/#33/#34/#14（含 #34 架构加固）
- 测试：163 → 165 → 167 用例全绿（普通 + TZ=UTC 双时区）
- 版本：030~038（029-037 已清理，038 在线 100%）
- 代码：72c6378（启动加固）/ e97db2c（#14）/ ee0425f（deploy-smoke + cloudrun.sh）
