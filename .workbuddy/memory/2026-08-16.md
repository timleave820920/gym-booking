# 2026-08-16 工作日志

> 今日主题：**教练端重构全量交付（DESIGN #D1）+ 时区 bug 根治（#28）+ 云托管持久化方案（#25）+ 云端 API 验证 + 本地 git 恢复 + 首次结构评审**。
> 本日 .workbuddy 日志曾断更（仅 7 行），晚间结构评审时发现并补全（见文末「评审」节）——教训：最忙的日子恰恰最需要记录。

## 里程碑周检查（自动化首次运行）

- L2/L3/架构规模化·阶段2 确认完成（TODO 均 [x]）；L2 CI 配置在位，但受 git 未接回远端影响暂无法触发推送验证
- 缺陷台账一致性：发现 5e66f5f（教练介绍页 feat + avatar-download fix）提交信息无 BUG-LEDGER 编号；台账自身有 #14 编号重复 + 一条无编号记录（应为 #12）
- 覆盖率检查：63.37%（基线 82.77%，-19.4pp），主因 8/15 新增 server 代码（coaches 接口/avatar-download/次卡支付后端）未进 coverage.test.js 探针；全量 run-tests.js 128/128 全绿
- 遗留提醒：补登记 5e66f5f 台账；决定是否扩展覆盖率探针；修复台账 #14 重复编号

## DESIGN #D1 教练端重构——全量交付（任务 1-11）

**设计确认**：教练账号页面完全重构——三 Tab 工作台（我的课程/我的学员/结算）+ 签到窗口统一课后 30 分钟 + 凭证码纯数字 + 管理后台一键设教练。设计文档《教练端重构设计方案.md》确认后写入 DEV-BACKLOG（11 条任务）。

**当日全部完成**（146/146 全绿 + 探针 pass）：
1. 签到窗口统一：后端 LATE_WINDOW 120→30 + 学员端窗口/文案同步（+120→+30）+ 凭证码去 `GYM-` 前缀改 bookingId padStart(4) 纯数字 + coach-scan parseCode 同步
2. 分成配置单源：`coach_config` 表 + `server/coach-config.js`（默认课时 ¥100 / 签到奖励 ¥5）
3. 我的学员接口：`GET /api/coach/students?coach_openid`（已签到聚合：昵称/头像/最近课程+日期/has_note/total_classes）
4. 学员笔记接口：`coach_notes` 表（UNIQUE(coach,student)）+ GET/PUT 仅本人 upsert 幂等
5. 结算接口：`GET /api/coach/settlement?coach_id&month`（课次×课时单价 + 签到×奖励单价）
6. 设教练接口：`POST /api/admin/coach-assign`（校验档案存在/防重复绑定/role=coach + logOp 留痕）
7. coach-home 三 Tab 工作台：我的课程（今天起 7 天日期条 + 窗口内签到按钮 → 相机扫码/手动纯数字核销）
8. 学员 Tab：列表 + 详情（跟课记录 + 笔记编辑）
9. 结算 Tab：月份切换 + 指标卡（课次/签到人数）+ 金额（课时费/奖励/合计）
10. 登录分流：教练登录 → coach-schedule → coach-home；admin-students「设为教练」按钮 + 档案选择弹层
11. 新接口测试：COACH-xx 系列 + coverage 探针（146/146 + 探针 pass；顺带修探针假红 #27）

## #28 云托管时区 UTC——签到窗口差 8 小时（P0 生产错位）

**现象**：真机 10:30 签不了 11:00 的课（BUGS-INBOX #10）；本地 146/146 全绿——本地（北京时间）发现不了，只有 UTC 环境（云托管/CI）才红。
**根因**：容器 `node:22-alpine` 默认 UTC，业务代码裸 `new Date().getHours()` 等隐式依赖系统时区。
**修复（四层）**：①Dockerfile 装 tzdata + `TZ=Asia/Shanghai`（alpine musl 必须 tzdata 否则 TZ 不生效）②新建 `server/time.js`（Intl 显式 Asia/Shanghai），6 个域模块全部改走 time.js，SQL 比较显式传参 ③TIME-01~04 确定性断言 + `TZ=UTC` 模拟容器时区全量 150/150 ④CONVENTIONS 规矩 #9（取时间必须走 time.js）——本地复现命令 `TZ=UTC DB_PATH=/tmp/gym-test-clean.db node minitest/run-tests.js`。
**顺带**：student-my-courses 课后窗口 +120→+30（前后端不一致漏网）、协议文案「结束后 2 小时」→「30 分钟」、i18n 过期文案修正。

## #25 云托管数据持久化（P0，待用户控制台操作）

**根因**：云托管容器文件系统不持久化——闲置缩容/推送重建后 SQLite 全丢，用户每次登录变「新的号」；且容器无 server/.env → WX_SECRET 空 → 身份回退公共 demo_user。
**修复（代码侧完成）**：Dockerfile 建 `/data` 挂载点 + 《云托管持久化与身份配置.md》操作手册（控制台挂 CFS 到 /data + 环境变量 DB_PATH/WX_APPID/WX_SECRET）。**用户操作前勿做正式数据迁移**；真机 #13「无教练权限」根因即重建后库重置。

## 云端 API 层验证——DESIGN #D1 新路由全部在线（#12/#8 根因消除）

070f0ed 重建完成后云端直连探测：`/api/coach/settlement` 200（40 场次/400 元聚合正确）、`/api/coach/students` 404 业务拦截（demo_user 未绑定教练，正常）、`/api/coach/notes` 400 参数校验、`/api/admin/coach-assign` 400 参数校验——**旧镜像根因（#12/#8）消除**。待真机最终确认（教练「我的学员」「结算」Tab、admin-students 设教练流程）。

## git 本地对象库恢复（P0 完成）

- 远端历史早已接回（master=070f0ed），本地 `.git` 对象损坏（`Could not read cdc354875`）持续绕 /tmp/gym-remote 提交
- **方案**：备份损坏 .git → `/tmp/git-broken-backup-20260816`，用干净 clone `/tmp/gym-remote/.git` 整体替换（工作区文件保留）→ git status/log/commit 全部恢复
- 行尾噪音清理：209 个文件 CRLF/LF 差异显示 M——`git add --renormalize .` 一键清空（哈希不变、仅刷新索引 stat）；net-config.json 运行时噪音保持不提交
- 文档同步：CLAUDE.md 已知坑区「本地 git 损坏」改「已修复」，TODO P0 标 [x]（提交 9871f46，hook 150/150 绿）
- **教训**：`git add --renormalize` 是 Windows 下全仓 CRLF 噪音的官方解法；备份要放 /tmp（本地 .git 同级备份会制造混乱）

## 首次结构评审（晚间，用户发起）

用户提出 4 维评估标准（流程清晰度/失败学习/瘦身复用/知识沉淀），输出《项目审查-2026-08-16.md》+ 三项落地：deploy-smoke.sh 部署冒烟、.workbuddy 入库（本日志补全）、每周五评估机制（CLAUDE.md 承诺 + cron 闹钟）。详见评审文档。
