# 教练端重构设计方案（DESIGN #D1）

> 状态：✅ 已确认（2026-08-16，用户确认 4 点：①coach-schedule 废弃 ②课次=本月已结束场次数 ③验证码改纯数字 ④设教练入口在学员管理页）
> 任务已写入 DEV-BACKLOG.md，说「开始dev」即开工。

## 一、需求描述

**目标**：把教练端从「今日课表+名单+扫码」三张散页，重构为一个完整的教练工作台，含三大板块：**我的课程 / 我的学员 / 结算**。让教练在一个页面里完成带课全流程，并用实时分成激励多带课、多招新。

**用户场景**：
- 教练早上打开小程序 → 看到未来 7 天自己的课程，该签到的课直接扫码核销学员
- 想回顾自己带过哪些学员 → 我的学员列表（昵称/头像/课程/日期）+ 学员笔记（下次课前的提醒）
- 月底看收入 → 结算页显示本自然月分成数字，实时更新

**验收标准**：
1. 我的课程：显示未来 7 天（含今天）该教练的场次；开课前 30 分钟～结束后 30 分钟内的课程显示签到入口
2. 签到：点签到按钮 → 相机扫码（学员二维码 GYM-编号）或手动输入验证码；验证通过 → 记录为已签到，界面即时刷新
3. 我的学员：收录所有**已签到**学员（含昵称/头像/课程详情/日期），可添加/编辑学员笔记（仅自己可见）
4. 结算：默认显示本自然月（课时费 + 签到奖励），月切换器可回看历史月份
5. 管理后台可一键把微信账号设为教练（绑定教练档案）
6. 签到窗口全局统一为：开课前 30 分钟 ～ 课后 30 分钟（后端裁决，前端同步）

## 二、界面设计

### 结构总览

教练端登录后直达**教练工作台单页**（`pages/coach-home/index`），页内顶部三段式 Tab 切换（课程/学员/结算），不做小程序全局 tabBar——受 tabBar 5 页上限限制（学员端已占 4 个），页内导航最稳。

```
┌──────────────────────────┐
│  👋 上午好，喻教练        │  ← 问候 + 教练名（沿用现有问候样式）
│  [ 我的课程 | 我的学员 | 结算 ]  ← 三段式 Tab
├──────────────────────────┤
│                          │
│  Tab1 我的课程           │
│  ┌────────────────────┐  │
│  │ 今天 8/16 周六      │  │  ← 周日期条（今天起 7 天，复用预约页样式）
│  │ [日][一][二]...     │  │
│  ├────────────────────┤  │
│  │ 🕙 10:00-11:00      │  │  ← 课程卡（时间|课程名|场馆|席位）
│  │ Hyrox冲冲冲         │  │
│  │ 成华好事馆 · 8/20   │  │
│  │     [签到] ← 绿色高亮│  │  ← 仅窗口内显示；其他显示"未开始/已结束"
│  └────────────────────┘  │
│  Tab2 我的学员           │
│  ┌────────────────────┐  │
│  │ [头像] 王小美        │  │  ← 学员卡（头像|昵称|最近课程+日期|笔记标记）
│  │   Hyrox新手 8/15    │  │
│  │   📝 有笔记          │  │
│  └────────────────────┘  │
│  Tab3 结算               │
│  ┌────────────────────┐  │
│  │ 2026年8月  [◀][▶]   │  │  ← 月份切换器
│  │ 本月课次  12 节      │  │
│  │ 签到人数  58 人      │  │
│  │ 课时费    ¥1,200    │  │
│  │ 签到奖励  ¥290      │  │
│  │ 合计      ¥1,490    │  │  ← 大数字，实时更新
│  └────────────────────┘  │
└──────────────────────────┘
```

### 各 Tab 细节

**Tab1 我的课程**
- 周日期条：今天起 7 天（含今天），复用 `student-courses` 的 buildWeek 逻辑（2026-08-16 刚统一）
- 课程卡：时间区间（左）+ 右侧三行（黑体大字课程名 / 中等场馆·日期 / 席位「已约/总数」）——复用教练介绍页课程条样式
- 签到入口规则：**开课前 30 分钟 ～ 课后 30 分钟**内的场次，卡右侧显示绿色「签到」按钮；窗口外显示「未开始 / 已结束」灰字
- 点签到 → 相机扫码（wx.scanCode，复用 coach-scan 的 parseCode 逻辑）或弹窗「手动输入验证码」（**纯数字**，复用 manualCheckin 逻辑）
- 签到成功 → 绿勾「已签到」，列表即时刷新

**Tab2 我的学员**
- 列表：仅收录**已签到**学员（按最近签到时间倒序），每项 = 头像 + 昵称 + 「最近课程名 + 日期」+ 有笔记则显示 📝 标记
- 点学员 → 详情页/弹层：该学员所有跟课记录（课程名/日期/时长）+ 笔记编辑区（textarea + 保存）
- 笔记：仅教练本人可见（后端按 coach_openid 隔离）

**Tab3 结算**
- 月份切换器：默认本月，可回看历史月份（数据由后端按自然月聚合）
- 指标卡：本月课次（该教练本月已结束场次数）、签到人数（本月该教练场次的签到总数）
- 金额：课时费 = 本月课次 × 课时单价；签到奖励 = 签到人数 × 签到单价；合计 = 两者之和
- 实时性：进入页面即拉取最新；签到后切回结算自动刷新

### 管理后台「设为教练」

后台学员列表每行加「设为教练」操作 → 弹层选择教练档案（下拉 coaches 表，可搜索）→ 确认后该学员 role=coach + coaches.user_openid 绑定。

## 三、程序架构

### 数据模型

| 表 | 变更 | 说明 |
|---|---|---|
| `users` | 无结构变更 | role 字段已支持 'coach' |
| `coaches` | 已有 user_openid 字段，复用 | 设教练时写绑定 |
| `course_sessions` | 无变更 | 已有 coach_id/date/start_time/end_time，结算聚合数据源 |
| `bookings` | 无变更 | checkin_at 已存在，签到数据源 |
| **`coach_notes`** | **新建** | 学员笔记：`id / coach_openid / student_openid / content / updated_at`，UNIQUE(coach_openid, student_openid) |
| **`coach_config`** | **新建**（配置单源） | 分成配置：`course_fee_fen`（课时单价，默认 10000=¥100）、`checkin_reward_fen`（签到奖励，默认 500=¥5） |

### 后端接口（新增 4 个，全部复用现有事务/校验风格）

| 接口 | 方法 | 参数 | 返回 |
|---|---|---|---|
| `GET /api/coach/sessions` | GET | `coach_id, from, to` | 场次列表（含 status 窗口判定字段 `checkin_open`）——**已存在**（coach-profile 在用），扩展 checkin_open |
| `GET /api/coach/students` | GET | `coach_openid` | 我的学员：`[{ openid, nickname, avatar, last_course, last_date, has_note, total_classes }]` |
| `GET/PUT /api/coach/notes` | GET/PUT | `coach_openid, student_openid` + `content` | 学员笔记读写（仅本人） |
| `GET /api/coach/settlement` | GET | `coach_id, month(YYYY-MM)` | 结算：`{ month, sessions, checkins, course_fee_fen, reward_fen, total_fen }` |
| `POST /api/admin/coach-assign` | POST | `openid, coach_id` | 管理后台设教练：role=coach + coaches.user_openid 绑定 |

### 签到凭证码改为纯数字 + 签到窗口全局统一（改动现有）

**凭证码纯数字**（用户确认 ③）：
| 位置 | 现状 | 改为 |
|---|---|---|
| `miniprogram/pages/student-checkin/index.js:37,58` | `'GYM-' + padStart(4)` | **纯数字** `String(bookingId).padStart(4, '0')`（二维码与文字同码） |
| `miniprogram/pages/coach-scan/index.js:41` | parseCode 兼容 GYM- 和纯数字 | 只认纯数字（保留 GYM 兼容可，无成本） |
| `miniprogram/pages/coach-scan/index.js:68,70` | 文案「如 GYM-0001」 | 改「纯数字签到码」示例 |

**签到窗口全局统一**：
| 位置 | 现状 | 改为 |
|---|---|---|
| `server/db/bookings.js:166-167` | EARLY_WINDOW=30 / LATE_WINDOW=120 | LATE_WINDOW **120→30**（课后 30 分钟） |
| `miniprogram/pages/student-checkin/index.js:38,44,54-57` | 文案「结束后 2 小时」+120 | 改「结束后 30 分钟」+30，窗口计算同步 |
| `minitest/run-tests.js` CHK-02/04/07 | 用例造数/文案 | 按新窗口调整（CHK-02 造数仍成立：课后 20 分钟签到 < 30） |
| `CONVENTIONS.md` B1 规则 | 「课后 2 小时」 | 「课后 30 分钟」 |

> ⚠️ 注意：B1 是"签到是唯一到课证明"基准，窗口缩短只影响可签到时间段，不影响统计语义。能量币签到奖励、成就统计全部基于 checkin_at，不受窗口变化影响。

### 教练端页面

| 页面 | 动作 |
|---|---|
| `pages/coach-home/index` | **新建**：三 Tab 工作台（课程/学员/结算），登录后教练直达此页（替换现有 coach-schedule 跳转） |
| `pages/coach-schedule/index` | 保留（"今日课表"语义并入我的课程 Tab；或废弃，看确认意见） |
| `pages/coach-scan/index` | 扫码逻辑抽出复用（parseCode/manualCheckin 移到 utils 或保留页面供 deep-link） |
| `pages/coach-students/index` | 保留（场次名单详情，从课程卡进入） |
| `pages/admin-students/index` | 加「设为教练」操作 + 弹层 |
| `pages/admin-coaches/index` | 从 mock 改接 coaches 表真实数据（可选，P2） |

### 幂等/并发/安全要点

- 签到沿用现有 checkinBooking（事务 + checkin_at 防重 + 权限校验）
- 结算为纯只读聚合（SUM/COUNT），无写库、无并发问题
- 笔记 PUT 幂等（UNIQUE 约束，upsert 语义）
- 设教练接口：校验目标账号存在 + 教练档案存在 + 防重复绑定；logOp 留痕

## 四、已确认的决定（2026-08-16 用户确认）

1. **coach-schedule 今日课表页**：废弃（登录跳转改 coach-home），coach-students 名单页保留
2. **结算的「本月课次」口径**：该教练本月**已结束**的场次数（不管有没有人签到）
3. **验证码格式**：纯数字（bookingId 4 位补零，二维码与文字同码）
4. **管理后台设教练入口**：学员管理页每行「设为教练」

## 五、开发任务拆分（已写入 DEV-BACKLOG.md）

| # | 任务 | 优先级 | 依赖 |
|---|---|---|---|
| 1 | 后端：签到窗口 LATE_WINDOW 120→30 + 前端文案同步 + CHK 用例调整 + CONVENTIONS B1 更新 + 凭证码纯数字 | P0 | 无 |
| 2 | 后端：coach_config 表 + coach-config.js 配置单源 | P1 | 无 |
| 3 | 后端：GET /api/coach/students（已签到学员聚合） | P1 | 无 |
| 4 | 后端：GET/PUT /api/coach/notes（coach_notes 表） | P1 | 2 |
| 5 | 后端：GET /api/coach/settlement（月度聚合） | P1 | 2 |
| 6 | 后端：POST /api/admin/coach-assign（设教练） | P1 | 无 |
| 7 | 前端：coach-home 三 Tab 工作台（课程 Tab + 签到入口 + 扫码/手动核销） | P0 | 1 |
| 8 | 前端：学员 Tab（列表 + 详情 + 笔记编辑） | P1 | 3,4 |
| 9 | 前端：结算 Tab（月份切换 + 指标 + 金额） | P1 | 5 |
| 10 | 前端：登录跳转改 coach-home + 后台设教练按钮 | P1 | 6,7 |
| 11 | 测试：新接口全量测试用例（run-tests.js + coverage 探针） | P1 | 2-6 |
| 12 | E2E 验证：本地 + 真机签到/结算链路 | P1 | 7-10 |
