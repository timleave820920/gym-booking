# 新学员标记设计方案（DESIGN #D11）

> 2026-08-19 提出｜状态：设计中（待确认后写入 DEV-BACKLOG）
> 一句话：**教练在课程学员名单里一眼看出谁是第一次上这类课的学员（按课程类型维度，不是按新用户）。**

## 一、用户拍板口径（2026-08-19）

| 决策点 | 拍板 |
|---|---|
| 「上过」判定 | **签到过才算上过**（B1：签到是唯一实际到课证明）——该学员曾在该课程类型（category）签到过任何一场课 → 老学员；否则 → 新学员（含已订未上、候补转正等） |
| 展示形式 | 名单每行学员右侧「新」绿色小徽标 + 名单顶部统计「新学员 N 人」 |
| 标记范围 | **仅正式订课学员**（候补排队中不标） |

## 二、判定口径（规则定义）

```
学员 × 场次所属课程 category：
  该学员历史签到记录中，存在同 category 的任何一场课（status=completed 且 checkin_at 非空）
    → 老学员（不显示徽标）
  否则
    → 新学员（显示「新」徽标）
```

- 判定基于签到（B1 唯一实际到课证明，不依赖订课/退订状态）
- 已订本场但未开课 → 该学员此前无同类型签到 → 新学员（教练正好识别「第一次来这类课」）
- 候补（waitlist）学员不标——不在正式名单语义内

## 三、数据与接口

现有接口 `GET /api/sessions/:id/students`（handleSessionStudents → listBookingsBySession）返回学员列表。

**改造**：该接口每个学员对象附加 `isNewCategory`（true = 新学员）。

SQL 实现思路（单场次名单规模小，逐学员查询可接受；或一条聚合）：

```sql
-- 1) 场次 → 课程 category
SELECT category FROM sessions s JOIN courses c ON s.course_id = c.id WHERE s.id = ?

-- 2) 名单里每个学员：
--    该学员在同 category 课程下是否有签到记录
--    （bookings 关联 sessions → courses，status=completed AND checkin_at IS NOT NULL）
SELECT COUNT(*) FROM bookings b
  JOIN sessions s ON b.session_id = s.id
  JOIN courses c ON s.course_id = c.id
  WHERE b.user_openid = ? AND c.category = ? AND b.status = 'completed' AND b.checkin_at IS NOT NULL
```

响应示例：

```json
{
  "code": 200,
  "students": [
    { "openid": "xxx", "nickname": "阿凯", "checkin_at": null,
      "isNewCategory": true, ... },
    { "openid": "yyy", "nickname": "小萌", "checkin_at": "2026-08-19 10:00",
      "isNewCategory": false, ... }
  ],
  "checked": 1, "total": 2,
  "newCount": 1
}
```

- 候选补丁：`listBookingsBySession` 查询里带出 category 或在校验后逐学员查（字段少、规模小，性能无忧）

## 四、界面设计（教练端课程学员名单）

```
我的课程 → 点某场次 → 学员名单
┌──────────────────────────────────┐
│ 学员名单（2 人 · 新学员 1 人）      │  ← 顶部统计
├──────────────────────────────────┤
│ 阿凯          [新]                │  ← 绿色小徽标
│ 小萌  已签到 ✓                    │  ← 老学员无徽标
└──────────────────────────────────┘
```

- 「新」徽标：绿色圆角小标签（12 字级），仅正式订课学员行显示
- 顶部统计：`学员名单（N 人 · 新学员 M 人）`
- 候补区学员不标

## 五、测试计划

- **NEW-01**：无同类型签到记录 → isNewCategory=true
- **NEW-02**：有同类型签到 → false
- **NEW-03**：不同类型课程签到过 → 仍为 true（类型维度，非用户维度）
- **NEW-04**：已订未上（无签到）→ true
- **NEW-05**：候补学员不在名单/不标
- **NEW-06**：响应含 newCount 且 = 名单中新学员数
- **FRONT-xx**：名单页「新学员」统计 + 徽标 wxml 断言防回退
- coverage 探针：isNewCategory 字段

## 六、实施步骤（确认后写入 DEV-BACKLOG）

1. **P0｜后端**：sessions/:id/students 附加 isNewCategory + newCount（NEW-01~06 测试）
2. **P0｜前端**：coach-students 名单页徽标 + 顶部统计
3. **P1｜防回退**：FRONT 断言 + coverage 探针

## 七、待确认点

1. 「新」徽标绿色圆角小标签 + 顶部「新学员 N 人」，样式认可？
2. 名单页顶部统计文案「学员名单（N 人 · 新学员 M 人）」认可？
