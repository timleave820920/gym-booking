# 客户来源追踪设计方案（DESIGN #D7）

> 2026-08-19 提出｜状态：设计完成（2026-08-20 调研修正，待确认后写入 DEV-BACKLOG）
> 一句话：**每个渠道发自己的专属小程序码/链接，用户点进来系统自动识别来源，无需用户告知；后台看「哪个渠道带来多少人、多少人订课、多少人复购」。**

## 〇、调研结论（2026-08-20，先验平台能力，DESIGN #D2 教训）

对微信开放能力与各渠道投放形态做了调研，5 条结论直接改方案：

1. **微信无「open-channel」通用 API**：没有任何官方接口能拿到「用户是从哪个 App/页面进的小程序」——归因只能靠我们自己的入口参数（scene/query），方案正确性依赖此前提。
2. **各渠道无法直接拉起小程序**（小红书/抖音/美团/大众点评 App 内均无跳转小程序能力，仅微信生态内可扫一扫/URL Link/分享）：渠道投放的实际形态 = **渠道内容里放「渠道专属小程序码」图片，引导用户保存 → 微信扫码打开**；URL Link 仅作辅助（部分平台外链有限制）。→ 渠道码形态主推小程序码，**与 D13 签到码复用同一套 wxacode 生成体系**（`GET /api/admin/checkin-qr` 已打通 getUnlimited 链路，D7 加 `?channel=c1` 参数即可按渠道出码，边际成本≈0）。
3. **scene 短码映射渠道表（替代 `s=xhs`）**：wxacode scene 限 ≤32 字符且**不允许 % 等特殊字符、不能中文**；改用固定短码 `c1/c2/c3/c4/c9` 映射 CHANNELS 配置表（code→短码→渠道名），码内不带任何业务字面量——渠道改名/新增只动配置表，已印的码不受影响。
4. **匿名期即可归因（最大修正）**：`wx.login` 无需用户授权即可换 openid——用户扫码进落地页那一刻就能静默登录拿 openid 并绑定归因，**不必等用户注册**。原「注册时传 source」会丢失「打开→注册」之间的归因窗口，改为：**打开渠道码 → 静默 wx.login → 立即归因**（ID-mapping 成本极低：openid 是唯一身份，落地页先建 openid 占位记录）。
5. **双轨归因 + 30 天保护期**：单一 first-touch 会漏掉「老用户在美团看到优惠券回来下单」的促单归因。方案改为：
   - **first-touch（拉新归因）**：首次打开渠道码即绑定 `users.source`（首次为准不覆盖）——回答「用户是哪个渠道拉来的」
   - **last-touch（促单归因）**：每笔订单记 `orders.channel_id`——回答「这笔钱是哪个渠道带来的」；从首次归因起 30 天保护期，期内渠道码打开可更新 orders 归因，期外不抢（防老用户被渠道码反复拉走刷 KPI）
   - 渠道维度加 **batch 批次**（同渠道不同投放批次：不同笔记/不同团购券），回答「哪条内容带来的」

## 一、需求背景与用户拍板口径

- 用户提出：追踪客户来源平台（小红书/抖音/大众点评/美团等）
- **收集方式（用户拍板）**：链接/小程序码中识别来源，**不需要用户告诉我们**——各渠道投放专属小程序码，打开即归因
- **追踪深度（用户拍板）**：全链路转化漏斗——来源 → 注册 → 首订 → 复购
- **渠道清单（用户拍板）**：小红书、抖音、大众点评、美团（+其他兜底，可配置）

## 二、核心机制：渠道码扫码归因（复用邀请追踪 + D13 签到码体系）

### 2.1 渠道码（短码映射 + 复用 wxacode）

| 形态 | 参数位置 | 示例 | 生成方式 |
| --- | --- | --- | --- |
| 小程序码（主） | scene 短码 | `scene=ch1`（小红书） | `GET /api/admin/channel-qr?channel=c1`（复用 D13 getUnlimited 链路，path=`pages/landing/index`） |
| URL Link（辅） | query | `?channel=c1` | 同一短码，parseSource 统一解析 |

- CHANNELS 配置单源（照 member-config.js 模式）：`{ c1:'小红书', c2:'抖音', c3:'大众点评', c4:'美团', c9:'其他' }`，短码恒定、名字可改
- 渠道码 path 统一指向**落地页**（未注册也能静默登录归因），不是登录页——登录页有用户教育成本，落地页可以只做归因 + 引导

### 2.2 捕获与归因规则（前端 + 后端）

- `app.js onLaunch/onShow` 统一解析 `parseSource(options)`：兼容 `query.channel` 与 `scene=ch1` 两种形态，命中 CHANNELS 短码才认
- **捕获即归因（匿名期）**：落地页 `wx.login`（无需授权）→ 后端静默建档（users 表 openid 占位，role=student）→ 写入归因：
  - 首次归因（users.source 空才写）：绑定渠道 + 渠道批次
  - last-touch：写近期归因缓存（users 表加 `last_channel` + `last_channel_at`），下单时转写 orders.channel_id；**30 天保护期**——`last_channel_at` 距今 ≤30 天才更新
- 与邀请追踪共存：`?channel=c1&inviter=xxx` 两个参数独立解析、互不干扰

### 2.3 落库时机表

| 时机 | 动作 |
|---|---|
| 打开渠道码（未建档） | 静默 wx.login → users 建档 → `users.source=c1`（first-touch）+ `last_channel=c1`（last-touch 起点） |
| 打开渠道码（已建档无 source） | 绑定 source（渠道转介绍归因） |
| 打开渠道码（已有 source） | 30 天保护期内更新 last_channel/last_channel_at；期外不更新 |
| 支付成功 | orders.channel_id = 下单时读取的 last_channel（落订单，不可变，钱闭环留痕） |
| channel_open 事件 | course_events 记 `channel_open`（openid + source + batch）→ 漏斗「打开」计数（D13 先例：复用事件表） |

## 三、数据模型

```sql
-- 1) users 表加列（SQLite try/exec + MySQL MYSQL_ENSURE_COLUMNS 幂等补列，照 life_photo / checkin 先例）
ALTER TABLE users ADD COLUMN source VARCHAR(10) DEFAULT '';          -- first-touch 拉新归因（短码 c1~c9）
ALTER TABLE users ADD COLUMN last_channel VARCHAR(10) DEFAULT '';    -- last-touch 促单归因（30 天保护期）
ALTER TABLE users ADD COLUMN last_channel_at DATETIME DEFAULT NULL;  -- last-touch 时间戳（保护期判据）
ALTER TABLE users ADD COLUMN channel_batch VARCHAR(50) DEFAULT '';   -- 首次归因的投放批次

-- 2) orders 表加列（下单时快照，不可变——归因是钱链路的一部分）
ALTER TABLE orders ADD COLUMN channel_id VARCHAR(10) DEFAULT '';

-- 3) 渠道点击事件（复用 course_events 表，event_type='channel_open'，source=短码 + batch=批次）

-- 4) 渠道清单 + 批次配置（代码单源 config 文件，照 member-config.js 模式）
CHANNELS = {
  c1: '小红书',
  c2: '抖音',
  c3: '大众点评',
  c4: '美团',
  c9: '其他'
}
-- 批次（P2 后台维护，初期代码单源）：batch_id → { channel: 'c1', name: '8月小红书笔记A', created_at }
```

## 四、接口设计

| 接口 | 说明 |
|---|---|
| `POST /api/auth/login`（现有，扩展） | 落地页静默登录：请求体新增可选 `channel`/`batch` → first-touch 绑定 + last-touch 保护期更新（幂等，未登录同接口） |
| `GET /api/channel-config` | 渠道码清单（短码→中文名），小程序端/后台共用 |
| `GET /api/admin/channel-qr?channel=c1` | 按渠道生成专属小程序码（复用 D13 getUnlimited 链路；无 WX_SECRET → 400 明确报错） |
| `GET /api/admin/source-analysis?days=30` | 漏斗聚合：每渠道（含批次） 打开→注册→首订→复购 四级计数 + 各级转化率 + 7/30 天趋势（含未归因 other/空） |
| `POST /api/admin/channels` | 运营后台维护渠道清单/批次（P2 可选，初期代码单源即可） |

## 五、界面设计（web 运营数据 tab 新增折叠卡「📊 客户来源」）

```
📊 客户来源                          [近7天 ▾ | 近30天]
┌─────────────────────────────────────────────────┐
│ 渠道      打开   注册   转化   首订   复购         │
│ 小红书     86    12   14.0%   8    3             │
│ 抖音      45     6    13.3%   4    1             │
│ 大众点评   30     9    30.0%   7    4  ← 转化最好  │
│ 美团      20     2    10.0%   1    0             │
│ 未归因     —     53    —      31   12            │
├─────────────────────────────────────────────────┤
│ 漏斗（以小红书为例）                              │
│ 打开 ████████████ 86                              │
│ 注册 ███ 12                                       │
│ 首订 ██ 8                                         │
│ 复购 █ 3                                          │
│ （条形图，宽度=各级相对打开量，逐级递减一目了然）     │
│ （渠道行可展开批次明细：8月笔记A / 9月团购券…）      │
└─────────────────────────────────────────────────┘
```

- 表格默认按注册数降序；转化率色阶（>30% 绿 / 10~30% 黄 / <10% 红）
- 漏斗默认显示注册数最多的渠道，点击渠道行切换
- 渠道行展开批次明细（last-touch 归因按 batch 聚合）
- 趋势区：各渠道注册数 7 天折线（复用 #D4 canvas 趋势图模式）

## 六、测试计划

- **SRC-01**：落地页带 `channel=c1` 静默登录 → users.source='c1' 落库（无需注册）
- **SRC-02**：重复打开不同渠道码 → first-touch 不覆盖（首次为准）
- **SRC-03**：已建档无 source 用户打开渠道码 → 绑定；已绑定不覆盖
- **SRC-04**：last-touch 30 天保护期：期内更新 last_channel / 期外不更新（直插 last_channel_at 造期外）
- **SRC-05**：下单 → orders.channel_id = last_channel 快照（支付后不可变）
- **SRC-06**：匿名打开 → channel_open 事件落库（openid 有值——调研修正：wx.login 即拿 openid，不再有真匿名）
- **SRC-07**：漏斗聚合口径（打开/注册/首订/复购计数与转化率断言，含批次明细）
- **SRC-08**：`?channel=c1&inviter=yyy` 双参数共存互不干扰
- **SRC-09**：未知短码 `channel=cX` 不认（防随意造码）
- **FRONT-xx**：web「客户来源」折叠卡 + 渠道码生成按钮静态断言防回退
- coverage 探针：source-analysis、channel-qr 接口

## 七、实施步骤（写入 DEV-BACKLOG 后按序执行）

1. **P0｜数据层**：users/orders 幂等补列 + CHANNELS 配置单源 + login 接收 channel/batch（first-touch + last-touch 保护期逻辑）
2. **P0｜前端捕获**：app.js parseSource（query + scene 短码兼容）+ 落地页静默登录归因
3. **P1｜渠道码**：GET /api/admin/channel-qr（复用 D13 getUnlimited 链路）+ web 生成入口
4. **P1｜漏斗接口**：GET /api/admin/source-analysis（打开/注册/首订/复购 + 批次 + 趋势）
5. **P1｜后台看板**：web 运营数据 tab「客户来源」折叠卡
6. **P1｜测试**：SRC-01~09 + FRONT 断言 + coverage 探针
7. **P2｜渠道管理**：后台增删渠道清单/批次（初期代码单源即可，先不做）

## 八、已拍板点（2026-08-20 调研后）

1. **渠道码形态**：主推小程序码（各渠道 App 无法直接拉起小程序，只能扫码），URL Link 仅辅助；复用 D13 wxacode 生成体系
2. **scene 用短码 `c1~c9` 映射渠道表**（scene 限 32 字符、禁 %、禁中文；短码改渠道名不动已印的码）
3. **归因双轨**：first-touch（users.source，首次为准）+ last-touch（orders.channel_id 快照，30 天保护期）
4. **匿名期归因**：落地页静默 wx.login 即归因，不等注册（ID-mapping 成本极低）
5. **批次维度**：同渠道不同投放内容可区分（P2 后台维护，初期代码单源）
