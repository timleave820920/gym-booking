# 微信云托管扩缩容配置指引（min/max 实例数）

> 2026-08-18 整理。针对并发目标（压测 500 并发全绿、远期 5000）的实例数配置说明。
> 配置入口在**微信云托管控制台**（本机无 tccli 云托管权限，控制台手动配置）。

---

## 一、配置入口

1. 微信开发者工具 → 顶部「云开发」/「云托管」控制台（或浏览器打开微信云托管控制台）
2. 左侧「服务管理」→ 选择服务 **gym-server**
3. 「服务设置」→「**扩容配置**」（部分版本在「服务详情 → 扩缩容」）

## 二、三个配置项

| 配置项 | 含义 | 推荐值 |
|---|---|---|
| **最小实例数** | 低于此数不会缩容（0 表示闲时可缩到 0） | **1**（避免每次首次访问冷启动 3~5s 白屏；若接受冷启动可设 0 省成本） |
| **最大实例数** | 高并发时最多弹到几台 | 先 **5**，观察真实流量再调（见第四节） |
| **单实例并发数** | 每台实例同时处理的请求数上限 | **100**（Node 原生 http 单实例扛 250+ 并发无压力，100 留余量防雪崩） |

## 三、理论承载计算

```
峰值承载 = 最大实例数 × 单实例并发数
例：5 × 100 = 500 并发/秒（压测基线即 500）
    20 × 250 = 5000 并发/秒（远期目标，需配额+MySQL 配合）
```

## 四、⚠️ 5000 并发目标的真实瓶颈是 MySQL，不是实例

1. **实例扩容无压力**：单实例压测 500 并发全绿（WAVE=50 验收基线），Node http 是 IO 型，实例数 × 并发数线性扩展。
2. **MySQL 连接是瓶颈**：每实例连接池 `MYSQL_POOL_SIZE=50`（`server/db-driver.js` 可配）。
   - 20 实例 × 50 = 1000 个连接 → 云端 MySQL 连接数上限（云托管内置 MySQL 有配额，需控制台查看/调整）
   - **超过 MySQL 连接上限 = 新请求排队/报错**，多弹实例反而加剧争抢
3. **联动调整清单**（冲 5000 时）：
   - 控制台确认 MySQL 实例规格与最大连接数（必要时升配）
   - `MYSQL_POOL_SIZE` 降到 **20~30**（单实例不需要 50，连接池按实例分摊更稳）
   - 最大实例数 × 新池大小 ≤ MySQL 最大连接 × 0.8（留安全余量）

## 五、成本与注意事项

- **按量计费**：实例数 × 运行时长计费，max=5 且流量低时只付 min 实例的钱（云托管有闲置缩容）
- 最小实例 0 与 1 的成本差异：1 台 24h 常驻（约 ¥0.0x/小时按规格），0 则彻底零成本但有冷启动
- 扩容弹起有**冷启动窗口**（约 10~30s），突增流量时先弹后稳属正常
- 配额：账号默认有实例配额，调 max 超过配额需在腾讯云控制台申请扩容配额
- 配置修改**即时生效**，无需重建版本/重启服务

---

## 六、当前状态与建议动作

| 项 | 现状 | 建议 |
|---|---|---|
| 最小实例数 | 待查（控制台） | 1 |
| 最大实例数 | 待查（控制台） | 5（观察 1~2 周再调） |
| 单实例并发数 | 待查（控制台） | 100 |
| MYSQL_POOL_SIZE | 50（默认） | 暂不动；冲 5000 时按第四节调 |
