# 开发总结 2026-08-18 · CI 全绿战役：MySQL 方言坑六批收尾 + #D5/#D6 收官 + #59 画像卡转需求

> High-level 复盘。缺陷细节见 BUG-LEDGER #58/#60/#61；设计见 浏览埋点与用户画像设计方案.md / 运营小助理设计方案.md。

## 一句话

**让从第一天就红的 CI test-mysql job 首次全绿（六批 MySQL 方言修复），#D5 浏览埋点+画像 与 #D6 运营小助理 全部交付，#59 从"bug"澄清为"一次性画像卡"需求落地——今天把双数据库驱动的最后一层方言债还清，DEV-BACKLOG D1~D6 全部闭环。**

## 背景：为什么 test-mysql 一直红（30 秒版）

DESIGN #D2 起生产走 MySQL（云托管内置），本地/CI 走 SQLite——双驱动抽象 `db-driver.js` 让业务代码"不感知"底层。但**"不感知"是假象**：方言差异（LIMIT 参数编码、保留字、聚合函数、DECIMAL 类型）在运行时才炸。本地 150 用例全绿 ≠ MySQL 全绿——test-mysql job（MySQL 8 service）自上线起全红，10 分钟挂满超时，成了"知道要修但一直没修"的债。今天用户要求"你看下 CI 是否全绿"，一次性还清。

## 六批修复（按依赖关系排，每批都是一个独立根因）

| 批 | 根因 | 现象 | 修复 |
|---|---|---|---|
| 1（#60 主根因） | **MySQL 事务被拆散**——`exec('BEGIN')` 在连接池随机连接执行，get/all/run 又走池里别的连接，COMMIT 落在无事务连接上是 no-op，事务连接悬空污染池 | 几乎所有事务用例红：订课/候补/结算/仪表盘 | `MysqlDriver._txConn` 事务专用连接：BEGIN 从池取单连接 beginTransaction，期间所有语句复用，COMMIT/ROLLBACK 释放 |
| 2 | **mysql2 参数编码**——number 一律编码为 DOUBLE，`LIMIT ?`/`OFFSET ?` 报 ER_WRONG_ARGUMENTS（MySQL 要求整数字面量） | 列表类接口 500（ADMIN-28b 等） | 5 处 LIMIT/OFFSET 改文本拼接 `Number()||默认值`（数字强制整数，无注入面） |
| 3 | **保留字裸用**——`change`/`date`/`read`/`desc` 在 MySQL 8 是保留字，SQLite 不保留 | dashboard/report/coin/messages 裸用报语法错 | 9 处加反引号（双方言兼容）；模板字符串内反引号须 `\`` 转义 |
| 4 | **测试直连残留 SQLite 方言**——`datetime('now','localtime','-N minutes')` | RCG 套件 UTC 下时间错位/语法错 | 改业务层 time.js 算好参数传入（规矩 #9 顺带收敛） |
| 5（#61） | **`MAX(a,b)` 是 SQLite 标量扩展**，MySQL 只有单参数聚合 MAX；`GREATEST` 又是 MySQL 专有（SQLite 无此函数）——没有公共函数 | 退订恢复余位直接语法报错 | `CASE WHEN booked_count > 0 THEN booked_count-1 ELSE 0 END`（100% 双方言兼容） |
| 6 | **SUM/ROUND 返回 DECIMAL**，mysql2 默认转**字符串**，SQLite 返回 number——`typeof === 'number'` 断言全炸 | DASH-07 只剩最后一个红 | `decimalNumbers: true` 连接池配置（金额/计数均 INT 聚合，2^53 内安全）+ dashboard 金额包 `fen()` 双保险 |

**外加两个测试基建修复**：
- **run-tests 挂起**：MySQL pool 是活跃句柄，正常失败路径（有 ❌ 断言）只设 exitCode 不退出 → 10 分钟 CI 超时。修复：main() 末尾 `process.exit(suiteFailed>0?1:0)`（异常路径同样处理，放 try/catch/finally 之外避免吞堆栈）
- **后端日志落盘可见性**：干净库模式 spawn 的 stderr 写日志文件，启动/测试失败打印尾部——"为什么红"不再靠猜

## 今天交付的功能

1. **#58（bug）**：后台四 tab 顺序 运营数据/课程设定/排课系统/教练分配 + 默认打开运营数据（FRONT-24）
2. **#D5 收官**：五项（埋点后端/前端埋点/画像收集/生日月 8 折/浏览分析看板）此前已实现，今天补上唯一漏项——**users-analysis 画像筛选**（gender/age_range/birthday_month，纯 JS 内存筛选无 SQL 方言风险）+ web 筛选控件 + EVT-02~05 + PRIVACY.md 明示埋点/画像用途 + DEV-BACKLOG 五项标 [x]
3. **#59（bug → 需求）**：用户报"保存画像后画像卡不再显示"——澄清后是**期望行为**：画像卡改**一次性表单**，保存后整卡隐藏，仅留「🎯 资料与生日 ›」白色圆角卡片入口（用户拍板，同联系客服样式）重新展开编辑（PIPL 更正权）；saveProfile 兜底 `res.profile || this.data.profile` 防响应异常；FRONT-21b 防回退

## 踩坑要点（给未来的自己）

- **MySQL 的"数字"有三种身份**：LIMIT 要整数字面量（不能 DOUBLE 绑定）、SUM 返回 DECIMAL（mysql2 默认 string）、COUNT 返回 number——断言前统一 `Number()` 或 fen() 强转。
- **`MAX(a,b)` 双方言没有公共替代**：SQLite 是标量扩展，MySQL 是 GREATEST（SQLite 又没有）——CASE WHEN 是唯一安全写法。
- **事务必须单连接承载**：双驱动下 exec('BEGIN')/COMMIT 的语义靠驱动实现收敛，业务代码只调 BEGIN/COMMIT，驱动层负责绑定同一连接。
- **测试进程不退出 ≠ 测试挂**：MySQL pool 句柄让事件循环永不空——一切 await 完成后进程仍不退出，CI 表现为超时。凡连接池测试都要显式 process.exit。
- **静态断言防回退有效**：MYSQL-xx 正则扫描 + FRONT-xx WXML 断言，是"运行时测不到的另一条腿"。

## 验证

```
本地 SQLite 干净库：357/357 ✅（含 UTC 时区模式）
coverage 探针：pass ✅
CI：test（SQLite）✅ + test-mysql（MySQL 8）✅ —— test-mysql 上线以来首次全绿，后续连续 4 个 run 稳定全绿
```

## 待办（下一个战役）

- TODO.md「后台管理页重新规划」① 手机端自适应 ③ 课程轮播图上传 ④ 排课 UX 重新设计（② 运营数据 tab 已完成）
- DEV-BACKLOG 已空——新需求需走 design 流程立项
