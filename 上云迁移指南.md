# 上云迁移指南：cpolar 本地 → 微信云托管

> 综合训练馆订课系统 · 迁移规划
> 更新日期：2026-08-15

---

## 一、迁移总览

```
当前（cpolar 本地）                    目标（微信云托管）
┌────────────────────┐               ┌────────────────────┐
│ Node ≥22.5 原生 http│               │ Docker 容器跑 Node  │
│ 手写路由 index.js   │ ── 90% 复用 ──▶│ 同一份 index.js      │
│ SQLite (node:sqlite)│ ── 主要改动 ─▶│ MySQL（云数据库）    │
│ 同步 API            │ ── 异步化 ───▶│ mysql2 异步 API      │
│ jscode2session     │ ── 可选简化 ─▶│ getWXContext() 直取  │
│ cpolar 隧道域名     │ ── 更稳定 ───▶│ 云托管固定 HTTPS 域名 │
│ 手动管理进程        │ ── 免运维 ───▶│ 平台托管+自动备份     │
└────────────────────┘               └────────────────────┘
```

**核心结论**：业务逻辑、API 设计、前端请求层**几乎不用改**；工作量集中在**数据层（SQLite→MySQL）**。

---

## 二、技术栈差异对照

| 层次 | 现在（cpolar） | 目标（云托管） | 改动量 |
|---|---|---|---|
| 运行时 | Node ≥22.5 | Node（Docker 镜像） | 无 |
| HTTP | 原生 http | 原生 http（容器内） | 无 |
| 数据库 | SQLite / node:sqlite 同步 | MySQL / mysql2 异步 | **大**（方言+异步） |
| 登录 | jscode2session + .env secret | getWXContext() 或保留现有 | 小 |
| 域名 | cpolar 隧道（会变） | 云托管固定域名 | 小（改 api.js） |
| 部署 | bat 脚本 + 手动 | 容器镜像 + 平台 | 无（新工作） |
| 测试 | minitest 本地 | minitest 指向云托管地址 | 小 |

---

## 三、数据层迁移（主要工作，80% 工作量）

### 3.1 现状盘点（已扫描）

| SQLite 专有用法 | 数量 | 迁移影响 |
|---|---|---|
| `datetime('now','localtime')` 等时间函数 | **41 处** | MySQL 用 `NOW()`，需逐个替换 |
| `last_insert_rowid()` | **8 处** | MySQL 用 `connection.insertId` |
| `LIMIT ?` 占位符 | 3 处 | 语法兼容（MySQL 支持），仅类型差异 |
| `AUTOINCREMENT` | 18 表 | MySQL 用 `AUTO_INCREMENT`，DDL 重写 |
| `INSERT OR IGNORE/REPLACE` | 2 处 | MySQL 用 `INSERT IGNORE` / `ON DUPLICATE KEY` |
| `PRAGMA`（WAL/外键） | 4 处 | MySQL 不需要（引擎默认），删除 |
| `.prepare()` 同步调用 | **约 193 处** | **全部改异步 await**（最大工作量） |

### 3.2 迁移步骤（推荐顺序）

```
阶段 1：代码准备（现在做，零成本）
  ├─ ① 新建 db/ 模块时保持「数据访问集中」→ 已达标 ✅（db.js 聚合 + 按域拆分）
  ├─ ② 新 SQL 用标准语法（NOW() 风格、避免 SQLite 专有函数）
  └─ ③ 同步 API 集中在 db 层，业务层不直接碰 db

阶段 2：数据库迁移
  ├─ ① 用工具导出：sqlite3 gym.db .dump > gym.sql（或用 Node 脚本逐表导出）
  ├─ ② 在云数据库 MySQL 建库，DDL 转换：
  │     INTEGER PRIMARY KEY AUTOINCREMENT → INT AUTO_INCREMENT PRIMARY KEY
  │     TEXT → VARCHAR(255) / TEXT（按字段）
  │     datetime('now','localtime') → NOW()
  │     UNIQUE(user_openid, session_id) → 同语法 ✅
  ├─ ③ 数据导入（分批 INSERT，或 csv 导入）
  └─ ④ 校验行数一致（18 表逐表 count）

阶段 3：db 层异步化（核心工作量）
  ├─ ① 引入 mysql2/promise
  ├─ ② 把 db-core.js 的 DatabaseSync 换成 mysql2 连接池
  ├─ ③ 所有 .prepare().get()/all()/run() 改为 await 异步
  │     同步：const u = db.prepare(...).get(openid)
  │     异步：const [rows] = await pool.query(...); const u = rows[0]
  ├─ ④ last_insert_rowid() → result.insertId
  ├─ ⑤ 事务 BEGIN/COMMIT → START TRANSACTION / COMMIT（或 pool.getConnection 事务）
  └─ ⑥ 逐模块测试（users → courses → bookings → orders → passes → coin...）

阶段 4：业务层适配
  ├─ ① 业务函数加 async/await（调用 db 的地方）
  ├─ ② 路由 handler 支持 Promise（index.js 的 sendJson 调用链）
  └─ ③ 错误处理：SQLite 的 ERR_SQLITE_ERROR → MySQL 的错误对象

阶段 5：部署切换
  ├─ ① api.js FALLBACK_BASE_URL → 云托管域名
  ├─ ② 后台 request 合法域名 → 云托管域名（配一次）
  ├─ ③ 登录：可选换 getWXContext()（简化）或保留 code2Session
  └─ ④ minitest 指向云托管地址跑全量回归
```

### 3.3 迁移成本估算

| 项 | 估算 |
|---|---|
| 表 DDL 转换 | 18 表，约 1 小时 |
| db 层异步化 | 约 193 处 prepare，**约 2-4 天** |
| 业务层 async 适配 | 约 1 天 |
| 测试回归 | 半天 |
| **合计** | **约 3-5 天**（一次性） |

---

## 四、登录迁移（可选简化）

| 方案 | 说明 | 推荐度 |
|---|---|---|
| **A. 保留现有 code2Session** | 代码不动，secret 存云托管环境变量 | ✅ 改动最小 |
| **B. 改用 getWXContext()** | 云托管注入 `X-WX-OPENID` 头，直接读，免 secret | ✅ 更省心 |
| C. 云开发云函数 | 前端 wx.cloud.callFunction，不用域名 | 架构变化大，不推荐 |

> **建议**：先用 A（代码零改动）跑通，后续优化时切 B。

---

## 五、前端改动清单（很小）

| 文件 | 改什么 |
|---|---|
| `miniprogram/utils/api.js` | `FALLBACK_BASE_URL` → 云托管域名（唯一改动） |
| 后台 | request 合法域名 → 云托管域名（配一次） |
| 其他 | **无**（wx.request 用法不变） |

---

## 六、迁移前后对比

| 指标 | cpolar 本地 | 微信云托管 |
|---|---|---|
| 可用性 | 电脑开着才有 | 7×24 在线 |
| 域名稳定性 | 免费版会变 | 固定 |
| 成本 | 免费/99元年 | 按量计费（低流量近乎免费） |
| 数据安全 | 本地文件 | 云端自动备份 |
| 运维 | 手动 | 免运维 |
| 微信集成 | 手动对接 | 内置（登录/支付） |

---

## 七、决策建议

1. **现在**：保持 SQLite + cpolar（免费够用），写新 SQL 用标准语法
2. **朋友测试稳定化**：上 cpolar 固定域名（99 元/年，见《cpolar 固定域名指南》）
3. **正式上线**：迁微信云托管 + MySQL（按本指南阶段 2-5）
4. **可选加分**：迁移时把 index.js 的 172 处 if-else 路由表化（顺手解决代码质量短板）

---

## 附：SQLite → MySQL 常用语法对照

| SQLite | MySQL |
|---|---|
| `datetime('now','localtime')` | `NOW()` |
| `date('now')` | `CURDATE()` |
| `last_insert_rowid()` | `conn.insertId`（驱动返回） |
| `LIMIT ? OFFSET ?` | 同语法 |
| `INTEGER PRIMARY KEY AUTOINCREMENT` | `INT AUTO_INCREMENT PRIMARY KEY` |
| `INSERT OR IGNORE` | `INSERT IGNORE` |
| `INSERT OR REPLACE` | `REPLACE INTO`（或 ON DUPLICATE KEY UPDATE）|
| `PRAGMA foreign_keys = ON` | 引擎默认（InnoDB）|
| `TEXT UNIQUE` | `VARCHAR(n) UNIQUE` |
| `||` 拼接（若有） | `CONCAT(a, b)` |
| 布尔（0/1） | TINYINT(1) |
| `strftime('%Y-%m-%d', ...)` | `DATE_FORMAT(...)` |
