/**
 * 能量币系统配置（唯一数据源）
 * ============================================================
 * 修改本文件数值后，后端自动生效（前端通过 /api/coin/config 获取展示）。
 * 修改后重启后端即可。
 * ============================================================
 */

module.exports = {
  // 行为 → 能量币（一次性获取）
  earnRules: {
    checkin:     10,     // 每次签到（到店核销）
    attendClass: 20,     // 完成一节课（签到成功时额外发放）
    invite:      100,    // 每成功邀请 1 人（叠加储值奖励）
    recharge:    50,     // 每充值 ¥100 → 50 币（按金额折算，向下取整）
    levelUp:     200,    // 每次升级 → 200 币
    achievement: 50      // 每解锁 1 项成就 → 50 币（成就系统 REWARD_COINS 同源）
  },

  // 每日获取上限（防刷币，0 = 不限制；2026-08-14 用户决定取消上限）
  dailyLimit: 0,

  // 能量币有效期（0 = 永久有效；N = N 个月后清零，暂仅记录不强制过期）
  expireMonths: 0
};
