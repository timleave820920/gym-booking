# DESIGN #D3 · 排位人数可视化设计方案

> 2026-08-18 确认（用户拍板：区分状态显示 / 三处展示 / onShow 自动刷新）
> 设计流程：需求澄清（AskUserQuestion）→ 本设计文档 → DEV-BACKLOG 登记 → 待「开始dev」

## 一、需求

1. 用户看到满员课程时，显示当前有几个人在排位——帮助判断要不要付费排位
2. 已在排位的课程，每次刷新页面看到最新的排位人数

## 二、现状

| 位置 | 现状 | 缺口 |
|---|---|---|
| 后端详情接口 `GET /api/sessions/:id` | 已返回 `waitlisted_by_me`（我是否已排位） | **无排队人数** |
| 前端详情页 | 「排位中」状态位已有（WXML:109） | 无人数文案 |
| 首页课程卡片 | 金色「满员」斜条（WXML:63-64） | 无人数 |
| 我的课程页 | 排位中的课有「排位中」状态 | 无人数、无我的位置 |

候补是**付费排位**（转正或退款，BUG 工作流 B3 已实现），人数直接决定「要不要付钱排队」的决策——这正是本需求的价值点。

## 三、界面设计

### 1. 课程详情页（student-course-detail）

- **满员 + 未排位**：「排位」按钮旁副文案 **「当前 N 人排队中」**（小字灰）
- **已排位**：「排位中」状态位改为 **「排位中 · 您前面还有 N 人」**
- 页面每次切回（onShow）自动拉最新数据

### 2. 首页课程卡片（student-courses）

- 满员卡片：金色「满员」斜条下加 **「N 人排队中」** 小字角标
- 进入页面/切回即刷新（列表 onShow 已有）

### 3. 我的课程页（student-my-courses）

- 排位中的课：状态显示 **「排队中 · 前面还有 N 人」**
- onShow 自动刷新

## 四、程序架构

### 后端（3 个接口加字段，无新接口）

1. **`GET /api/sessions/:id`（handleSessionDetail）**
   - 加 `waitlist_count`：`COUNT(waitlist WHERE session_id=? AND status='waiting')`
   - 携带 openid 时加 `my_wait_position`：排在我前面的人数 = `COUNT(... AND created_at < 我的 created_at)`（同场次同状态）
   - 一次查询补 2 个 COUNT，无结构改动

2. **`GET /api/sessions?date=`（listSessionsByDateForUser）**
   - 每场次加 `waitlist_count`：`GROUP BY session_id` 一条聚合 SQL 一次查出全部场次人数（**禁止 N+1 循环查询**）

3. **我的课程页接口（排位中的课列表）**
   - 每项带 `waitlist_count` + `my_wait_position`

### 前端（3 个页面）

- 详情页：按钮副文案 + 状态位文案 + onShow 刷新
- 列表页：满员卡片加人数小字（数据由接口带出，onShow 已有）
- 我的课程页：排位中课显示人数 + onShow 刷新（#35 已确立 onShow 刷新先例）

## 五、数据口径

- **人数** = `status='waiting'` 的排位数（不含已转正/已退款/已退出）
- **我的位置** = 同场次 `created_at` 比我早的 waiting 人数（即「前面还有 N 人」）
- `UNIQUE(user_openid, session_id)` 保证同一用户不会重复计数
- **实时性**：每次 onShow 拉最新，无缓存、无轮询

## 六、测试计划

| 层级 | 用例 |
|---|---|
| run-tests.js（WAITLIST 域） | 排队人数正确；我的位置正确；转正后人数 -1；退出/退款后人数 -1 |
| coverage.test.js | 新字段探针 1 行 |
| 前端静态断言（FRONT-xx） | 三处展示节点存在（防回退） |

## 七、明确不做（防过度设计）

- ❌ 不做实时推送/轮询（onShow 刷新已满足「每次刷新看最新」）
- ❌ 不做「预计等待时间」估算（数据量不足以预测）
- ❌ 不做排位按钮上的「转正概率」提示
