# GLOSSARY.md · 项目术语库

> **给谁看**：新加入项目的开发者 / 非技术成员 / 想搞懂这个项目的任何人。
> **怎么读**：每条 = 「一句话人话定义」+ 「为什么对这个项目重要」。按领域分组，按需查阅。
> **原则**：说人话，不堆黑话。看完能读懂项目文档（CONVENTIONS / BUG-LEDGER / DEFINITION-OF-DONE）就算达标。

---

## 一、测试与质量（本项目最重视的部分）

| 术语 | 说人话的定义 | 对本项目的意义 |
|---|---|---|
| **自动化测试** | 用脚本代替人肉点点点，自动检查功能对不对 | 我们的 81 项测试就是「机器版验收员」，每次改代码后自动把关 |
| **断言（assert）** | 测试里的一句「我断定这里应该等于 X」，不对就报红 | 每一条 `check('MEM-03', ...)` 都是一条断言，是防回归的最小单元 |
| **回归测试** | 修完 bug 后，专门验证「这个 bug 不会死灰复燃」的测试 | 每个真 bug 必修一条回归断言（规矩 #7），它是 bug 的「墓碑」，再犯立刻红 |
| **干净库模式** | 跑测试时用一个**全新的空数据库**，测完就删，不碰日常使用的开发库 | 历史旧表会「掩盖」schema 类 bug（本地测不出、云端才爆）；干净库让每次测试都从最新表结构开始，这类 bug 本地就能抓 |
| **schema 类 bug** | 数据库表结构（字段/约束）和代码写的不一致导致的 bug | 本项目的 3 个真 bug 之一就是它：表规定 `session_id` 不能为空，充值却写空值 → 干净库当场崩 |
| **覆盖率（coverage）** | 测试「走过」了代码的百分之多少 | 我们用它发现「哪些代码没人测」——基线 59%，db.js 达 100% 行覆盖 |
| **覆盖率探针** | 一条走遍核心链路的「巡逻测试」，顺带摸清覆盖率 | `coverage.test.js`，CI 里自动跑，是覆盖率的度量尺 |
| **测试闸门** | 一道「测试不过就不让过」的关卡 | 我们的闸门有两道：L1 本地提交时、L2 云端推送后，任何一道红了就拦住 |
| **三层闸门** | 三道不同位置的质检关卡：本地 hook → 云端 CI → 发布前真机 | L1 管「提交」、L2 管「推送」、L3 管「上线」，越往后代价越高，所以越早拦越好 |
| **pre-commit hook** | Git 自带的「提交前自动执行脚本」功能 | 我们用它强制「改代码先测、红不提交」——不靠自觉，靠机制 |
| **负向验证** | 故意搞坏一个东西，确认闸门真的会拦 | 我们把 `session_id` 临时改回 NOT NULL → 干净库模式当场红 → 证明「本地就能抓 schema bug」不是嘴上说说 |
| **冒烟测试** | 大改后先快速跑一遍主干流程，确认「没冒烟」再深入 | 干净库模式上线后先跑 81 项确认基本盘没坏，再往下做 |
| **退出码（exit code）** | 脚本结束时的「成绩单」：0=成功，非 0=失败 | 规矩 #6：验证必须看完整输出+退出码——探针失败曾被 grep 过滤掩盖 1 个月，教训深刻 |
| **幂等（idempotent）** | 同一操作执行多少次，结果都一样 | seed 脚本幂等（有数据就跳过）；支付幂等（重复调用不重复扣款）——防止重试搞出脏数据 |
| **E2E / 端到端测试** | 从用户操作到数据库落库的完整链路测试 | 我们的 run-tests.js 就是 E2E：注册→下单→支付→签到全链路走 HTTP 真实接口 |

## 二、数据库

| 术语 | 说人话的定义 | 对本项目的意义 |
|---|---|---|
| **SQLite** | 一个「文件即数据库」的轻量数据库，不需要装服务器 | 本项目后端唯一存储，一个 `.db` 文件搞定，适合小程序阶段 |
| **WAL 模式** | 一种写入方式：先记「草稿本」，再落正本，读写不互相堵 | 我们开了 `PRAGMA journal_mode = WAL`，允许并发读写，避免卡顿 |
| **Schema（表结构）** | 表的「图纸」：有哪些列、什么类型、能不能为空 | 图纸和代码不一致就是 schema 类 bug——干净库模式就是为抓它而生 |
| **NOT NULL 约束** | 表的规矩：「这一列不准为空」 | `orders.session_id` 曾经 NOT NULL 但充值写空值 → 撞墙。现在改可空并留注释 |
| **迁移（migration）** | 表结构变老用户升级的办法 | 我们用 `ALTER TABLE ... ADD COLUMN` 给旧表加新列（try/catch 幂等），新老库都能跑 |
| **种子数据（seed）** | 空库首次启动时预填的基础数据（教练/场地/课程） | CI 干净环境先 `node server/seed.js` 再跑测试，保证测试有地基 |
| **事务（transaction）** | 「要么全成功、要么全失败」的一组操作 | 下单+扣余位必须同时成功/失败，否则会出现「钱扣了位没占」的脏数据 |
| **防超卖** | 库存 1 个，两个人同时抢，只放行一个人 | 订课事务 + 余位检查双保险，并发测试 SEC-04 专门验证 |
| **参数化查询（prepared statement）** | 先把 SQL 模板发给数据库，再传参数，不拼接字符串 | 防 SQL 注入的行业标准做法——我们 154 处全用，这是代码体检的强项 |
| **外键（foreign key）** | 表与表之间的「户口关联」：这行的 id 必须存在于另一张表 | `orders.session_id → course_sessions.id`，保证订单不会指向不存在的场次 |

## 三、架构

| 术语 | 说人话的定义 | 对本项目的意义 |
|---|---|---|
| **巨石文件（monolith）** | 所有代码挤在一个文件里，越写越难改 | `db.js` 曾是 1817 行的巨石——这就是「规模化问题」的教科书案例 |
| **按域拆分（domain modules）** | 按业务领域（用户/订单/课程…）把代码分文件 | db.js 拆成 8 个域模块 + 106 行聚合入口，从此新逻辑加在对应文件即可 |
| **聚合入口** | 一个「只负责导出，不写业务」的薄文件 | `db.js` 现在只剩 require + 导出，别的文件 `require('./db')` 用法不变——对外零破坏 |
| **路由表（route table）** | 把「网址 → 处理函数」的对应关系写成一个表 | `API_ROUTES` 54 条声明式路由替代 40+ 个 if-else，新接口=表里加一行 |
| **循环依赖** | A 文件 require B，B 又 require A，互相咬住 | 拆模块时的经典坑；解法：函数体内 `dbMod.xxx` 惰性访问，不顶层解构 |
| **跨域引用** | 一个域（如订单）用到另一个域（如用户）的函数 | 拆分后必须显式 require——「拆完必做静态审计」就是这个意思 |
| **惰性访问（lazy access）** | 用到哪个函数时再拿，加载时不提前解构 | 避免循环依赖的死锁，是拆分大文件的保命技巧 |

## 四、Git 与 CI

| 术语 | 说人话的定义 | 对本项目的意义 |
|---|---|---|
| **Git 提交（commit）** | 给当前代码改动拍一张「存档照」 | 我们的纪律：每轮功能一个独立提交，回滚/追溯全靠它 |
| **分支（branch）** | 代码的平行宇宙 | 本项目主要在 master 直推，靠三层闸门保安全 |
| **推送（push）** | 把本地存档同步到云端仓库 | push 是 L2 CI 的扳机——一推送，云端自动开始质检 |
| **CI / 持续集成** | 代码一进仓库就自动跑测试的机制 | 我们用 GitHub Actions，每次 push 自动：建干净库 → seed → 81 项测试 → 覆盖率 → 报告 |
| **GitHub Actions** | GitHub 自带的「云端自动化流水线」 | 项目里 `.github/workflows/ci.yml` 就是它的剧本（workflow） |
| **Workflow（工作流）** | 自动化流水线的剧本文件 | 定义了「拉代码→装 Node→seed→起服务→跑测试」每一步干什么 |
| **干净环境** | 一个啥都没有的全新运行环境（没有历史数据） | 本地旧库会掩盖 bug，云端干净环境专治「本地测不出」 |
| **回滚（rollback）** | 代码出问题后退回上一个存档 | 双主题系统整版回滚先例：大改动不满意直接 `git checkout` 回到 a5faa63 |
| **LF / CRLF（换行符）** | 文本里表示「换行」的两种编码方式 | Windows 会把脚本转成 CRLF 导致 sh 执行报错 → 我们加 `.gitattributes` 强制 LF |
| **.gitattributes** | 告诉 Git「这个文件该怎么处理」的说明书 | 防止换行符/编码被 Git 乱改，脚本类文件强制 `eol=lf` |

## 五、后端与接口

| 术语 | 说人话的定义 | 对本项目的意义 |
|---|---|---|
| **API / 接口** | 前端问后端要数据/办事的「服务窗口」 | 本项目所有页面数据都走 `/api/xxx` 接口，前后端以 JSON 对话 |
| **路由（route）** | 「这个网址该找谁处理」的指路牌 | 54 条路由在 `API_ROUTES` 表里一目了然 |
| **openid** | 微信给每个用户的「终身身份证号」 | 用户唯一标识，所有业务（订单/余额/课程）都挂它名下 |
| **jscode2session** | 用微信登录码换 openid 的官方接口 | 正式上线后接它拿真实身份；现在演示阶段固定 `demo_user` |
| **模拟支付 vs 微信支付** | 假支付（点一下就成功）vs 真实微信收钱 | 现在用模拟支付跑通全流程；正式支付需注册正式小程序+商户号 |
| **状态机（状态流转）** | 一个东西只能按固定顺序变状态 | 订单：pending(待付)→paid(已付)→refunded(已退)，不能乱跳，保证钱和数据对得上 |
| **回调（callback）** | 微信支付成功后主动通知后端「钱到了」 | 正式支付接入时的防掉单机制，现在模拟支付不需要 |
| **候补转正** | 满员课有人退订，排在最前面的候补自动补位 | waitlist 表 + 自动任务，本项目复杂业务逻辑之一 |

## 六、上线与部署

| 术语 | 说人话的定义 | 对本项目的意义 |
|---|---|---|
| **正式小程序 / 测试号** | 真上架的 AppID vs 微信开发者工具里的练习号 | 测试号导致云开发/支付/真实手机号全被禁用——这是所有上云的前置条件 |
| **云开发** | 腾讯提供的云端后端托管（数据库/函数/存储） | 代码已就绪（`USE_CLOUD` 开关），等正式 AppID 后部署 |
| **合法域名 / HTTPS** | 微信只允许小程序访问已备案的 https 域名 | 真机环境必须配置，否则接口全被拦截 |
| **局域网 IP 自动适配** | 后端启动时自动探测本机 IP 写进配置，前端动态读取 | 解决「换个网络 IP 变了前端就连不上」的经典痛点 |
| **DoD（Definition of Done）** | 一张「做到什么程度才算完成」的验收清单 | `DEFINITION-OF-DONE.md`：范围/可靠性/体验/证明四柱，上线前逐项打勾 |

## 七、协作与流程（这个项目特有的「规矩文化」）

| 术语 | 说人话的定义 | 对本项目的意义 |
|---|---|---|
| **开发契约（CONVENTIONS）** | 和 AI 协作开发时「不能打破的规矩清单」 | `CONVENTIONS.md`，7 条强制规矩全部由机制保证，不靠口头自觉 |
| **双保险（bug 修复流程）** | 修 bug 必须同时做：修复代码 + 回归测试 + 台账登记 | 规矩 #7——保证每个 bug 教训不丢、再犯必红 |
| **缺陷台账（BUG-LEDGER）** | 记录每个 bug「现象/根因/修复/回归测试/防护层」的账本 | 新成员翻台账 = 快速学习这个项目踩过的坑 |
| **断言破坏必须告知** | 测试被临时改坏时，必须明说并恢复 | 规矩 #5——信任基石，防止「测试看着绿其实被动手脚」 |
| **四重保证机制** | 怕忘事，用 4 个地方互相备份提醒 | TODO 状态机 + CONVENTIONS + 项目记忆 + 每周日自动检查任务 |
| **操作日志（logOp）** | 钱相关的关键操作都留痕 | 支付/充值/退款/兑换/签到写入 `ops.log`，用户投诉时可回溯（DoD 可追溯性要求） |
| **安全重构（safe-refactor）** | 拆大文件时的防踩坑流程 | 拆 db.js 3 轮实战沉淀成 skill：先查依赖全景、锚点替换、拆后静态审计 |

## 八、环境坑速查（Windows 实测）

| 坑 | 现象 | 应对 |
|---|---|---|
| **杀不掉 node 进程** | `taskkill //PID x //F` 杀不死后端，端口 3000 还被旧代码占用；重启后测试连到的仍是旧代码（表现：改了代码不生效） | 用 PowerShell：`Stop-Process -Id <pid> -Force`；或 `netstat -ano \| findstr :3000` 先找 pid。**根治**：pre-commit hook 已用干净库模式（独立端口 3100+），不再依赖 3000 |
| **改了代码测试还失败** | 明明修复了，重跑还是旧错误 | 先确认后端进程是修复后启动的（看启动日志时间戳）；重启后端再测 |
| **测试残留脏数据** | 失败运行留下的 `uid_test_`/`uid_cov_`/`uid_dbg_` 数据导致下次测试外键报错 | 用干净库模式跑（自动清理）；调试脚本用完必须清理自己造的数据 |
| **CRLF 换行符** | Windows 把脚本转成 CRLF，sh 执行报 `\r` 错误 | `.gitattributes` 已强制 `eol=lf`（.githooks/pre-commit、*.sh） |

---

*创建：2026-08-13。随项目发展持续补充。新增术语时保持「一句话人话 + 项目意义」两段式。*
