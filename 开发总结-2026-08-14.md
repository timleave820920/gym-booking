# 综合训练馆订课系统 · 开发总结（2026-08-14）

> 今日主题：**拉朋友真机测试冲刺** —— 公网穿透、真机性能优化、测试号体系、社交感功能（预约墙同堂次数）、以及一次 git 仓库灾难性损坏。
> 提交 10 个（33d47d6 → 28b5e05，已推送至 af69022；其后 7 个提交因 Git 仓库损坏待恢复，详见文末 ⚠️）。
> 详细逐轮记录见 `.workbuddy/memory/2026-08-14.md`。

---

## 一、今日成果总览

| 领域 | 成果 |
|---|---|
| 测试账号体系 | 3 个测试号重建（喻馥雅=教练 / 蚂蚁 / 艳子，openid=昵称 djb2 哈希，前后端一致）；登录页「邀请码」改「昵称快捷登录」，命中即切对应账号，微信/手机号登录一律田立（demo_user） |
| 公网穿透 | Cloudflare Quick Tunnel → **cpolar 国内节点**（延迟 0.26~0.30s，优于 Cloudflare 1.3s 且稳定）；`server/start-cpolar-tunnel.bat` 一键起隧道 |
| 真机性能 | 首屏秒开：场次列表本地缓存（10min TTL，先渲染后刷新）+ 骨架屏替代白屏；地址源单一配置化 |
| 测试号 | 切换测试号 AppID（wxd63f6b720b3021ea），任何微信可扫预览码，绕开「项目成员」权限限制 |
| 产品迭代 | 次卡包档位改名（月卡→12次包、双月卡→24次包）；已预订课程可点进详情；预约墙头像下显示昵称；**预约墙同堂次数小圆标**；同堂说明小字；我的课程卡片可进详情 + 详情页「排位中」按钮态 |
| 测试基建 | 修复测试时间耦合缺陷（22:00 场次夜间跑必挂）；新增假用户测试数据体系（seed-fake-users.js） |
| 文档 | 本开发总结、BUG-LEDGER 3 条、ENVIRONMENTS 公网模式、TODO 更新 |

---

## 二、功能实现清单

### 1. 测试账号体系（bf5b680 / c0621b8）
- 按昵称哈希生成稳定 openid（`demo_<hash36>`），前后端算法一致
- 喻馥雅登录自动进教练端（coach_id=1 绑定档案）；蚂蚁/艳子进预约页
- 登录页：昵称输入实时匹配测试账号 → 弹确认 → 一键登录；邀请绑定改从分享卡片 `pending_inviter` 读取

### 2. 公网穿透（cpolar，af69022 + 两次重连）
- 环境：本机已装 cloudflared；cpolar 3.3.18 手动安装（C:\Program Files\cpolar\）
- 命令：`cpolar http 3000 -log stdout -log-level INFO`（默认 -log none 无输出，必须加参数）
- 免费版域名重启会变：`server/start-cpolar-tunnel.bat` 一键启动并提示同步前端

### 3. 真机性能优化（66e0e79）
- `utils/session-cache.js`：按日期缓存原始 sessions，TTL 10min
- 预约页/活动页：有缓存先渲染（秒开）→ 后台刷新替换；无缓存显示骨架屏
- 刷新失败且已有数据 → 保持展示（不闪回 mock）
- 缓存存原始数据，渲染时重新 decorate（席位/状态实时）

### 4. 预约墙同堂次数（28b5e05 + 今日追加）
- 后端 `listBookedUsersWithInfo(sessionId, viewerOpenid)`：一次 SQL 批量算「共同 booked 场次数」（含当前场）
- 前端头像右下角荧光绿小圆标（#C6F04B + 黑描边）：>9 显示 `...`，0 也显示
- 假用户（fake_ 前缀）返回确定性伪随机 0-5（同场稳定、跨场不同），配 seed-fake-users.js 造数

### 5. 体验细节（dd4fa4e / 3131859 / 本轮）
- 已预订课程点击课程条可进详情（预约页/活动页放开拦截；进行中/已结束未预订仍不可点）
- 详情页底部按钮状态机：已预订 → 「已预订」；排位中 → 「排位中」（新）；其余按时间状态
- 预约墙昵称：登录补全（handleLogin 空昵称自动写）+ 前端兜底「微信用户」
- 我的课程页卡片可点击进详情（按钮 catchtap 防冒泡）

---

## 三、踩过的坑（技术教训）

| # | 坑 | 现象 | 根因与解法 |
|---|---|---|---|
| 1 | Cloudflare Quick Tunnel 不自愈 | 控制流断后进程僵死，URL 永久失效 | 无自动重连换址，只能手动杀进程重启；弃用换 cpolar |
| 2 | cpolar 默认无日志 | 起隧道看不到 URL | `-log stdout -log-level INFO` |
| 3 | cpolar 服务抢 token 时机 | 服务在 token 写入前启动 → 一直 `authToken is null` | 杀进程重启后读到配置 |
| 4 | **net-config.json 被并行实例覆盖**（致命） | 真机登录报「本地服务器没有启动」，公网却 200 | 并行后端（PORT=3536/3596）启动时把 net-config 写成局域网错误地址，前端优先读它。**根治：废弃 net-config 作为前端地址源，地址唯一由 api.js FALLBACK 人工配置**（fb451d8） |
| 5 | 预览权限迷宫 | 新微信扫预览码提示无权限 | 预览码只有「项目成员」可扫，体验成员不行；后台成员管理仅管理员可见。**方案：测试号 AppID，任何微信可扫** |
| 6 | 真机域名校验 | 预览包请求被拦 | 真机需右上角「···」→「打开调试」（仅勾选工具设置不够） |
| 7 | GitHub 网络间歇失败 | push/fetch 443 reset/超时 | 国内网络常态；提交本地安全，网络恢复后补推 |
| 8 | **测试时间耦合** | 22:00 后跑测试必挂（WTL-06 转正失败） | 测试满员场次定在当天 22:00，`refundExpiredWaitlist` 在 GET /api/waitlist 时把已开课场次候补自动退款。修复：21 点后满员场次改用明天日期（run-tests.js） |
| 9 | **git 仓库灾难性损坏**（进行中） | git 报 not a git repository；objects/pack 丢失 | 疑似并行会话 gc 中断 + refs 被删。本地 66e0e79 之后 7 个提交历史丢失（**工作区代码零丢失**）；GitHub 有 af69022 及之前历史，网络恢复后 fetch 重建（详见文末） |

---

## 四、做好的设计

1. **前端秒开缓存体系**：缓存 = 原始数据 + 渲染时重新 decorate（状态实时），TTL 10min 自动过期，刷新失败保持展示不闪 mock —— 兼顾「快」与「准」
2. **地址源单一配置原则**：运行时产物（net-config.json）不可作为配置源（会被并行实例互相覆盖）；地址唯一由 api.js 人工配置，局域网/公网切换只改一处
3. **假用户测试数据体系**：`server/seed-fake-users.js` 幂等脚本（清 fake_% → 随机 3-5 名 → 订 demo_user 全部场次）；后端对 fake_ 前缀返回**确定性伪随机** coCount（同场稳定、跨场不同），真实用户按实际计算 —— 测试 UI 数字样式又不污染真实统计
4. **登录号 ↔ AppID 匹配规则**：工具登录微信必须是当前项目 appid 的开发者；测试号模式用申请测试号的微信登录工具，真实号模式用田立主号

---

## 五、环境与协作状态（⚠️ 重要）

- **公网测试链路（当前有效）**：cpolar 隧道 `https://35d0ecae.r22.cpolar.top` → 本地 3000 → SQLite；前端地址源 = api.js FALLBACK（唯一）
- **测试号**：AppID `wxd63f6b720b3021ea`（project.config.json）；secret 用户持有未入库；测试号不能上传发布
- **假用户**：陈语嫣/吴雨桐/王一诺/郑凯文（92 条订课），可 seed-fake-users.js 重置
- **并行会话**：今日多次检测到并行实例（3536/3596 端口、git 操作），已造成 net-config 覆盖与 git 损坏——**强烈建议后续单会话操作本仓库**

### ⚠️ git 仓库损坏与恢复计划
- **现状**：`.git/objects/pack/*.pack` 丢失（只剩 .idx），`.git/refs` 被删；本地 66e0e79 之后 7 个提交（af69022/8f2c643/fb451d8/4381ded/dd4fa4e/3131859/28b5e05）的本地对象丢失；**工作区所有文件完好，代码零丢失**
- **远端**：GitHub master = af69022（已验证 ls-remote）
- **恢复步骤（GitHub 网络恢复后执行）**：
  1. 删除损坏元数据：`.git/objects/pack/multi-pack-index + *.idx`、`.git/objects/info/commit-graphs`
  2. `git fetch origin`（全量恢复 af69022 及祖先）
  3. 重建 `refs/heads/master` = af69022
  4. 工作区重新 add + commit（7 个丢失提交内容合并为一个新提交，含今日全部未提交改动）
  5. push
- **当前不可在本地跑 git 命令**（仓库处于损坏态），勿动 .git

---

## 六、明日建议

1. **GitHub 网络恢复后立即执行 git 恢复**（上文步骤），补交今日全部改动
2. 填 `server/.env` 的 WX_SECRET（测试号）→ 重启后端 → 朋友各自独立 openid（并行会话已搭好 .env 加载）
3. 拉朋友真机完整走一遍核心链路（订课/候补/签到/次卡/能量币），重点验证同堂次数与昵称展示
4. 规划正式部署（云服务器 + 备案 或 微信云开发），结束临时隧道依赖
