# 限时次卡包 · 需求与规则设计（待审批）

> 状态：**设计稿，待田立审批后开发**
> 关联 TODO：P1｜限时次卡包
> 本文档遵循项目硬性约定：钱闭环（扣↔退对称）、关键操作留痕（logOp）、新接口=测试+探针、钱计算前后端同规则。

---

## 一、一句话需求

销售**有时间限制的次卡包**；订课时支付顺序：**次卡次数 → 储值余额 → 微信支付**；次卡为**单卡累加模式**（重复购买直接叠加次数与有效期）。

## 二、业务规则（v2，已按田立决策更新）

### R1 次卡包档位（数值可配置化，当前两档）
| 档位 | 次数 | 有效期 | 价格 |
|---|---|---|---|
| 月卡 | 12 次 | 60 天 | ¥900 |
| 双月卡 | 24 次 | 120 天 | ¥1800 |

- 档位存 `class_packages` 表（name/total_count/valid_days/price_fen/desc），后台可增改，前端动态读取。
- **每档带使用说明**（desc 字段）：如「60 天内有效，逾期剩余次数作废，可与已有次卡叠加」。

### R2 购买与生效（单卡累加模式 ⭐D8）
- 每个用户逻辑上**只有 1 张次卡**；重复购买**累加次数 + 顺延有效期**：
  - 无有效卡 → 新建：`remaining = 次数`，`expires_at = 购买日 + N 天`；
  - 有有效卡 → 累加：`remaining += 次数`，`expires_at = 原 expires_at + N 天`（示例：当前剩 20 天 → 买 60 天 → 作废期延长为 80 天）；
  - 卡已过期 → 视同无卡，从购买日重新起算。
- 购买走订单体系（`order_type='pass'`）→ 支付（余额/微信）→ 成功即累加发卡。
- **首期不做"购买后退卡"**（D6 确认）。

### R3 使用顺序（订课时）
1. 次卡有剩余且未过期 → **扣 1 次**，实付 ¥0；
2. 无次卡/用完/过期 → 储值余额（会员价）；
3. 余额不足 → 微信支付（原价）。
- 用户不可跳过次卡选余额。

### R4 候补排位也用次卡（⭐D2）
- 候补排位支付顺序与订课一致（**次卡 → 余额 → 微信**）；用次卡排位即**先扣 1 次**；
- 转正 → booking 沿用该支付来源，**不重复扣次**；
- 退出候补 / 过期退款 → 按支付来源**对称退回**：
  - `pass` → 退 1 次（该次回到卡上，有效期不变）；
  - **若退时卡已过期 → 该次作废清理，不退**（D2 确认）；
  - `balance` → 退余额；`wxpay` → 原路退回。

### R5 用次卡不享会员折扣（D5 确认）
- 次卡预付已优惠，扣次不涉金额、无折扣；折扣仅作用于余额支付。

### R6 退订退次（钱闭环）
- `bookings/waitlist.pay_source` 记录 `pass/balance/wxpay`；
- 退订对称：`pass` → 退 1 次（卡已过期 → 次数作废清理）；`balance` → 退余额；`wxpay` → 原路；
- 扣次/退次**幂等**（同一订单/订课只能一次）。

### R7 过期处理（D3 确认）
- 到期未用完次数**作废不退**，购买页与会员卡显著提示「逾期清零」；
- 到期前 3 天站内信提醒（复用消息系统），到期当天发「已过期」通知。

### R8 会员卡展示（⭐D10）
- 我的会员卡页新增**次卡区块**：剩余次数 + 距过期天数（如「剩余 8 次 · 距过期 45 天」）+ 到期日期；无卡显示「暂无次卡，去购买」。

### R9 购买入口（⭐D9）
- **活动中心宫格**新增「次卡包」入口 → 次卡购买页；
- 购买页：两档卡片（次数/有效期/价格/**使用说明**）+ 支付方式 + 「逾期清零」提示。

### R10 消息通知
- 购买到账（累加后剩余 X 次）、扣次订课、退次、过期提醒/过期作废，共 4 类站内信。

## 三、数据模型（v2，单卡累加）

```sql
CREATE TABLE class_packages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL, total_count INTEGER NOT NULL,
  valid_days INTEGER NOT NULL, price_fen INTEGER NOT NULL,
  desc TEXT DEFAULT '', active INTEGER DEFAULT 1,
  created_at TEXT DEFAULT (datetime('now','localtime'))
);

CREATE TABLE user_passes (           -- 每用户最多一条有效卡（累加模式）；过期后新购另起
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_openid TEXT NOT NULL,
  order_id INTEGER NOT NULL,         -- 最近一次购买订单（溯源）
  total_count INTEGER NOT NULL,      -- 当前累计次数
  remaining INTEGER NOT NULL,
  expires_at TEXT NOT NULL,          -- 作废时间（顺延累加）
  status TEXT DEFAULT 'active',      -- active / expired
  created_at TEXT DEFAULT (datetime('now','localtime'))
);

ALTER TABLE bookings ADD COLUMN pay_source TEXT DEFAULT 'wxpay';
ALTER TABLE bookings ADD COLUMN pass_id INTEGER DEFAULT 0;
ALTER TABLE waitlist ADD COLUMN pay_source TEXT DEFAULT 'wxpay';
ALTER TABLE waitlist ADD COLUMN pass_id INTEGER DEFAULT 0;
-- orders.order_type 扩展 'pass'（购买次卡）
```

## 四、接口（新增 4 + 改造 3）

| 接口 | 说明 |
|---|---|
| `GET /api/passes/packages` | 可售档位（含使用说明 desc）|
| `GET /api/passes/my?openid=` | 当前卡：剩余次数 / 到期日 / 距过期天数 / 是否过期 |
| `POST /api/orders`（orderType=pass）| 购买次卡下单（累加发卡在支付回写）|
| `GET /api/passes/available?openid=` | 订课前查可用次卡（前端展示"次卡支付 ¥0 · 剩余 X 次"）|
| `POST /api/bookings` | 订课：次卡优先扣次（改造）|
| `POST /api/waitlist` | 排位：次卡优先扣次（改造）|
| `DELETE /api/bookings|waitlist/:id` | 退订/退出：按 pay_source 对称退（退次/退余额）|

## 五、前端页面

1. 活动中心宫格「**次卡包**」入口（新）；
2. **次卡购买页**：两档卡片（含使用说明）+ 支付方式 + 逾期清零提示（新）；
3. **我的会员卡**：新增次卡区块（剩余 X 次 · 距过期 Y 天）（改造）；
4. 订课/候补支付页：次卡可用时默认「次卡支付 ¥0 · 剩余 X 次」并置灰其他方式。

## 六、防刷与一致性

- 扣次事务内校验 `remaining > 0` 且未过期，并发扣不成负数；
- 扣次/退次幂等；过期判定服务器时钟；
- 关键操作（发卡/扣次/退次）走 `logger.logOp()` 留痕；
- 每新接口 ≥1 断言进 `minitest/run-tests.js` + 1 行探针 `coverage.test.js`；
- 测试覆盖：购买累加、有效期顺延、扣次订课、退订退次、候补扣次/退次、过期作废、余额兜底、并发防负、幂等。

## 七、决策确认表（2026-08-14 田立批复）

| # | 决策 | 结论 |
|---|---|---|
| D1 | 档位定价 | ✅ 12次/60天/¥900、24次/120天/¥1800，仅两档，数值可配置 |
| D2 | 候补用次卡 | ✅ 排位即扣次；退回时卡已过期 → 次数作废清理 |
| D3 | 过期处理 | ✅ 作废不退 |
| D5 | 次卡不享折扣 | ✅ 确认 |
| D6 | 购买后退卡 | ✅ 首期不做 |
| D8 | 单卡累加 | ✅ 重复购买累加次数 + 顺延作废日期（剩20天买60天→80天）|
| D9 | 购买入口+使用说明 | ✅ 活动中心宫格入口，每档带使用说明 |
| D10 | 会员卡展示 | ✅ 会员卡体现剩余次数 + 过期天数 |

---

*设计 v2，2026-08-14 田立批复。批准后开发：数据表 → 购买/累加链路 → 订课/候补扣次 → 对称退次 → 会员卡展示 → 前端页面 → 测试。*
