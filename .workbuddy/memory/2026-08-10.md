# 2026-08-10 工作记录

## 综合训练馆订课系统 - 全链路设计稿（首版完成）

**文件**: Ardot 设计文件「综合训练馆订课系统」(fileId: 713237257834362)

**需求确认（通过提问收集）**:
- 终端：移动端 + 管理后台
- 角色：学员 + 教练 + 管理员（三角色）
- 功能：课程浏览与订课、支付与会员卡、排课与场地管理、签到与数据统计、成就/锻炼记录
- 风格：运动活力
- 品牌名：通用名（综合训练馆 / 训练馆管家）
- 教练端形态：手机端视图
- 交付范围：全链路覆盖

**设计系统**:
- 风格: Kinetic Collage Gradient (运动活力)
- 配色: 奶油底 #F9F4DF + 青柠主色 #B9FF66 + 电光紫 #5B57EB + 藕粉 #F8B7B8 + 暖黑 #1A1A23
- 字体: 中文 Noto Sans SC / 西文 Inter Tight, DM Sans
- 建立 17 个品牌颜色变量 (Brand set)
- 组件库 (01 组件库): 20 个 SVG 图标 + 状态栏 + 学员端胶囊TabBar + 教练端TabBar

**页面清单（19 页）**:
- 学员端 App 10 页 (375×812): 首页、课程列表、课程详情、预订确认、支付、我的课程、签到、成就记录、个人中心、会员卡
- 教练端 App 3 页 (375×812): 今日课表、学员名单、扫码签到（深色）
- 管理后台 Web 6 页 (1440×900): 数据仪表盘、排课管理（周视图）、场地管理、学员管理（表格）、教练管理、营收统计（柱状图+环形图）

**画布布局**: 学员端 x0-2115 y0-872（两行各5页）；教练端 y1744；后台 y2616 起，每页间隔 60px，横排。

## 第二轮修改（用户反馈调整）:
- 首页：隐藏课程分类栏（课程无分类）
- 课程列表：隐藏分类标签；新增周日期选择条（周一到周日，8/10-8/16）；约课按钮显示价格「¥68 约课」
- 课程详情：删除「选择场次」区块；信息卡显示剩余席位「8 / 20」；底部文案改为「单次 ¥68 · 约课后直接支付」
- 删除确认订单页（学员端-04）；详情页立即预订直接进入支付页
- 支付页：新增课程信息卡（课程名/时间/教练/价格），标题改为「确认支付」
- 删除会员卡概念及页面：删除会员卡页（学员端-10）与个人中心会员卡入口；个人中心用户卡改为「累计锻炼 32 节课」
- 后台/教练端同步清理：营收统计「会员卡收入→单次课程收入」、图例重命名；学员管理列「会员等级→学员状态」，等级改活跃/忠实/体验学员；教练端学员名单同步

**当前页面清单（17 页）**:
- 学员端 App 8 页: 首页、课程列表、课程详情、支付、我的课程、签到、成就记录、个人中心
- 教练端 App 3 页、管理后台 Web 6 页（不变）

**待办/可迭代**:
- 用户确认后可补充：登录/注册页、订单详情、退订确认弹窗等
- 可导出 PDF 汇报稿

## 第三轮：微信小程序第一版代码（完成）

**技术栈确认**（通过提问）：原生微信小程序 + JavaScript + 内置 mock 数据 + 全链路 17 页

**代码输出位置**: `综合训练馆订课系统/miniprogram/`（微信开发者工具直接导入根目录即可）

**完成内容**:
- 17 个页面四件套（js/json/wxml/wxss）：学员端 8 + 教练端 3 + 后台 6
- 全局配置：app.json（含自定义 tabBar custom:true）、app.wxss（品牌色）、sitemap、project.config.json
- 自定义胶囊 TabBar 组件（custom-tab-bar/），还原设计稿胶囊样式，4 个 tab 页 onShow 同步选中态
- mock 数据（utils/mock.js）：课程/周日期/我的课程/教练课表/学员名单/后台统计/营收等
- 图片素材 23 张从设计稿导出至 miniprogram/images/
- 图标全部用 SVG data-URI 内联（无 emoji，遵守设计规范）
- README.md 使用说明

**关键交互**:
- 订课链路：课程列表（日期过滤）→ 详情（剩余席位）→ 立即预订 → 直接支付（无确认订单页）
- 支付模拟、签到切换、退订确认均已实现
- 排课周视图、营收柱状图/环形图用 CSS/SVG 实现

**验证**: 全部 JS node --check 通过、JSON 解析通过、WXML 标签配对与 wx:for/wx:key 检查通过

## 第四轮：添加教练端/后台测试入口 + 导航优化

- 个人中心新增「角色入口 · 演示」区块：教练端（今日课表）、管理后台（数据仪表盘）两个入口，测试用
- 后台 6 页互跳由 navigateTo 改为 redirectTo（避免页面栈过深）
- 后台 6 页顶栏加「退出」按钮（exitToStudent → switchTab 回个人中心）
- 教练端今日课表「我的」tab 改为「退出」→ switchTab 回个人中心
- 修复 sed 追加导致的 5 个后台 JS 结构损坏（重新写入）
- 测试路径：个人中心 → 教练端/后台入口；后台页之间可互跳；点「退出」回学员端

**注意**: 微信开发者工具 CLI 服务端口未激活（需登录+设置-安全设置开启服务端口），CLI 自动化不可用；用户手动在工具里操作。本机路径 C:\Program Files (x86)\Tencent\微信web开发者工具，版本 0.54.1。
**更新**: 用户已手动开启服务端口，CLI 可用（端口 35549→实际监听 49933）；preview 成功生成二维码（AppID wx509088154a505409），项目 open 成功。

## 第五轮：后台新增「排课配置」功能

**需求**: 配置未来课程（选类型/教练/时间 + 发布到云端）；学员/教练端只显示本周课程；刷新时间可设定（如每周一几点）

**设计稿**: 新增「后台-07 排课配置」页面（3:30, x9000 y2616, 1440×900）：
- 左侧表单卡：课程类型（燃脂团课/力量训练/瑜伽普拉提/骑行有氧）、教练（阿凯/小满/大壮）、日期（2026-08-17 起 7 天）、时段（10:00-11:00）、场地/容量
- 右侧：本周课表预览卡（3 条带色块）+ 发布卡（深色卡，发布课表按钮 + 刷新设置「每周一 00:00 更新」+ 设置入口）

**小程序代码**:
- 新增 `pages/admin-create-course/`：完整表单（类型/教练/场地/日期 7 天/时段 picker/容量）+ 发布课表（模拟推送 + 成功弹窗）+ 刷新设置（星期 ActionSheet + 时间 picker）
- `admin-schedule` 页：工具行改「配置排课」按钮（跳转新页）+ 顶部刷新设置胶囊按钮（每周X HH:MM，可点击改星期/时间）
- app.json 注册新页面
- 学员/教练端本周过滤逻辑已由既有实现满足（课程列表周日期条 8/10-8/16、教练端当日课表）

**验证**: JS/JSON/WXML 全部通过

## 第六轮：创建 Git 仓库并推送

**仓库**: https://github.com/timleave820920/gym-booking（public，gh CLI 创建）
**本地身份**: timleave820920 / 19054591@qq.com（仓库级配置）
**初始提交**: `8350e35 chore: init 综合训练馆订课系统微信小程序`（109 files, 5244 insertions）
**分支**: master，tracking origin/master

**关键经验**:
- 本机已装 GitHub CLI（gh 2.97.0）且已登录 timleave820920（token 有 repo 权限）
- `gh repo create gym-booking --public --source <dir>` 一步完成建仓+关联 remote
- **HTTPS 推送无输出失败**的坑：git 默认 credential.helper=manager 拿不到 token → 运行 `gh auth setup-git` 后 push 成功
- .gitignore 排除：.workbuddy/、preview-qrcode.*、project.private.config.json、minitest/、node_modules/
- .gitattributes 统一 LF 行尾（Windows 下避免 CRLF 噪音）

**后续**：功能迭代后 `git add -A && git commit -m "..." && git push` 即可。

## 第七轮：实现微信登录功能

**设计稿**: 新增「学员端-00 登录」页（4:2, x-435 y0, 375×812）：
- 品牌区：深色 Logo 徽标（青柠哑铃图标）+「综合训练馆」+ 标语
- 登录表单区：微信一键登录深色大按钮 + 协议勾选（默认勾选）+ 演示分割线 + 三身份卡（学员/教练/管理员，选中深色描边）+ 提示文案
- 踩坑：VERTICAL 容器内子元素绝对定位导致错位 → 装饰圆删除，flex 流式布局修复；emoji 图标违反规范 → 换彩色圆徽章 SVG

**小程序代码**:
- 新增 `pages/login/`：wx.login 获取 code（演示不真正请求后端）→ 保存 token/userInfo 到 storage → 按角色跳转（学员 switchTab 首页 / 教练 redirectTo 课表 / 管理员 redirectTo 后台）
- app.json：登录页设为首页（第 1 个）
- app.js：onLaunch 读取登录态、isLoggedIn()、logout()
- 个人中心新增「退出登录」按钮 → reLaunch 登录页
- 后台 7 页 + 教练端「退出」统一改为清除登录态 + reLaunch 登录页
- 首页用户名从登录态读取（去"同学/教练/管理员"后缀）

**当前页面数**: 19（登录 + 学员8 + 教练3 + 后台7）
**验证**: JS/JSON/WXML 全部通过，预览包 998.7 KB
**注意**: 预览二维码用 `--qr-format image` 生成（见第六轮经验）

## 第八轮：登录页反馈修改（三点）

1. **移除角色选择**：登录页删除学员/教练/管理员三身份卡 + 分割线 + 提示（设计稿 4:35/4:39/4:49 已删），默认学员身份，加说明文案「登录即代表同意以学员身份使用本服务」
2. **授权问询**：点击登录 → `wx.getUserProfile` 系统弹框（询问允许使用头像/昵称/手机号）；拒绝时演示模式弹 showModal 模拟问询「是否允许使用微信号/手机号登录」
3. **真实用户数据**：授权后保存微信真实 avatarUrl/nickName 到 storage + globalData；个人中心头像昵称、首页问候语自动显示真实数据；头像加载失败回退默认（binderror avatarError）；首页昵称超 8 字截断、占位名简化

**验证**: JS 通过、预览 996.7 KB、git 待提交

## 第九轮：手机号快捷登录 + 首页显示微信昵称

1. **手机号快捷登录**：
   - 设计稿登录页：微信按钮下加「或」分隔线 + 白色描边「手机号快捷登录」按钮（浅绿手机图标）
   - 小程序：`<button open-type="getPhoneNumber" bindgetphonenumber>` 官方授权；企业认证小程序直接获 code 换手机号；未认证/拒绝时演示模式弹窗模拟问询；登录后保存脱敏手机号 138****2210
2. **首页问候显示微信昵称**：refreshUser() 移到 onShow（每次显示刷新），授权后「早上好，{微信昵称}」立即生效；占位名（小陈同学/微信用户）才简化为小陈
3. 手机图标用 SVG data-URI（无 emoji），遵循设计规范

**注意**: getPhoneNumber 需企业认证小程序才返回真实手机号；个人版触发系统授权弹框。

## 第十轮：完善资料流程（微信头像昵称填写能力）

**背景**: 用户反馈「手机登录后没显示微信名」——根因是微信平台限制：getPhoneNumber 只返回手机号，不返回昵称；getUserProfile 新版基础库（2.27.1+）只返回"微信用户"默认值。**没有任何接口能静默获取真实微信昵称**。

**方案**: 官方「头像昵称填写能力」：
- 头像：`<button open-type="chooseAvatar" bindchooseavatar>`（用户从微信头像库选择）
- 昵称：`<input type="nickname">`（键盘上方弹出「使用微信昵称」按钮，用户点一下一键填入真实微信昵称）
- 登录完成时若昵称为默认值（微信用户/小陈同学）→ 弹出「完善资料」弹层（finishLogin 判断 needProfile）
- 设计稿：登录页弹层方案（4:61）布局难控已删，改为底部提示文案「首次登录将引导完善微信头像与昵称」；弹层在小程序 WXML 实现（profile-mask/profile-card）

**流程**: 登录（任一方式）→ 昵称默认 → 弹完善资料 → 用户选头像+点「使用微信昵称」→ 完成 → doLogin → 首页显示真实昵称

**验证**: JS/WXML 通过、预览 1002.5 KB

## 第十一轮：SQLite 后端 + 用户注册/登录

**需求**: 创建背后数据库记录已注册用户；首次=注册，第二次=登录。

**技术方案**:
- 数据库: SQLite 通过 Node 内置 `node:sqlite`（`DatabaseSync`，零 npm 依赖，Node 22.5+）
- 后端: Node 原生 http 服务（server/index.js），端口 3000，无框架
- 数据库文件: `server/data/gym.db`（WAL 模式），已 .gitignore 排除
- users 表: openid(唯一) / nickname / avatar / phone / role / total_classes / created_at / last_login_at / login_count

**API**:
- POST /api/auth/login — 注册/登录合一（首次 201 isNewUser:true，再次 200 isNewUser:false，login_count 递增）
- POST /api/auth/profile — 更新资料
- GET /api/users、/api/users/stats、/api/health

**小程序端**:
- 新增 `miniprogram/utils/api.js` 请求封装（BASE_URL=http://127.0.0.1:3000，真机改局域网 IP 192.168.194.11）
- 登录页 doLogin 改造：wx.login 拿 code → 生成本地持久 openid（存 storage，同设备一致）→ 调后端 → isNewUser 区分「注册成功/欢迎回来」
- project.config.json 已 urlCheck:false（开发者工具可请求本地后端）

**验证**: 端到端测试通过（首次注册次数1→二次登录次数2→三次3）；服务重启后数据持久不丢；后端当前运行于 127.0.0.1:3000

**注意**: 真实环境需用 code 调微信 jscode2session 换真 openid（server/index.js handleLogin 有注释）；生产改 HTTPS 域名。

## 第十二轮：后台学员管理接入真实数据库

- `admin-students` 页从 mock 数据改为 `api.getUsers()` 拉取真实注册用户
- 页面新增：数据库连接状态条（绿=已连接共N名 / 红=未连接）、加载中提示、空状态（SVG 图标）
- 表格列改为：学员（头像/昵称/手机号/注册时间）、学员状态（按角色+登录次数推断：教练紫/管理员深/忠实绿/活跃绿/新学员橙）、登录次数、操作
- 头像加载失败回退默认（avatarError）
- 数据库当前 3 名用户：晓琳(159****8807)、测试用户A、阿杰
- 验证：数据映射正确、预览 1006.8 KB

## 第十三轮：微信云开发方案（后端上云）

**背景**: 用户询问后端运行在哪（当前=本机 server/index.js），希望上腾讯云与生产环境一致。确认选**方案A 微信云开发**（CloudBase）。

**已交付**:
- `cloudfunctions/login/` — 注册/登录云函数（`cloud.getWXContext()` 自动拿真实 openid，首次注册/再次登录+1，users 集合）
- `cloudfunctions/users/` — 用户列表云函数（list/stats）
- `miniprogram/utils/api.js` 双模式：`USE_CLOUD` 开关（true=云函数 / false=本地后端），`CLOUD_ENV` 环境ID
- `app.js` 启动时按 USE_CLOUD 初始化 wx.cloud
- `CLOUD_DEPLOY.md` 部署手册（开通环境→建 users 集合→上传云函数→切 USE_CLOUD→验证）
- README 更新双模式说明

**用户待办**: 微信开发者工具开通云开发环境 → 建 users 集合 → 上传两个云函数 → api.js 设 USE_CLOUD=true + CLOUD_ENV → 验证真实 openid 入库

**注意**: 用户云环境状态"不确定"，已留 CLOUD_DEPLOY.md 指引；本地 server 模式不受影响可随时切回。

## 第十四轮：云开发尝试与回退（关键决策记录）

**云环境已开通**: gym-prod-timleave001（用户网页端开通，选云数据库文档型）

**重大发现**: 项目 AppID `wx509088154a505409` 实际是**微信开发者工具的「小程序测试号」**！测试号无法使用云服务：
- 导入项目时「微信云开发」选项灰色不可选
- CLI cloud 命令报 -601059 "测试号不能使用云服务"
- 工具内找不到云开发入口（被测试号隐藏）

**根因**: 需注册正式小程序（个人主体需 18 岁+身份证+新邮箱+手机号，同身份证上限 5 个；**个人主体无法使用微信支付**——订课系统涉及收款，建议未来用个体工商户主体注册）

**用户决策（2026-08-10 14:24）**: 今日先用本地数据库开发（server/index.js + SQLite），待注册好工商信息后注册正式小程序再切云开发。

**当前状态**: USE_CLOUD=false（本地模式），本地库已有 4 名用户（田立/晓琳/测试用户A/阿杰）；云函数代码（cloudfunctions/）已备好待用；CLOUD_DEPLOY.md + CLOUD_GUIDE.md 已入库

**工具故障处理经验**: 工具假死时 taskkill 杀不掉的顽固进程（wechatdevtools.exe）可用 `cli.bat open --project <路径> --port <新端口>` 绕过启动新实例。

## 第十五轮：真机联调与故障处理（收尾）

**本轮问题链**:
1. git 提交失败（index.lock/COMMIT_EDITMSG 无法删除）→ 根因是僵尸进程占用 → 重启电脑 + .NET API 删除锁文件解决
2. 工具卡死/变慢 → 根因是**11 个工具实例堆叠**（重启自启 + 多次 CLI 调用各拉起实例）→ taskkill 全部清理后恢复
3. 误判 WeChatAppEx 为工具进程 → 实际是**微信 PC 版小程序渲染进程**（RadiumWMPF 插件），与工具无关
4. CLI 报 EPERM（User Data/.cli 文件被锁）→ 影响 CLI 生成二维码，**不影响工具界面操作**；工具界面点「预览」可绕过
5. 真机登录报"无法连接本地后端" → 旧预览包（13:40）BASE_URL 还是 127.0.0.1，真机连自己回环地址失败；重新预览（含 192.168.194.11 的新包）即可

**最终状态**:
- 模拟器测试**全部通过**（登录/注册/数据库链路 OK）
- 真机待用户晚点重测（WiFi 连接问题 + 需重新预览新包）
- 代码全部提交推送（HEAD=d9dee12），本地远程同步
- 后端运行中（127.0.0.1:3000），库内 4 用户
- Windows 防火墙 3000 端口入站规则**未创建成功**（需管理员权限，Access denied）——若真机重测仍连不上，需用户管理员运行命令放行

**经验**:
- Windows 下 .git 锁文件删除用 PowerShell .NET API（[System.IO.File]::Delete）比 rm/taskkill 可靠
- 微信 PC 版（xwechat/RadiumWMPF）会生成大量 WeChatAppEx 进程，勿误杀
- CLI 的 .cli 通信文件被锁时，改用工具界面操作

## 第十六轮：数据库管理页（代码完成，待重启后收尾）

**需求**: 本地数据库管理页面——查看所有用户、删除单个账号、一键清空所有用户。

**已完成（代码全部写好，语法验证通过）**:
- 后端 `server/db.js`: 新增 deleteUserById / deleteUserByOpenid / clearUsers（清空后重置 sqlite_sequence）
- 后端 `server/index.js`: 新增 DELETE /api/users?id=xxx（删单个）、DELETE /api/users/clear（清空）
- 小程序 `utils/api.js`: 新增 deleteUser / clearUsers 方法
- 新增页面 `pages/admin-db/`: 数据库信息卡（连接状态/位置/用户数）+ 危险操作区（一键清空，双确认）+ 用户列表（每行删除按钮，确认弹窗）
- app.json 注册 admin-db 页面；后台导航条新增「数据库」标签（8 项）

**阻塞问题（需重启解决）**:
1. git 提交失败（COMMIT_EDITMSG 僵尸锁再次出现，覆盖/删除均 Permission denied）
2. 后端服务无法常驻（bash 后台 & 进程被回收）+ SQLite readonly（WAL 文件被僵尸句柄锁）

**✅ 重启后全部解决（15:48-15:52）**:
- git 锁释放 → 提交推送成功（4455a80 数据库管理 + 459fdad gitignore 清理误提交文件）
- 误提交清理：gym.db.bak / test-write.tmp 已 git rm --cached，.gitignore 增加 *.db.bak/*.tmp
- 后端启动成功（WAL 锁释放），接口全部测试通过:
  - DELETE /api/users?id=2 → 删除成功（4→3人）
  - DELETE /api/users/clear → 清空 5 人，统计归零
  - **ID 重置验证**: 清空后新注册用户 ID=1（sqlite_sequence 重置生效）
- CLI 恢复（.cli 锁解除）→ 预览包生成成功 1017.4 KB
- 数据库当前 0 用户（测试数据已清理，等用户真机/模拟器重新注册）

**遗留**: 后端运行中（127.0.0.1:3000）；数据库管理页面待用户在工具/真机验证 UI

## 第十七轮：演示身份入口（管理员登录）

**背景**: 用户找不到管理员登录入口——之前重构登录页时移除了身份选择。

**实现**: 登录页底部新增「演示身份入口」（分隔线 + 三个快捷入口）：
- 学员 → /pages/student-home（switchTab）
- 教练 → /pages/coach-schedule（redirectTo 兜底）
- 管理员 → /pages/admin-dashboard（redirectTo 兜底）
- quickLogin(e) 方法：不写数据库，直接生成对应角色 userInfo + token 存 storage，跳转对应端
- 图标用 SVG data-URI（人形/教练板/盾牌），无 emoji，遵循设计规范
- 已提交推送 f3145dd；预览包 1020.5 KB

**注意**: Ardot 连接在本轮不稳定（mcp__ardot 工具不可用），设计稿未更新，仅实现代码。登录页设计稿后续可补身份入口视觉。
3. 待提交文件: app.json / api.js / server/db.js / server/index.js / pages/admin-db/（新增）

**重启后收尾步骤**: 删 git 锁 → 提交推送 → 启动 server（node index.js）→ 测删除/清空接口 → 工具预览验证页面

## 第十八轮：建立开发待办清单 TODO.md

- 根目录新建 `TODO.md`，作为持续维护的开发待办清单（用户可随时扔想法、从中挑下一个目标）
- 结构：📥 想法收集箱（Inbox，先扔不分类）→ 🎯 下一个目标（P0 优先）→ ✅ 已完成里程碑 → 9 大板块（账号登录/课程订课/支付订单/签到考勤/教练端/管理后台/数据后端/部署上线/体验优化），每项带 [ ]/[~]/[x] 状态与 P0/P1/P2 优先级
- 两个 P0 头号目标：① 注册正式小程序（个体工商户主体，当前测试号禁云开发/微信支付/真实手机号）② 真实订课链路落库（courses/bookings/orders 表，当前仅用户真实、课程等全 mock）
- 用法约定：用户可直接改文件，也可在对话里发想法由我归档
- **新增想法（Inbox）**：微信登录后自动使用微信头像+昵称，无需手动点选（替代当前完善资料手动选择流程）

## 第十九轮：i18n 国际化（中英切换）

**需求**: 所有页面文字统一到语言文件；登录页左上角语言切换（中/英，默认中文）。

**已交付**:
- `miniprogram/utils/i18n.js`: 161 个 key 中英词典 + loadLang/setLang/getLang/t 方法
  - 结构: `DICT = { key: { zh: '...', en: '...' } }`，语言偏好存 storage `app_language`
  - **重要修复**: t() 需按语言重组字典（遍历 DICT 取 `DICT[key][lang]`），不能直接返回 `DICT[currentLang]`（DICT 没有 zh/en 顶层属性——本轮踩的坑，debug 5 轮才定位）
- `app.js`: onLaunch 初始化语言（globalData.lang），新增 switchLang 方法
- `login/index.*`: 文字全部改 {{t.xxx}}；左上角加语言切换圆钮（EN/中）；switchLang 方法
- `student-home/index.*`: 文字改 {{t.xxx}}；带占位符文案（greeting/metaText/coachText）在 JS 用 replace 组装

**技术要点**:
- **WXML 不支持调用 .replace 等方法**，占位符替换必须在 JS 中完成（data 里组装好再绑定）
- 测试 i18n 需 mock wx（global.wx = { getStorageSync, setStorageSync }）
- 登录页+首页 28 个 key 覆盖验证通过，中英切换正常

**状态**: 已推送 1aca1f5（含 TODO.md 一并提交）；预览 1.0 MB；**其余 18 个页面尚未接入 i18n**（TODO.md 已记，后续轮次继续）

- **新增想法（Inbox）**：按时段问候（6-12早 / 12-13午 / 13-18下午 / 18-22晚 / 22-次日6夜深），学员端首页

## 第二十轮：微信登录自动使用头像昵称（移除完善资料流程）

**需求**: 微信登录后自动使用微信头像和昵称，无需手动点选（TODO.md Inbox 想法落地）。

**改动**:
- `login/index.js`: finishLogin 直接 doLogin（删除 needProfile 判断）；移除完善资料方法（onChooseAvatar/onNickInput/confirmProfile）；data 移除 showProfile/tempAvatar/tempNick
- `login/index.wxml`: 删除完善资料弹层（profile-mask 区块）
- 微信登录 getUserProfile 返回的 avatarUrl/nickName 自动传递 → 后端入库 → 首页显示

**验证**: 端到端通过（openid=test_wx_auto 入库昵称"微信昵称测试"+头像 URL 正确）；测试数据已清空；预览 1.0 MB；已推送 8ec335e

**注意**: 新版基础库（2.27.1+）getUserProfile 可能只返回"微信用户"默认值（真机场景），此时头像昵称为默认——这是微信平台限制，非代码问题；如需真实昵称仍需官方头像昵称填写能力（当前已按用户要求移除手动流程）

## 第二十一轮：登录页管理员入口改为一键清空按钮

**需求**: 登录页演示身份入口的「管理员」改为一键清空数据库按钮，点击→弹框确认→清空。

**改动**:
- login/index.js: quickLogin 移除 admin（只留 student/coach）；新增 clearDb 方法（wx.showModal 二次确认 → api.clearUsers()，loading 提示 + 错误处理）
- login/index.wxml: 管理员入口改为「清空数据库」（bindtap=clearDb，垃圾桶图标，红色警示文字）
- login/index.wxss: demo-ico-admin 替换为 demo-ico-clear（垃圾桶 SVG 红色 + 浅红底），新增 demo-txt-danger 红色文字

**验证**: JS/WXML 通过、后端清空接口联通（已清空 1 测试用户）、预览 1.0 MB、已推送 bc30c7e

## 第二十二轮：完善资料弹层头像占位改正圆

**需求**: 登录页完善资料弹层里"黑色椭圆框"改为正圆，与选完后的正圆头像同尺寸。

**根因定位**: 代码里 avatar-btn 本就是 160×160 正圆；"黑色椭圆"实际来自默认占位图 `images/2_556.png`——该图自带深色圆环+深色人像剪影（逐像素分析 alpha/luminance 确认），视觉上像黑色椭圆。

**修复** (login/index.wxml + wxss + js，已推送 1a60c44):
- 默认占位改为空串 → WXML `wx:if="{{tempAvatar}}"` 显示图片 / `wx:else` 显示纯 CSS 正圆占位 `.avatar-ph`（152×152 浅米色圆 + 灰色人像 SVG，与选完后头像同尺寸）
- finishLogin 把 `2_556.png` 映射为空（视为未选头像）；用户真实授权头像仍正常显示
- `.avatar-btn` 加固：display:flex 居中 + box-sizing + line-height:1 + font-size:0，防 button 默认样式拉伸成椭圆
- 注意：2_556.png 仍作为 App 全局默认头像（个人中心等），未改动

**踩坑**: Python 3.13 struct.unpack 要求缓冲区与格式**严格等长**（多/少都报 "requires a buffer of N bytes"），解析 PNG IHDR 需切片精确到 10 字节。

## 第二十三轮：学员首页移除搜索框

**需求**: 学员进入后的首页，时间/问候下方的搜索框去掉。

**改动** (student-home 三件套，已推送 b7b4305):
- WXML 删除 search-bar 区块（问候语 → Banner 直接衔接）
- JS 删除 goSearch 方法（原本也只是 switchTab 到课程页，与"查看更多"重复）
- WXSS 删除 search-bar / icon-search / search-ph 样式
- i18n 的 searchCourseHint key 保留未删（无副作用，后续搜索功能可能复用）

## 第二十四轮：课程数据结构设计文档

**需求**: 梳理课程数据结构（属性清单），为数据库表、排课配置工具、后端 API 设计提供依据。

**交付**: 根目录 `DATA-MODEL.md`（已推送）：
- **核心决策：课程模板(courses) 与 场次(course_sessions) 分离**——模板=静态信息(名称/分类/难度/时长/价格分/封面/描述)，场次=具体某天某时段某教练某场地(容量/已约数/状态)；days 重复规则 → schedule_templates 表
- 6 张表 DDL（courses/course_sessions/bookings/coaches/venues/schedule_templates）+ 3 组状态枚举（场次/预约/支付）
- 关键规则：价格存分、余位不存(算)、原子扣减防超卖、唯一约束防重复约、退订释放余位、价格快照
- 现有 mock 字段映射表、排课配置工具字段映射、建议 API 清单、落地路径
- 配套 ER 图（mermaid，会话内联展示）

**后续**: 落地= server/db.js 建表 + mock 迁移 + api.js 接接口（对应 TODO P0 真实订课链路）。

## 第二十五轮：建表 + 录入初始数据（用户口述）

**需求**: 用户口述初始数据，建立数据库并录入。

**交付**（已推送）:
- `server/db.js` 新增 6 张表 DDL（coaches/venues/courses/schedule_templates/course_sessions/bookings + 索引 idx_sessions_date/course），完全按 DATA-MODEL.md §2
- 新增 `server/seed.js` 种子脚本（幂等，可重复跑：`node server/seed.js`）

**初始数据**（用户口述，8/10~8/30 共 126 场次，全部教练=喻馥雅）:
- 教练 2：喻馥雅（Hybrid综合体能,引体向上,产后康复）、马春艳（减脂）
- 场地 1：成华好事馆（容量 20）
- 课程 2：Hyrox冲冲冲（Hybrid综合训练/难度3/60分/¥80→8000分）、Hyrox新手（难度1/60分/¥90→9000分）
- 周规则 42 条：周一~周日 × 6 时段（10-11/11-12/15-16/16-17/20-21/21-22，奇数时段冲鸭、偶数新手）
- 场次 126 条：8/10~8/30 每天 6 堂，published，booked_count=0，容量 20
- 验证：8/10 课表正确、教练/场地分布正确、两课各 63 场次、8/30 仍有 6 节

**注意**: 马春艳暂未排课（用户选择全部喻馥雅带）；bookings 空表待预约功能接入。

## 第二十六轮：课程编辑网页（电脑端）+ 课程 CRUD/发布 API

**需求**: 制作电脑浏览器访问的课程编辑网页：新增/编辑/删除课程，所有属性下拉菜单式，末尾「发布课程」按钮。

**交付**（已推送 c601643）:
- `web/courses.html` 单文件网页（品牌视觉：奶油底/青柠/电光紫）：左侧课程列表（新增/编辑/删除入口），右侧表单——课程信息（名称文本框 + 分类/难度/时长/价格/状态/封面/描述全部下拉）+ 每周排课规则行（周几/起止时间/教练/场地/容量下拉，可多条）+ 末尾发布课程（起止日期下拉 + 按钮）
- `server/index.js` 新增 API：GET /api/meta（下拉选项：教练/场地/分类/价格/时长/时段/图片清单/周几等）、GET/POST /api/courses、PUT/DELETE /api/courses/:id、POST /api/courses/:id/publish；新增静态服务：/ → web/courses.html、/images/ → miniprogram/images（封面可预览）
- `server/db.js` 新增函数：listCoaches/listVenues/listCourses(含规则)/getRules/replaceRules/createCourse/updateCourse/deleteCourse(级联删规则+场次，有订单则 409 拒绝)/publishSessions(按规则生成场次，幂等跳过已存在)

**端到端验证通过**: 新增201→编辑200→发布(8/10命中周一规则新增1场次)→重复发布幂等跳过→删除级联清理→场次总数保持126

**踩坑**: SQLite 中 `"_"` 是标识符不是字符串字面量（报 no such column），拼接 key 必须用单引号 `'_'`。

**注意**: 服务已重启运行（node server/index.js，后台任务）；网页访问 http://127.0.0.1:3000/；手机局域网真机联调用 http://192.168.194.11:3000/

## 第二十七轮：学员端课程列表接入真实场次数据

**需求**: 学员端拉取正确的课程列表并显示（TODO P0 第一步落地）。

**交付**（已推送 1fccc30）:
- `server/db.js`: 新增 listSessionsByDate(date)/getSessionById(id)，公共 SESSION_SELECT JOIN（场次×课程×教练×场地，含 remaining=capacity-booked_count、price_fen、cover）
- `server/index.js`: GET /api/sessions?date=YYYY-MM-DD（返回当日已发布场次）、GET /api/sessions/:id；缺参 400
- `miniprogram/utils/api.js`: 新增 getSessionsByDate/getSession（USE_CLOUD 时 reject 提示）
- `student-courses` 页: 本周日期条改为**动态计算**（周一~周日真实日期，不再用 mock 写死 8/10-8/16）；选日拉取当日场次，卡片显示课程/教练/场地/余位/价格；**后端不可用时自动回退 mock 演示数据**并显示"后端未连接"提示条；加载中状态
- `student-course-detail` 页: 支持 session_id 参数走真实接口；兼容旧 id 参数走 mock；课程未设封面用 2_193.png 占位；bookNow 携带 session_id

**验证**: 8/10 返回 6 场次（Hyrox冲冲冲/新手 × 3 时段，喻馥雅/成华好事馆，余 20，¥80/¥90）；详情接口正常；空日期返回 []; 缺参 400

**遗留**: 首页热门课程仍为 mock；支付/我的课程/签到未接真实数据；课程封面为空（可在课程编辑网页设置）

## 第二十八轮：首页改"今日课程"+已结束灰化

**需求**: 首页"热门课程"改为"今日课程"，默认显示当天课程；按当前时间判断，已结束的课程标记"已结束"并灰掉按钮。

**改动**（已推送 cb8fa01）:
- `student-home/index.js`: loadTodayCourses() 调 GET /api/sessions?date=今天 → 当天场次；isEnded(end_time) 比较当前时间>当天结束时间 → ended 标记；refreshUser 每次 onShow 重算 ended（跨时段自动更新）；后端不可用回退 mock（前3门，同样判断结束）
- wxml: 标题 {{t.todayCourses}}；按钮 `{{item.ended ? t.ended : '¥xx 约课'}}` + book-ended 灰化样式；新增今日无课空状态；离线提示条
- `i18n.js` 新增 key: todayCourses / noTodayCourse / ended（中英）
- goDetail 传 session_id（真实场次详情）

**注意**: 真实场次封面为空（课程编辑网页可配）；已结束课程仍可点进详情页（仅按钮灰化），如需禁止跳转可后续加

## 第二十九轮：课程三态判断补全（进行中红标不可点）

**需求**: 判断逻辑补全——当前时间在课程时段内标记"进行中"、显示红色、且不能点击。

**改动**（已推送 4e5ab04）:
- `getStatus(start,end)` 三态：now<start=upcoming（可约）/ start<=now<end=ongoing（进行中）/ now>=end=ended（已结束）
- 首页课程卡片：名称旁红底红字标签「进行中」（status-ongoing）、灰标签「已结束」；按钮三态（红色进行中/灰色已结束/绿色约课）；卡片 opacity 0.72 弱化
- goDetail 拦截：非 upcoming 状态弹 toast「课程进行中/已结束，无法预约」，不跳详情页
- i18n 新增 ongoing key（中英）；每次 onShow 重算状态
- 验证：16:59 时 16:00-17:00 的课=进行中（红），10:00-12:00/15:00-16:00=已结束，20:00+=未开始

## 第三十轮：生成开发总结文档

- 根目录新建 `开发总结-2026-08-10.md`：今日功能清单（设计/小程序20页/后端16API/7表/种子数据/课程编辑网页/真实数据接入）+ 技术决策 + 踩坑经验（微信平台限制/环境工具/编码技术/架构设计四类）+ 系统状态 + 下一步建议
- 已推送；后续维护：每日迭代后同步更新或滚动总结

## 第二十二轮：本地服务器访问不了（IP 变更）

**问题**: 用户报本地服务器访问不了。
**根因**: 本机 IP 从 192.168.194.11 变为 192.168.101.10（网络变化），BASE_URL 还是旧 IP → 连不上（curl exit 28 超时）。
**修复**: api.js 的 LOCAL_BASE_URL 更新为 http://192.168.101.10:3000，验证 200 OK；已推送 55ddc2f；预览 1.0 MB。
**经验**: 局域网 IP 会随网络变化，遇到"连不上服务器"先检查 ipconfig 当前 IP 与 api.js 配置是否一致（本项目已两次因 IP 变更断连：192.168.194.11 → 192.168.101.10）。

## 第二十三轮：接入真实微信登录（code2session）

**需求**: 用户希望测试用户是"真实微信账号"（真实 openid 入库）。

**实现**: server/index.js 新增微信登录支持：
- WX_APPID / WX_SECRET 从环境变量读取（secret 不硬编码）
- code2Session(code)：用 node:https 调微信 jscode2session 接口换真实 openid（注意：必须用 https 模块，http.get 会报 Protocol not supported）
- handleLogin 优先用 code 换真实 openid；换取失败（无 secret/假 code）自动降级用客户端 openid（wechatVerified 标记）
- 客户端无需改动（doLogin 已同时传 code + openid）

**验证**: 假 code + dummy secret → 微信返回错误 → 降级注册成功（wechatVerified: false）；已推送 c478d70

**启用真实微信登录的下一步（用户待操作）**: 在小程序后台（mp.weixin.qq.com）→ 开发管理 → 开发设置 → 查看/重置 AppSecret，然后重启后端：
`cd server && WX_APPID=wx509088154a505409 WX_SECRET=<真实Secret> node index.js`
之后真机扫码登录即用真实 openid 入库。
- **新增想法（Inbox）**：团购自助验券功能（学员持团购平台券码到店，出示二维码/券码自助核销）

## 第二十四轮：真实订课链路落库（P0 达成）

**需求**: 支付确认后真实订课，我的课程显示已订课程；为空显示"您尚未订课"。

**后端**（server/db.js + index.js）:
- db.js 新增: createBooking（事务：创建订单+扣减余位，防超卖；重复订课拒绝；**退订后可重新激活原订单**）、listBookingsByUser、cancelBooking（退订恢复余位）、countBookingsByUser
- index.js 新增路由: POST /api/bookings（订课）、GET /api/bookings?openid=（查询）、DELETE /api/bookings/:id?openid=（退订）
- bookings 表早已建好（booking_no/user_openid/session_id/amount_fen/status/pay_status）

**客户端**:
- api.js: bookCourse / getMyBookings / cancelBooking
- student-pay: 支付成功后调 bookCourse（openid+sessionId+amountFen），失败弹窗
- student-my-courses: 从后端加载真实订课（过滤 booked），空态"您尚未订课"+去选课按钮；退订真实调后端
- 注意: 课程详情页 bookNow 已传 session_id（真实场次id）到支付页

**验证**: 订课成功（Hyrox冲冲冲 ¥68）/重复拒绝/退订恢复余位/重订激活/查询正常；端到端模拟支付页调用通过；已推送 9bd7108；预览 1.0 MB

**遗留**: "已完成"tab 仍为空态（签到功能未落库 checkin_at）；签到、我的成就等仍 mock
- **新增想法（Inbox）**：微信推送重要消息（开课提醒/签到提醒/课表变动等），消息类型可自定义配置（订阅消息 subscribeMessage）

## 第二十五轮：支付成功落地页

**需求**: 支付完成后新增落地页：显示"支付成功！"+ 查看按钮 → 回主页面默认打开「我的」tab。

**实现**:
- 新增页面 pages/pay-success/: 绿色对勾图标（SVG）+ 标题"支付成功！" + 课程信息卡（封面/名称/教练/场地/时间/实付金额）+ 「查看」按钮 + 提示文字
- 「查看」→ wx.switchTab 到 /pages/student-profile（tabBar 第 4 项「我的」）
- student-pay 支付成功回调从 showModal 改为 wx.redirectTo 落地页
- app.json 注册 pay-success 页面
- 已推送 56ead2f；预览 1.0 MB

**tabBar 结构**: 今日(student-home) / 本周(student-courses) / 我的课(student-my-courses) / 我的(student-profile)
- **新增想法（Inbox）**：个人中心新增入口 → 独立"联系我们"页（微信小助理二维码 + 官方微信群二维码 + 返回按钮）

## 第二十六轮：课程列表席位显示（已订/总席 + 满员）

**需求**: 今日/本周课程列表显示真实席位（如 01/20）；满员（20/20）按钮变"已满"且不可点击。

**实现**（student-courses 页）:
- JS: decorateSession() 统一装饰真实/mock 场次数据——计算 booked（cap-remaining）、seatText（两位补零"01/20"）、isFull（booked>=cap）
- goDetail 加 data-full 判断：满员直接 return（整卡和按钮都不可点）
- WXML: 教练行加 seat-count（席位计数，满员红色）；按钮 wx:if 满员显示"已满"否则"¥xx 约课"
- WXSS: seat-count/seat-full（红）/btn-full（灰）/card-full（整卡降透明度 0.75）

**验证**: 席位计算边界正确（00/20、01/20、18/20紧张、20/20满员）；真实数据验证 Hyrox冲冲冲 10:00 场次显示 01/20（测试用户订课生效）；已推送 a24fc41；预览 1.0 MB

**注意**: 首页(student-home)的热门课程卡片是推荐位（mock），未做席位显示——用户说的"今日/本周"是课程列表页
- **新增想法（Inbox）**：课程描述随状态变化（未开始=介绍/报名提示，进行中=进行中提示，已结束=结束/复盘说明，课程详情页三态文案）
- **新增想法（Inbox）**：好友赠送一堂课（A 赠课 → B 微信收小程序分享页 → 注册登录后自动获得该课，需赠课记录表）

## 第二十七轮：已预订课程显示「已预订」且不可点击

**需求**: 用户已订的课程显示"已预订"且不能点击。

**后端**:
- db.js: listSessionsByDateForUser(date, openid) — 查用户 booked 状态的 session_id 集合，给每个场次加 booked_by_me 标记；handleSessionDetail 也支持 openid 标记
- index.js: /api/sessions 与 /api/sessions/:id 读取 openid 参数

**前端**:
- api.js: getSessionsByDate(date, openid)、getSession(id, openid) 支持传 openid
- student-courses: loadSessions 传 openid；decorateSession 加 isBooked（优先于满员）；goDetail 拦已订；WXML 按钮三态（约课/已满/已预订）；btn-booked 样式（品牌深色底+荧光绿字）
- student-course-detail: loadSession 传 openid 设 isBooked；bookNow 拦已订；WXML 已订态按钮

**验证**: 田立视角场次6 booked_by_me=true，其他 false；JS/WXML 校验通过；已推送 4a9821f；预览 1.0 MB
- **新增想法（Inbox）**：满员课程候补排位（付费排位；有人取消→最早排位者自动获得席位+取消者退款；课程开始仍未排到→自动退款，需 waitlist 表+自动转正/退款任务）

## 第二十八轮：tab 更名 + 我的页真实锻炼次数

**需求**: ①「我的课」tab 改「上课」 ②「我的」页锻炼次数 = 已订且已结束的场次总数（真实数据）。

**实现**:
- app.json: tabBar 第3项 text 我的课→上课；student-my-courses 页标题同步改「上课」
- 后端 db.js: countFinishedWorkouts(openid)（已订且场次已结束：日期<今天 或 今天且end_time<now）、countUpcomingBookings(openid)（待上课）；handleStats 支持 ?openid= 返回 myStats{finishedWorkouts, upcomingBookings, totalBookings}
- api.js: getUsersStats(openid) 支持传 openid
- student-profile: onLoad/onShow 从后端拉真实锻炼次数显示"累计锻炼 X 节课"（后端不可用保持默认）

**验证**: 田立数据 finishedWorkouts=0（订的课未结束）/upcomingBookings=1（21:00未开始，20:00进行中）✓；造昨日场次测试 finishedWorkouts=1 ✓（已清理）；已提交 f923d73（推送因 GitHub 网络失败暂缓，本地已保存）

**注意**: 进行中的课（start<now<end）既不算完成也不算待上课——符合"锻炼次数=已结束场次"定义

## 第二十九轮：本周列表"所有日期为空"修复

**现象**: 用户排满了本周课程，但「本周」页所有日期显示空。

**排查**: 数据库正常（本周每天 6 场）、API 正常（每天 6 场）、BASE_URL 正常——问题在前端。
**根因**: selectDate 里 `e.currentTarget.dataset.date` 是**字符串**（如 "11"），setData 后 selectedDate 变成字符串；onShow 重载时 `weekDays.find(d => d.date === selectedDate)` 用 `===` 严格比较（d.date 是数字 11），**数字 !== 字符串 → find 返回 undefined → 不加载数据**。且切换日期时 loadSessions 先清空列表 → 列表一直空。
**修复**: selectDate 里 `Number(dataset.date)` 转数字；onShow 查找用 `Number(selectedDate)` 兜底。
**状态**: 已提交 9c0755d（GitHub 推送仍失败，网络连不上 443，本地已保存 2 个提交 f923d73 + 9c0755d 待推）

## 第三十轮：本周课程全空的真正根因（app is not defined）

**现象**: 用户报本周所有日期课程为空（持续两轮未解决）。

**真正根因**（用户提供 Console 报错）: `ReferenceError: app is not defined at loadSessions` —— student-courses/index.js 里 loadSessions 用了 `app.globalData.userInfo`，但**文件顶部没有 `const app = getApp()`**。一进页面 loadSessions 抛异常 → courseList 永远空数组 → 所有日期空。

**为什么之前没发现**: 后端/API/前端数据流全链路验证都正常；用 Node 模拟渲染时用的是假数据，没走 app.globalData 那行——教训：**模拟测试要覆盖真实代码路径**。

**修复**: student-courses/index.js 顶部加 `const app = getApp()`；并全面扫描所有页面确认无同类遗漏（全部通过）。

**已推送**: 7879d71（GitHub 网络恢复，连带 f923d73/9c0755d/a8ad6a5 全部推上）。

**经验**: 用户报错信息（Console 红色报错）是最快定位手段——第二轮就该直接问用户要报错，而不是继续猜。

## 第三十一轮：本周课程按时间显示状态（统一今日首页逻辑）

**需求**: 「本周」页面课程状态按时间动态显示（未开始/进行中/已结束），与「今日」首页一致，且"以此类推"统一。

**实现**:
- 新建 utils/course-status.js 公共工具: getSessionStatus(date, startTime, endTime, now)——按日期+时间判断：未来日期→upcoming / 过去日期→ended / 当天按时间分 upcoming/ongoing/ended
- student-courses: decorateSession 加 status（传 date=full）、disabled（已预订|非upcoming|满员 不可点）；WXML 加状态标签（进行中红/已结束灰）+ 按钮五态（约课/已预订/已满/进行中/已结束）；引入 i18n t
- student-home: getStatus 改为调用公共工具（统一逻辑）
- 样式: status-tag/status-ongoing/status-ended/btn-ongoing/btn-ended

**验证**: 边界测试全对（今天20:34: 20-21点ongoing/21-22点upcoming/10-11点ended；明天upcoming；昨天ended）；已推送 7a31d3c；预览 1.0 MB

**i18n**: 已含 ongoing(进行中)/ended(已结束) key

## 收工（20:40）

**今日总结**: 从上午的版本管理故障处理到晚上的真实订课链路，今天推进巨大。所有代码已提交推送（HEAD=e9df46a），工作区干净，TODO.md 已更新。

**已推送提交链**: ... → 7a31d3c（本周状态）→ e9df46a（TODO 更新）

**遗留事项**:
- 后端运行中（127.0.0.1:3000，WX_SECRET=dummy 待换真实 AppSecret）
- 数据库含测试数据（田立 2 门订课 + 2 个测试用户 + 历史场次 8/9-8/30）
- GitHub 推送偶发网络失败（重试即可，已全部同步）
- 剩余 18 个页面未接入 i18n（词典已备 166 key）
