# ENVIRONMENTS.md · 环境与发布规范（v2）

> **一句话看懂（2026-08-18 对齐现实）**：正式运营后本应有 4 个「房间」——实验室（开发）、质检站（自动检查）、彩排厅（真机验证）、正式营业的店（生产）。**当前阶段是「3+1」**：
> - ① 本地开发（SQLite 实验室）→ ② CI 质检 → ③ 个人号云托管 = **线上开发环境（预发布兼生产合一）**
> - ④ 公司号 = **未来生产环境**（注册后整体迁移，见第七节迁移清单）
>
> 为什么 ③ 和 ④ 当前合一是合理的：个人号阶段**没有真实用户**，云托管既是彩排厅也是店——代码 push 即上线，真机即验收。等公司号注册好、整体迁移完成，才恢复严格四环境分设。
> **给谁看**：所有参与者（开发/合伙人/运营）。读完这篇，你就知道「这件事该在哪做」。

---

## 一、四个环境的地图（先记住这 4 个房间）

| 环境 | 打个比方 | 它是什么 | 它的职责 | 数据从哪来 |
|---|---|---|---|---|
| **① 本地开发环境** | 你家客厅的「实验室」 | 你电脑上的整套系统：后端跑在 `127.0.0.1:3000`（SQLite 数据库），小程序在微信开发者工具里调试 | **写代码、做实验**——怎么折腾都行，改坏了大不了重启 | 本地数据库文件 `server/data/gym.db`（测试数据，随便造） |
| **② 质检环境（CI）** | 工厂里的「机器质检员」 | GitHub 云端的自动化流水线：每次提交/推送自动跑全量测试 + 覆盖率 | **自动把关**——代码不合格就亮红，不许进入下一步 | 临时干净库（每次全新的，不存任何真实数据） |
| **③ 预发布环境** | 演出前的「彩排厅」 | 真机走一遍核心流程（登录/订课/签到/教练工作台），配置与生产一致 | **人肉验收**——机器测不出的体验/交互/多端问题在这里查 | 测试数据；**当前 = 个人号云托管（与生产合一，见上方说明）** |
| **④ 生产环境** | 「正式营业的店」 | 微信正式小程序（公司主体 AppID）+ 微信云托管（MySQL 持久化） | **服务真实用户**——真实下单、真实收款、真实数据，改错的代价最大 | 云托管内置 MySQL（生产独立库，2026-08-17 起已启用） |

> 记忆口诀：**①写代码 ②机器查 ③真人验 ④上线跑**。任何改动都从 ① 出发，经 ② 绿灯、③ 验收，最后才进 ④。

---

## 二、什么动作，在什么环境做（对照表）

| 你要做的事 | 在哪个环境 | 为什么 |
|---|---|---|
| 写代码、改页面、改数据库结构 | ① 本地 | 开发的本职工作，不影响任何人 |
| 跑自动化测试（224 用例） | ① 本地（或提交时自动） | 快速反馈，改完马上知道好坏 |
| `git commit`（提交存档） | ① 本地 | **L1 闸门**：hook 自动跑全量测试，红了拒绝提交 |
| `git push`（推送到云端仓库） | ① 本地 → 触发 ② | **L2 闸门**：CI 在干净库再跑一遍 + 覆盖率；**同时触发云托管自动重建（当前 master=线上）** |
| 部署验证 | ③（个人号云托管） | **deploy-smoke.sh 必跑**：8 项冒烟（health/新路由 400/登录落库/管理页/访问码），区分新/旧镜像，防 BUG-LEDGER #12 重演 |
| 新功能/大改动后真机体验 | ③ 预发布 | 模拟器 ≠ 真机；当前 = 云托管真机预览码直接验证 |
| 上线前的完整手测（L3 清单） | ③ 预发布 | 按 DEFINITION-OF-DONE 逐项打勾 |
| 提交微信审核、正式发布 | ④ 生产 | 只有这里能触达真实用户（公司号注册后才分设） |
| 排课、改价格、发优惠、看营收 | ③ 当前 / ④ 未来（管理后台） | 运营操作直接影响营业数据；走管理网页 + Admin-Token + logOp 留痕（不直接改库） |
| 用户投诉了，查这笔订单/退款 | ④ 生产（云数据库） | 数据都在云上，可追溯（DoD 要求） |
| 本地 ↔ 云端数据搬运 | ①②④ 之间（迁移脚本） | 两边数据**不互通**，必须走脚本，不能手动复制 |

---

## 三、一次改动的完整旅程（发布流水线 · 当前现实版）

```
你在 ① 本地改代码
   ↓
git commit → L1 闸门（hook 自动跑 224 用例）→ 红：打回改
   ↓ 绿
git push → L2 闸门（CI 干净库跑测试+覆盖率）→ 红：打回改
   ↓ 绿 + 云托管 webhook 自动重建（重建窗口期数分钟，访问到旧镜像属预期）
bash deploy-smoke.sh → 8 项冒烟判定新/旧镜像 → 未过：等重建完成或查日志
   ↓ 绿
微信开发者工具预览码 / 真机 → ③ 预发布 真人手测（登录/订课/签到/教练链路）
   ↓ 全过
正式发布（公司号阶段）→ ④ 生产 真实用户使用
```

**每个环节都拦得住错误**：① 改错自己知道，② 机器查错，③ 真人查体验，④ 才是最后一道。越早拦住，代价越小。

**两类线上操作纪律（当前合一阶段）**：
- **代码部署**：push → 冒烟（闸门已强制，见上）
- **数据/运营操作**：只走管理网页（Admin-Token 校验）+ logOp 留痕，禁止直接改生产库——例：合并教练档案、删档案、改头像均走 web 管理接口

---

## 四、铁律（三条，违反必出事）

1. **改动必须从 ① 出发，走完闸门才准进 ④**——永远不要跳过测试直接在生产改
2. **各环境数据库互相独立**——本地 SQLite、CI 临时库、生产 MySQL 是三个世界；数据迁移只能走脚本（`scripts/migrate-sqlite-to-mysql.js`，SQLite→MySQL）
3. **配置按环境隔离**——AppID/AppSecret/云环境 ID/ADMIN_TOKEN 是生产机密，绝不写死在代码里（密钥走环境变量 / server/.env，gitignore）

---

## 五、诚实标注：当前状态（2026-08-18）

| 事项 | 状态 |
|---|---|
| ① 本地开发环境 | ✅ 完备（224 用例 + 干净库模式 + 冒烟测试 smoke.js） |
| ② CI 质检环境 | ✅ 上线（GitHub Actions 全绿，push/PR 自动触发） |
| ③ 预发布环境 | ✅ **= 个人号云托管（合一）**：deploy-smoke 8 项 + 真机预览；L3 真机手测清单按需执行 |
| ④ 生产环境 | ✅ 个人号云托管已全量上线（Docker + MySQL 持久化 + ADMIN_TOKEN/WX_* 环境变量全配，2026-08-17 起）——**公司号注册后整体迁移，见第七节** |
| **「完整生产环境」的达成标准** | 公司号小程序 + 云托管 MySQL 迁移完成 + 微信支付/商户号就位 + L3 清单全绿 |

---

## 六、历史：公网穿透测试模式（2026-08-15 起废弃，仅存档）

**背景**：云托管上线前，拉朋友真机测试需要后端公网可达。方案演进：Cloudflare Quick Tunnel → **cpolar 免费隧道**（国内节点）。2026-08-15 切云托管后**不再需要**；cpolar 仅剩「本地后端联调公网」场景可用（需同步 FALLBACK_BASE_URL + 小程序合法域名，域名重启会变）。

| 项 | 历史值（2026-08-14） |
|---|---|
| 公网地址 | `https://35d0ecae.r22.cpolar.top`（免费版域名重启会变） |
| 前端地址源 | `miniprogram/utils/api.js` 的 `FALLBACK_BASE_URL`（**现为 TCB_BASE_URL，云托管固定域名**） |
| 启动方式 | `server/start-cpolar-tunnel.bat`（隧道域名 → 同步 api.js → 重新编译） |
| AppID | 测试号 `wxd63f6b720b3021ea`（已换正式号 `wx0aee5332d4ef20fd`） |
| 真机要求 | ①工具勾选「不校验合法域名」②真机右上角「···」→「打开调试」 |

---

## 七、公司账号迁移清单（2026-08-18 用户确认 · 未来生产环境）

> **背景**：当前在个人微信账号下开发（= 线上开发环境），后续整个小程序迁移到公司账号（= 生产环境）。注册公司号后按此执行。

### 关键事实：openid 是 AppID 维度的

换公司号 AppID 后，**所有用户 openid 全部变化**，users 表无法按 openid 关联。破解方式二选一：
- **无真实用户时（推荐时机）**：迁移只带业务数据（课程/教练/排课模板/配置），**不带用户**——新环境重新种子 + 用户重新微信登录注册，天然新号，零映射成本
- **已有真实用户时**：须先完成真实手机号链路（企业认证后 `getPhoneNumber`，TODO 1️⃣ 区），迁移时用手机号做 openid 映射

### 迁移步骤

| # | 项 | 动作 |
|---|---|---|
| 1 | AppID/AppSecret | `project.config.json` + `server/.env`（WX_APPID/WX_SECRET）换公司号 |
| 2 | 云托管新环境 | 新环境建服务，改 `miniprogram/utils/api.js` 三处：`TCB_ENV`/`TCB_BASE_URL`/`TCB_SERVICE`（唯一人工配置源，其余代码零改动） |
| 3 | MySQL 数据 | 旧库导出业务表（课程/教练/排课模板/配置）→ 新库导入；用户数据按上方 openid 策略决定带不带 |
| 4 | ADMIN_TOKEN | 新环境重新生成（生产必配，web 管理接口强制校验 Admin-Token header） |
| 5 | 小程序 | 重新提交微信审核、正式发布（公司主体需备案） |
| 6 | 部署验证 | `bash deploy-smoke.sh` 对新域名跑 8 项冒烟 |
| 7 | 微信支付 | 商户号申请 + `wx.requestPayment` 接入（个人号阶段无法验证，正式收款前置） |
| 8 | 收尾 | 更新本文件状态表 + TODO 迁移待办打勾 + 开发总结记录 |

---

*v1 创建：2026-08-13（L3 发布闸门第一步）；v2 更新：2026-08-18（对齐「个人号=线上开发、公司号=未来生产」现实 + 部署冒烟流程 + 迁移清单）。环境有变化时更新此文件并同步 CONVENTIONS。*
