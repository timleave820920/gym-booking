# 综合训练馆订课系统 · 长期项目笔记

## 项目战略目标（2026-08-13 用户明确）
- 大功能已齐，剩余：接入微信企业号 + 云端部署
- **作品目标**：消费产品级质量，规模化问题必须解决好（非演示级）
- **个人目标**：通过本项目学到与 AI 协作做产品的实在经验
  - 提升工程认知：明确下一阶段应积累的知识方向
  - 沉淀 skill / 规范文档，让后续 vibe coding 更高效高质量

## 代码质量基线（2026-08-13 体检：7/10 中上）
- 强项：prepared statements 154 处、4 处关键事务、统一 sendJson、密钥环境变量、173 提交纪律、70 端到端断言
- 弱项：手写 if-else 路由 40+ 分支、db.js 单文件 1817 行、测试无断言框架、前端 wxss 6K 行重复、空 catch 多
- 提升优先级：①测试框架化 ②db.js 按域拆分 ③路由表化 ④统一错误码/日志 ⑤前端样式收敛

## 风格方向状态（重要上下文）
- 多次 mockup 迭代（Foodnoms/UGLYCASH/Nike/运动风格 A-D/质感 C'-D'/V2 完整版）后用户仍不满意
- 双主题系统（C 浅色/D 深色）落地后用户判定"完全不行"，已回滚到 a5faa63（主题改造前）
- 当前风格 = 原始奶油底 #F9F4DF + 荧光绿 + 黑描边；风格方向待重新决策

## 环境与协作注意
- ⚠️ **git 仓库 2026-08-14 损坏 → 2026-08-15 本地重建根提交 f2a1d7f**：.git/objects/pack 丢失 + refs 被删，本地 7 个提交历史丢失；已用工作区全量快照重建（258 文件，代码受保护）；**待 GitHub 网络恢复后接回远端历史**：`git fetch origin` → `git reset --mixed af69022` → 重新 add/commit → push（勿 force push）；恢复前注意清理 stale 锁（.git/index.lock / HEAD.lock）
- **公网穿透模式（2026-08-14 起）**：cpolar 免费隧道（域名重启会变，当前 `https://2397875d.r8.cpolar.cn`）+ 测试号 AppID wxd63f6b720b3021ea（任何微信可扫预览码）；前端地址**唯一配置源 = api.js FALLBACK_BASE_URL**（net-config.json 已废弃，BUG-LEDGER #14：运行时产物会被并行实例覆盖，绝不可作前端配置源）；真机必须开「···→打开调试」+ 工具勾选不校验合法域名
- **登录号 ↔ AppID 匹配**：工具登录微信必须是当前项目 appid 的开发者——测试号模式用申请测试号的微信登录工具；真实号模式用田立主号
- 存在并行会话操作同一项目（曾有重复实现、SQLite 锁冲突、记忆文件锁、git 损坏）
- 演示阶段 openid 固定 demo_user；正式接 jscode2session 时替换（server/.env 填 WX_SECRET）
- 微信开发者工具 CLI 桥接偶发不可用（port 59775 timeout），预览需手动编译兜底
- **测试时间耦合教训**：测试造数不能写死当天晚间时段（22:00 场次夜间跑必挂，run-tests.js 已动态化）
- **次数包规则（2026-08-15 实现）**：次卡必须覆盖上课日才能抵扣——`getUserPassForDate(openid, date)`（`date(expires_at) >= 上课日期`）；卡今天过期 → 订明天及以后场次支付页显示「次数包已过期」警示条，仅储值/微信支付

## 测试资产
- 假用户测试数据：`node server/seed-fake-users.js`（幂等重置 3-5 名假用户 + 确定性伪随机同堂次数 0-5）；当前 4 名：陈语嫣/吴雨桐/王一诺/郑凯文
- 测试号 AppSecret 仅填 server/.env（gitignore），不入库
