# 综合训练馆订课系统 · 开发总结（2026-08-10）

> 一天完成从设计稿到可运行系统的全链路开发：Ardot 设计 → 微信小程序（20 页）→ SQLite 后端 → 数据库建模与种子数据 → 电脑端课程编辑网页 → 学员端真实数据接入。
> 本文档为当天工作的功能清单 + 经验教训沉淀，详细逐轮记录见 `.workbuddy/memory/2026-08-10.md`。

---

## 一、今日成果总览

| 阶段 | 成果 |
|---|---|
| 设计 | Ardot 全链路设计稿（学员/教练/管理后台三角色），17 个品牌色变量 + 20 个 SVG 图标组件库 |
| 小程序 | 20 页四件套（登录 + 学员端 8 + 教练端 3 + 后台 8），自定义胶囊 TabBar，中英双语（i18n 161 key） |
| 后端 | Node 原生 http + SQLite（零 npm 依赖），16 个 API 接口 |
| 数据库 | 7 张表（users + 6 张课程相关），含索引与状态枚举 |
| 数据 | 种子脚本一键录入：2 教练 / 1 场地 / 2 课程 / 42 条周规则 / 126 个场次 |
| 管理工具 | 电脑端课程编辑网页（web/courses.html）：增删改 + 全下拉属性 + 一键发布 |
| 真实数据接入 | 学员端课程列表（按日期查场次）、首页"今日课程"（三态判断） |

**代码**：GitHub 仓库 `timleave820920/gym-booking`，master 分支，当日提交 20+ 个，全部推送。

---

## 二、功能实现清单

### 1. 小程序前端（20 页）
- **登录**：微信一键登录 + 手机号快捷登录 + 协议勾选 + 演示身份入口（学员/教练/一键清空数据库）+ 中英切换 + 完善资料弹层（官方头像昵称填写能力）
- **学员端**：首页（时段问候/今日课程/Banner）、课程列表（动态本周日期条）、课程详情（余位/价格）、支付（模拟）、我的课程、签到、成就记录、个人中心
- **教练端**：今日课表、学员名单（可切换签到）、扫码签到（深色扫描界面）
- **管理后台**：数据仪表盘、排课管理（周视图）、排课配置（未来课程+发布+刷新设定）、场地/学员/教练管理、营收统计、数据库管理页（删除/清空）

### 2. 后端（server/，端口 3000）
- **认证**：注册/登录合一（首次=注册 201，再次=登录 200）、资料更新、用户列表/统计/删除/清空
- **课程**：课程 CRUD、下拉选项 meta、按规则发布场次（幂等）、按日期查场次、场次详情
- **静态**：课程编辑网页、图片资源

### 3. 数据库（7 表，见 DATA-MODEL.md）
`users` / `coaches` / `venues` / `courses` / `course_sessions` / `schedule_templates` / `bookings`

### 4. 真实数据接入（今日重点）
- 学员端课程列表：本周日期动态生成 → 按日期拉取真实场次（课程/教练/场地/余位/价格）→ 离线自动回退 mock 并提示
- 首页"今日课程"：实时拉当天场次 + **三态判断**（未开始可约 / 进行中红标不可点 / 已结束灰化）
- 详情页：session_id 走真实接口，兼容旧 mock 入口

### 5. 课程编辑网页（web/courses.html）
新增/编辑/删除课程，全属性下拉式（分类/难度/时长/价格/封面/状态/描述 + 排课规则行），末尾"发布课程"按钮（起止日期 + 幂等生成场次），品牌视觉。

---

## 三、关键技术决策（已落地）

1. **课程模板与场次分离**（courses + course_sessions）：模板管"长什么样、多少钱"，场次管"哪天哪时段谁带"；预约/签到/营收全挂场次，改价格不污染历史场次
2. **价格存"分"**（integer）：避免浮点误差，微信支付也按分传
3. **余位不存储，实时计算**（capacity - booked_count）：防双写不一致；抢课用原子 SQL 防超卖
4. **发布幂等**：按 (日期+时段+场地) 去重，重复发布自动跳过
5. **双模式 API**（USE_CLOUD 开关）：本地后端开发调试 / 云函数生产，切换只改一行
6. **离线兜底**：学员端后端不可用时回退 mock 演示数据并提示，演示永不白屏

---

## 四、踩坑与经验教训

### 1. 微信平台限制（最重要）
- **当前 AppID 是"测试号"，云开发/微信支付/真实手机号全部不可用** → 这是今日最大发现与决策点：先本地数据库开发，待注册正式小程序（个体工商户主体，个人主体不能收款）再切云
- **getUserProfile 新版拿不到真实微信昵称**（2.27.1+ 只返回"微信用户"）→ 用官方「头像昵称填写能力」（chooseAvatar + nickname 输入框），这是唯一合规路径
- **getPhoneNumber 需企业认证**才返回真实手机号，个人主体只能走系统授权
- **体验成员机制**：未上线小程序只有体验成员能用真实登录能力 → 多台真机测试要先加体验成员

### 2. 环境/工具类
- **git 锁文件僵尸**（COMMIT_EDITMSG / index.lock 删不掉）→ 根因是僵尸进程占用，重启电脑 + PowerShell `[System.IO.File]::Delete` 解决
- **开发者工具多实例堆叠假死**（最多 11 个）→ 全部 taskkill；顽固进程用 `cli.bat open --port 新端口` 绕过
- **别误杀 WeChatAppEx**：那是微信 PC 版的小程序渲染进程，不是开发工具
- **CLI 的 .cli 通信文件被锁（EPERM）** → 改用工具界面操作，不影响开发
- **真机连不上后端**：预览包里 BASE_URL 是 127.0.0.1 → 真机连自己回环必然失败，必须用局域网 IP（192.168.194.11）重新预览
- **防火墙 3000 端口**：手动建规则需管理员权限失败，但 node.exe 程序级放行（Windows 弹窗允许后自动建）已生效
- **SQLite WAL 文件被僵尸句柄锁** → 只读/无法启动，重启进程即释放

### 3. 编码/技术类
- **SQLite 双引号 `"_"` 是标识符不是字符串**，拼接 key 报 `no such column` → 必须用单引号 `'_'`
- **Python 3.13 struct.unpack 缓冲区必须与格式严格等长**（多 1 字节也报错），解析 PNG 头要精确切片
- **node:sqlite 需 Node 22.5+**（内置 DatabaseSync，零依赖）
- **WXML 不支持调用方法**（.replace 等），i18n 占位符替换必须在 JS 完成
- **异步加载竞态**：首页 onShow 的文案装饰可能在异步数据返回前执行 → 统一走 decorate() 在加载与刷新两处调用，避免卡片文案为空
- **sed 批量改代码会损坏 JS 结构** → 5 个后台页面重写过一次，教训：改代码用文件级编辑而非文本流替换

### 4. 架构/设计类
- 排课"发布"要幂等 + 去重 key，否则重复点击产生脏数据
- 删除课程要级联（规则+场次）且校验订单（有预约 409 拒绝），防止孤儿数据
- 全量功能先用 mock 跑通交互，再按"模板/场次"模型逐层替换为真实数据，每步可验证

---

## 五、当前系统状态

```
前端小程序（20 页）   ←  后端 server/index.js (http://127.0.0.1:3000)   ←  SQLite gym.db（7 表）
                              ├── 课程编辑网页 http://127.0.0.1:3000/ （电脑浏览器）
                              └── 数据：2 教练 / 1 场地 / 2 课程 / 42 周规则 / 126 场次 / 0 预约
真机联调：同 WiFi 访问 http://192.168.194.11:3000
```

**已完成**：登录/注册、用户数据库、课程/场次数据库、课程管理网页、学员端课程列表与今日课程（真实数据 + 三态）
**未完成**（见 TODO.md）：预约/支付落库（bookings 表已建未用）、微信支付、扫码签到真实化、后台统计真实数据、教练端真实数据、云开发部署、正式小程序注册、i18n 全页面覆盖

---

## 六、下一步建议（优先级排序）

1. **P0 预约链路落库**：订课/退订接口 + bookings 表读写 + 我的课程页接真实数据（原子扣减余位）
2. **P0 注册正式小程序**（个体工商户）：解锁微信支付/云开发/真实手机号（文档与代码已就绪：CLOUD_GUIDE.md + cloudfunctions/）
3. **P1 支付接入**：wx.requestPayment + 支付状态流转
4. **P1 后台排课发布真实化**：admin-create-course 从模拟推送改为调用 POST /api/courses 或 /api/sessions
5. **P1 首页/后台统计真实数据**、教练端课表与名单
6. **P2 打磨**：i18n 全页面、封面图配置、空态/加载态、包体积优化

---

*文档整理：2026-08-10 17:05 · 配套文件：TODO.md（待办）、DATA-MODEL.md（数据模型）、CLOUD_GUIDE.md（上云指引）、README.md（使用说明）*
