# 浏览埋点与用户画像设计方案（DESIGN #D5）

> 2026-08-18 设计草案（待用户确认界面与架构后写入 DEV-BACKLOG.md）
> 状态：✅ 已确认（2026-08-18 用户拍板 4 项决策，任务已写入 DEV-BACKLOG.md）

---

## 一、背景与目标

**现状**：运营数据（#D4）与 RMF 分层全部基于**已发生的订课/签到行为**——我们只看得到「结果」，看不到「兴趣」。用户看了什么课、犹豫了什么课、搜了什么词，全部是盲区。

**两个目标**：

1. **捕捉浏览意图**：埋点课程浏览行为，回答「用户看了没订的是哪些课」——这是比订课行为**更实时、更接近购买**的信号。
2. **补社交维度画像**：收集性别/年龄/生日，让推荐从「行为相似」走向「人相似」，并为生日权益（运营钩子）提供数据基础。

---

## 二、浏览埋点设计

### 2.1 埋点页面推荐（按价值排序）

| 优先级 | 页面 | 事件 | 价值 |
|---|---|---|---|
| P0 | **课程详情页** | `course_view`（进入/离开，停留时长） | 核心意图信号：看了没订 = 犹豫/被价格时间满员劝退 |
| P0 | **首页** | `page_view` + 推荐位点击 | 曝光→转化漏斗第一层；推荐位 ROI |
| P0 | **搜索页** | `search`（关键词上报） | 搜索词 = 直接意图；无结果词 = 课程缺口 |
| P1 | 课程列表/分类页 | `course_list_view`（携带筛选条件：品类/时段/教练） | 品类兴趣倾向 |
| P1 | 满员课程排位弹层 | `waitlist_view` | 满员→排位转化率（付费排位转化） |
| P2 | 我的课程页 | `page_view` | 个人空间活跃度 |
| P2 | Banner/活动位 | `banner_click` | 运营位效果评估 |

### 2.2 事件结构

```
POST /api/track/batch   （批量上报，登录态）
[
  { event_type, target_id, source, page, session_id, duration_ms },
  ...
]
```

- `event_type` 白名单：`page_view` / `course_view` / `course_list_view` / `search` / `waitlist_view` / `banner_click`
- `target_id`：课程/场次/banner id（无则不传）
- `source`：进入来源（`home_recommend` / `search` / `category` / `share` / `history`）
- `session_id`：一次浏览旅程（进小程序→切后台/退出），前端生成
- `duration_ms`：停留时长，离开时补报（`onHide`/`onUnload` 时 flush）

**技术要点**：
- 前端 `utils/track.js` 统一模块：本地队列攒批 → 防抖 5s 或 10 条或页面隐藏时上报 → 失败静默重试一次（不阻塞主流程）
- 后端 `course_events` 表：`(openid, event_type, target_id, source, page, session_id, duration_ms, created_at)`，索引 `(openid, event_type, created_at)`
- 校验：事件类型白名单、批量上限 50、无登录态丢弃
- 隐私：PRIVACY.md 更新；「我的」页提供「关闭个性化推荐」开关（关闭后不再采集浏览行为，推荐退回纯订课行为模型）——合规加分项，成本低

### 2.3 基于埋点的分析（运营侧）

| # | 分析 | 怎么算 | 运营动作 |
|---|---|---|---|
| 1 | **浏览→订课漏斗** | 曝光→详情→下单→签到 每层转化率 | 定位流失层：详情→下单掉得狠 = 价格/时间问题 |
| 2 | **意图捕捉（核心）** | 近 7 天浏览同一课程 ≥2 次但未订 = 高意向 | 定向站内信「您关注的 XX 课下周二有新增场次」；加排位优先提醒 |
| 3 | **浏览热度 vs 订课热度** | 浏览高、订课低的课程 | 运营调价/加场/调整时间——「感兴趣但没订」是改排期的依据 |
| 4 | **搜索意图** | 搜索词 TOP + 无结果词 | 无结果词 = 新增课程方向；TOP 词 = 首页推荐位投放 |
| 5 | **停留时长** | 详情页停留 >2 分钟未订 | 开课前 3 小时时机性触达（此时用户大概率还在犹豫） |
| 6 | **时段偏好** | 浏览时段 vs 订课时段 | 推荐时机优化（晚上浏览高峰推课） |
| 7 | **推荐位 ROI** | banner 点击率/转化率 | 决定运营位是否值得占首页 |

---

## 三、社交画像（性别/年龄/生日）

### 3.1 获取方式——先说清楚现实

> ⚠️ **微信不提供真实性别/年龄/生日**：`wx.getUserProfile` 的 gender 字段自 2021 年微信隐私收紧后不再返回真实值；没有任何接口能拿到真实性别/年龄/生日。
> **唯一可靠途径 = 用户自填**，配合激励。

**收集渠道（两阶段）**：

1. **注册后轻引导**：首次进入「我的」页 → 非强制引导卡片（「完善资料，享受生日月专属权益」）→ 可跳过、可稍后
2. **激励收集**：完成画像 → 送能量币（建议 +20）；生日月首订 8 折券（运营钩子，见 3.2）

**字段设计**：

| 字段 | 类型 | 说明 |
|---|---|---|
| `gender` | tinyint | 0 未填 / 1 男 / 2 女 |
| `birthday` | date | 可推出**年龄**与**生日月**（存生日不存年龄——年龄会漂移，生日不会） |
| `region` | varchar | 可选城市（后续可做同城运营，本期可做可不做） |

**合规**：PRIVACY.md 明示用途（个性化推荐 / 生日权益 / 数据分析）；「我的」页资料卡可随时修改、清空；清空后推荐退回纯行为模型。**不设强制，收集率目标 50%+ 靠激励而非逼迫**。

### 3.2 基于画像的运营

| # | 场景 | 打法 |
|---|---|---|
| 1 | **生日月权益（核心钩子）** | 生日当天站内信推送祝福 + 生日月首订 8 折券 + 能量币彩蛋 → 一年一次的精准触达时点，且用户自己提供（无成本） |
| 2 | **冷启动推荐** | 新用户无行为数据时：按年龄段的课程分布推荐（用**存量订课数据自动学**——某年龄段学员实际订什么课，不拍脑袋）；性别同理（女性学员高频课程集合 → 新女性用户冷启动） |
| 3 | **沉睡用户定制触达** | RMF 沉睡（14/30 天） × 画像组合：35+ 沉睡 →「康复/瑜伽新课」文案；年轻沉睡 →「新搏击课/限时活动」文案——同一群用户，文案命中率更高 |
| 4 | **画像完整度运营** | 运营看板：X% 用户缺生日 → 定向引导填单（缺生日的用户价值 = 潜在生日触达位） |
| 5 | **课程排期参考** | 学员年龄/性别分布 → 课程结构调整依据（如晚间班偏年轻、周末班偏亲子/中老年） |

---

## 四、程序架构

```
小程序                    后端
utils/track.js ──批量──▶ POST /api/track/batch ──▶ course_events 表
我的页资料卡 ──GET/PUT──▶ /api/me/profile  ──────▶ users 表 +gender/birthday/region
运营数据 tab 新增折叠卡 ──▶ GET /api/admin/events-analysis（ADMIN_PATHS 保护）
users-analysis 扩展：筛选加 gender/age_range/birthday_month，CSV 加画像列
```

**改动面清单**：

| 层 | 改动 |
|---|---|
| 数据库 | `course_events` 新表；`users` 加 3 列（gender/birthday/region）——双方言建表（SQLite+MySQL） |
| 后端 | `POST /api/track/batch`（白名单校验）；`GET/PUT /api/me/profile`；`GET /api/admin/events-analysis`（漏斗/意图人群/搜索词/热度对比）；users-analysis 筛选+CSV 扩展 |
| 前端 | `utils/track.js`；首页/详情/搜索/列表/排位弹层埋点接入（约 5 页）；「我的」页画像卡片；运营数据 tab「浏览分析」折叠卡；PRIVACY.md 同步 |
| 测试 | TRK-xx（埋点校验/批量上限/未登录丢弃）、PROF-xx（画像读写/清空）、EVT-xx（浏览分析聚合）+ coverage 探针；新接口 = 新测试（CONVENTIONS 规矩 #2） |

---

## 五、已确认决策（2026-08-18 用户拍板）

1. **埋点范围**：仅 P0 三事件（首页曝光 / 课程详情浏览含停留时长 / 搜索词）。banner 点击、排位弹层、列表筛选 **本期不做**（后续按需加，事件表结构已预留 event_type 白名单扩展）。
2. **生日激励**：填单送 20 能量币 + 生日月首订 8 折券（成本项已拍板）。8 折券取整规则：`amountFen × 0.8` 向下取整到分，与会员价「向下取整」口径一致。
3. **「关闭个性化推荐」开关**：**后续做**。本期埋点采集先上线，隐私文案进 PRIVACY.md，开关后续版本补。
4. **本期范围**：**先收集后推荐**——本期只做埋点采集 + 画像收集 + 浏览分析看板（含意图人群清单展示）+ CSV 画像列；推荐引擎（冷启动/自动意图触达）下期基于积累数据单独立项（#D6 预留）。

