# 微信开发者工具 - 开通云开发操作指引（找不到按钮怎么办）

> 你的项目已满足云开发的全部前提（`cloudfunctions/` 目录 ✓、`cloudfunctionRoot` 配置 ✓、真实 AppID ✓），只差最后一步：在工具里开通云环境。

---

## 第一步：确认你打开的是这个项目

1. 微信开发者工具左上角，确认**当前项目名称**是「综合训练馆订课系统」
2. 如果不是，点右上角「**+ 号 / 导入项目**」，选择目录：
   `C:\Users\tianli\WorkBuddy\综合训练馆订课系统`
3. 导入时 AppID 选择「**使用自己的 AppID**」，填入 `wx509088154a505409`
   （不要选测试号！测试号无法开通云服务）

---

## 第二步：找云开发入口（三种版本，任选其一）

### 方法一（最常见）：顶部工具栏
- 工具栏上有「**云开发**」图标按钮（云朵样式），直接点击
- 位置：工具栏中部，通常在「编译」按钮旁边

### 方法二：菜单栏
- 顶部菜单「**工具**」→「**云开发**」
- 或「**工具**」→「**云开发控制台**」

### 方法三：资源管理器（侧边栏）
- 左侧「资源管理器」面板中，展开 `cloudfunctions` 文件夹
- **右键 `login` 文件夹** → 如果出现「**上传并部署：云端安装依赖**」选项 → 说明云开发已就绪
- 右键菜单里同时会有「**云开发控制台**」入口

---

## 第三步：开通云环境

1. 点击「云开发」后，首次会弹出开通引导：
   - **环境名称**：填 `gym-prod`（或任意英文名）
   - **计费方式**：选「**按量付费**」（有免费额度，开发期基本不花钱）
   - 创建后等待约 1 分钟初始化
2. 开通后记下**环境 ID**（形如 `gym-prod-xxxxxxxx`）

---

## 第四步：创建数据库集合

1. 云开发控制台 → 左侧「**数据库**」
2. 点「**+ 创建集合**」→ 名称填 **`users`**（必须精确）
3. 权限：选「仅创建者可读写」（默认即可）

---

## 第五步：上传云函数

1. 回到开发者工具，左侧资源管理器展开 `cloudfunctions`
2. **右键 `login`** →「**上传并部署：云端安装依赖**」
3. **右键 `users`** →「**上传并部署：云端安装依赖**」
4. 等待上传完成

---

## 第六步：切换小程序到云开发

编辑 `miniprogram/utils/api.js`，改这两行：
```js
const USE_CLOUD = true;                 // 改为 true
const CLOUD_ENV = 'gym-prod-xxxxxxxx';  // 改为你的环境 ID
```

编译运行 → 登录 → 首次注册 → 云开发控制台数据库 `users` 集合中能看到真实 openid。

---

## 如果还是找不到按钮/开通失败

| 现象 | 原因 | 解决 |
|---|---|---|
| 工具栏没有云开发按钮 | 项目可能还是测试号 | 重新导入项目，AppID 填 `wx509088154a505409` |
| 提示「测试号不能使用云服务」 | AppID 是测试号 | 项目详情里把 AppID 改成真实小程序 |
| 点击云开发无反应 | 工具版本过旧 | 工具 → 帮助 → 关于，升级到最新稳定版 |
| 右键云函数没有上传选项 | cloudfunctionRoot 未识别 | 检查 project.config.json 中 `"cloudfunctionRoot": "cloudfunctions/"` |

---

## 备选：网页版云开发控制台

如果工具内找不到，也可以直接打开网页版（需扫码登录）：
- 控制台地址：https://console.cloud.tencent.com/tcb
- 或从微信公众平台 https://mp.weixin.qq.com → 左侧「开发」→「云开发」

---

## 完成后告诉我

开通后把**环境 ID** 发给我，我帮你：
1. 把 `api.js` 的 `USE_CLOUD` 和 `CLOUD_ENV` 配置好
2. 验证云函数能正常调用
3. 把后台学员管理切到云数据库
