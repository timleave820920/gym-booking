# 固定二维码签到设计方案（DESIGN #D13）

> 2026-08-19 提出并拍板｜状态：设计中（待确认后写入 DEV-BACKLOG）
> 一句话：**场馆张贴一个固定的官方小程序码，学员相机扫它即签到——无歧义自动签，有歧义弹框选课；教练核销入口移除（只留扫码）。**

## 一、用户拍板口径（2026-08-19）

| 决策点 | 拍板 |
|---|---|
| 固定码形态 | **官方小程序码**（wxacode，相机直接扫 → 进签到页） |
| 签到判定 | 无歧义 → 直接「签到成功」；有歧义（同时段多课）→ 弹框选课后「签到成功」 |
| 教练核销 | **只留扫码**——教练端核销入口移除 |

## 二、机制设计

### 2.1 固定码

- 后端调微信 `wxacode.getUnlimited`（生产 appid/secret 已配置）生成固定小程序码：`scene=checkin`、`path=pages/checkin/index`
- 生成一次缓存（文件存 /images/checkin-qr.png 或内存），web 后台「运营数据」tab 提供「签到码」区块：展示二维码图片 + 「重新生成」按钮
- 码内容固定（无学员信息）——签到主体是**扫码设备的登录账号**，码本身公开无泄露风险

### 2.2 签到页（pages/checkin/index）

- onLoad 解析 `scene=checkin`（或 query）→ 校验 → 自动触发签到判定
- 判定结果三态：
  | 窗口内订课数 | 行为 |
  |---|---|
  | 0 | 提示「当前没有可签到的课程」（未订课 / 不在签到窗口） |
  | 1 | 自动签到 → 「✅ 签到成功」（展示课程名/时间） |
  | ≥2 | 弹框列出候选课（课程名+时间）→ 学员选 → 签到成功 |

### 2.3 判定规则（后端）

```
POST /api/checkin/scan（openid + 固定 token 校验）
  1. 取学员 status='booked' 且处于签到窗口（开课前 30 分钟～课后 30 分钟，B1/DESIGN #D1 沿用）的订课
  2. count === 0 → 无课可签
  3. count === 1 → 直接调 checkinBooking 签到
  4. count >= 2 → 返回候选列表（不落库）
POST /api/checkin/select { bookingId } → 学员选定后签到（校验该 booking 属当前登录学员且在窗口内）
```

- 复用现有 `checkinBooking`（时间窗口/已签到幂等/状态校验全部沿用）
- 签到成功写 checkin_at → 成长统计/签到率/结算联动不变（B1 口径不变，只是签到入口变了）

### 2.4 教练核销移除

- 教练端核销入口（coach-home 凭证码核销 UI）移除
- 后端 `checkinByCode` 接口与 `checkin_code` 随机码：**保留接口作异常兜底还是彻底删除**——待确认（删除影响既有测试较多；保留则无前端入口，仅接口存在）

## 三、界面设计

### 学员签到页
```
┌──────────────────────────────┐
│          📷 签到              │
│  扫码成功                     │
│  ┌────────────────────────┐  │
│  │ ✅ 签到成功             │  │
│  │ 综合体能训练 · 10:00    │  │
│  │ 教练 · 阿凯             │  │
│  └────────────────────────┘  │
│      （自动判定唯一课程）      │
└──────────────────────────────┘

（有歧义时）弹框：
┌──────────────────────────────┐
│  检测到多节可签到课程，请选择   │
│  ○ 综合体能 · 10:00-11:00    │
│  ○ 力量训练 · 10:30-11:30    │
│           [确认签到]          │
└──────────────────────────────┘
```

### web 后台「签到码」区块（运营数据 tab）
```
📱 签到码（场馆张贴用）   [重新生成]
[    小程序码图片        ]
路径 pages/checkin/index · scene=checkin
```

## 四、安全与防滥用

- 码公开无风险：签到主体 = 扫码设备登录账号（openid 服务端取，防伪造）
- 无法防「把手机交给别人代签」——任何签到机制都有此边界（现教练核销同样不能 100% 验证），接受
- 固定码防串号：码不含订课信息，无猜码/串号问题（现有随机码机制本身要防的已不存在）

## 五、测试计划

- **CKIN-01**：窗口内唯一订课 → 自动签到成功（checkin_at 落库）
- **CKIN-02**：窗口内无订课 → 「当前没有可签到的课程」
- **CKIN-03**：窗口内多订课 → 返回候选列表（不落库）
- **CKIN-04**：弹框选定 → 签到成功；选非本人订课 → 拒绝
- **CKIN-05**：窗口外（课前 31 分钟/课后 31 分钟）→ 拒绝（B1 边界沿用）
- **CKIN-06**：已签到再扫 → 幂等提示「已签到」
- **CKIN-07**：未登录 → 引导登录
- **CKIN-08**：scene 非 checkin 的码 → 拒绝
- **FRONT-xx**：签到页 + web 签到码区块静态断言；教练端核销入口移除断言
- coverage 探针：checkin/scan、checkin/select

## 六、实施步骤（确认后写入 DEV-BACKLOG）

1. **P0｜后端**：wxacode 固定码生成接口（缓存 + 重新生成）+ POST /api/checkin/scan（判定三态）+ POST /api/checkin/select + CKIN-01~08
2. **P0｜前端**：pages/checkin/index 签到页（scene 解析 + 自动判定 + 弹框选择）+ 教练端核销入口移除
3. **P1｜后台**：web 运营数据 tab「签到码」区块（展示 + 重新生成）
4. **P1｜测试**：CKIN 用例 + FRONT 断言 + coverage 探针

## 七、待确认点

1. 教练端核销：**彻底删除**后端 checkinByCode/随机码（测试同步删改），还是**接口保留作兜底**（无前端入口）？
2. 学员端现有「签到凭证」页（展示二维码/随机码给教练扫的页面）——随教练核销移除一并移除，认可？
